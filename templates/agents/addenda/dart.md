
## Dart/Flutter Stack Notes

- Follow Effective Dart style. Use `dart analyze` as the quality gate.
- Prefer composition over inheritance for widgets.
- Use `const` constructors wherever possible for performance.
- Flag state management anti-patterns: global mutable state, deeply nested setState.
- Check for missing `key` parameters on list-built widgets.
