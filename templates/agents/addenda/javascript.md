
## JavaScript Stack Notes

- Prefer `const` over `let`; never use `var`.
- Use strict equality (`===`) unless comparing with `null`/`undefined` intentionally.
- Prefer async/await over raw Promises for readability.
- Flag callback-heavy code as a refactoring opportunity.
- Check for missing error handling in async functions.
