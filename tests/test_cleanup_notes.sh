#!/usr/bin/env bash
# =============================================================================
# test_cleanup_notes.sh — m24 skip-stub.
#
# Exercised count_unresolved_notes from the deleted lib/notes_cleanup.sh.
# The Go port lives in internal/notes/cleanup.go::RemoveDone /
# CountUnchecked, and the CLI surface is `tekhton note count`.
# =============================================================================
echo "test_cleanup_notes.sh: skipped (m24 — see internal/notes/cleanup.go)"
exit 0
