# Config that does NOT set TEKHTON_UI_GATE_FORCE_NONINTERACTIVE.
# Parity script runs this fixture twice — once with no CI env vars and once
# with GITHUB_ACTIONS=true — to assert the m138 auto-elevation rule.
PROJECT_NAME="ci-default"
CLAUDE_STANDARD_MODEL="claude-sonnet-4-6"
ANALYZE_CMD="echo ok"
