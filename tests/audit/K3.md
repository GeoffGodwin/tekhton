# Bucket K3 Audit — Long-tail (N-Z)

Audited 72 tests. Verdict counts: KEEP=70, DELETE-STALE=0, PORT-TO-GO=0, NEEDS-REVIEW=2.

## Verdicts

| Test | Verdict | Reason |
|---|---|---|
| test_pipeline_order.sh | KEEP | Exercises `lib/pipeline_order.sh` (240 lines, live). |
| test_pipeline_order_m110.sh | KEEP | Exercises M110 additions in `lib/pipeline_order.sh` / `pipeline_order_policy.sh`, both live bash. |
| test_pipeline_order_policy.sh | KEEP | Exercises `lib/pipeline_order_policy.sh` (131 lines, live). |
| test_pipeline_runner.sh | KEEP | m18 smoke test for `tekhton pipeline run-attempt` Go binary plus stage stubs — cross-language integration coverage. |
| test_print_box_frame.sh | KEEP | Exercises `_print_box_frame` and `report_retry` in `lib/common_box.sh` (live). |
| test_pristine_state_enforcement.sh | KEEP | Exercises live `lib/test_baseline.sh`, completion/acceptance gates, pre-run clean sweep config (M92). |
| test_progress.sh | KEEP | Exercises `lib/progress.sh` (248 lines, live). |
| test_progress_bar_no_subshells.sh | KEEP | Exercises `_render_progress_bar` in live `lib/milestone_progress_helpers.sh`. |
| test_project_version_bump.sh | KEEP | Exercises live `lib/project_version_bump.sh` (240 lines). |
| test_project_version_detect.sh | KEEP | Exercises live `lib/project_version.sh` (198 lines). |
| test_project_version_hint.sh | KEEP | Exercises live `lib/project_version.sh` get_version_bump_hint. |
| test_prompt_helpers.sh | KEEP | Exercises `lib/prompts_interactive.sh` (138 lines, live, distinct from m15 shim). |
| test_prompt_isolation_guardrails.sh | KEEP | Greps live prompt template files (`prompts/tester.prompt.md` etc.) for required content. |
| test_prompt_rendering.sh | NEEDS-REVIEW | Exercises `render_prompt` shim into Go engine; engine itself fully covered by `internal/prompt/prompt_test.go`. Bash test still validates shim variable-export path — could keep as integration smoke or delete given Go coverage. |
| test_prompt_templates.sh | KEEP | Validates presence of template files in `prompts/` directory (still authoritative). |
| test_quota.sh | KEEP | Exercises `lib/quota.sh` (293 lines, live — quota glue still in bash). |
| test_quota_roundtrip.sh | KEEP | Exercises `enter_quota_pause`/`exit_quota_pause` round trip in live `lib/quota.sh`. |
| test_quota_sleep_chunked.sh | KEEP | Exercises `_quota_sleep_chunked` in live `lib/quota_sleep.sh` (M124). |
| test_readme_split.sh | KEEP | Docs invariant: README ≤300 lines and docs/ links resolve. |
| test_recovery_block.sh | KEEP | Exercises `_print_recovery_block` in live `lib/orchestrate_diagnose.sh`. |
| test_rejection_artifact_preservation.sh | KEEP | Exercises `_choose_resume_start_at` in live `lib/orchestrate_aux.sh` (M93). |
| test_replan_consolidation_rule.sh | KEEP | Validates content of live `prompts/replan.prompt.md`. |
| test_replan_detect.sh | KEEP | Exercises `detect_replan_required`/`trigger_replan` in live `lib/replan_midrun.sh`. |
| test_repo_map_fixtures.sh | KEEP | Exercises tree-sitter fixture project + live `lib/indexer*.sh` glue. |
| test_repo_map_integration.sh | KEEP | End-to-end repo-map flow against live `lib/indexer*.sh` + `tools/repo_map.py`. |
| test_report.sh | KEEP | Exercises `print_run_report` in live `lib/report.sh` (265 lines). |
| test_report_color.sh | KEEP | Exercises `_report_colorize` in live `lib/report.sh`. |
| test_report_error.sh | KEEP | Exercises `report_error` in live `lib/common_box.sh`. |
| test_report_retry_formatting.sh | KEEP | Exercises `report_retry` in live `lib/common_box.sh`. |
| test_rescan.sh | KEEP | Exercises `rescan_project` + helpers in live `lib/rescan.sh` / `lib/rescan_helpers.sh`. |
| test_rescan_metadata.sh | KEEP | Exercises `_record_scan_metadata` in live `lib/rescan_helpers.sh`. |
| test_resilience_arc_integration.sh | KEEP | Cross-cuts m126–m133 bash arc (preflight gate, build-fix loop, recovery routing) — all still bash. |
| test_resilience_arc_json_escape.sh | KEEP | Tests `_arc_json_escape` helper in `tests/resilience_arc_fixtures.sh` (live). |
| test_resilience_arc_loop.sh | KEEP | Exercises `run_build_fix_loop` in live `lib/coder_buildfix.sh`/`lib/coder_buildfix_helpers.sh`. |
| test_retry_config_defaults.sh | KEEP | Verifies retry defaults via `lib/config.sh` shim → Go `internal/config`; integration smoke. |
| test_review_cache_invalidation.sh | KEEP | Exercises `run_stage_review` in live `stages/review.sh`. |
| test_review_effective_coder_turns.sh | KEEP | Grep regression on `stages/review.sh` (live). |
| test_review_inloop_recalibration.sh | KEEP | Exercises in-loop recalibration in live `stages/review.sh`. |
| test_review_map_files_global.sh | KEEP | Verifies `_REVIEW_MAP_FILES` scope comment in live `stages/review.sh`. |
| test_rule_max_turns_consistency.sh | KEEP | Exercises `_read_diagnostic_context` in live `lib/diagnose.sh` plus state shim — m10 cutover already accounted for in test header. |
| test_scout_repo_map_tools.sh | KEEP | Exercises Scout tool-allowlist gating in live coder/scout glue. |
| test_security_stage.sh | KEEP | Exercises live `stages/security.sh` helpers (`_parse_security_findings`, etc.). |
| test_self_host_dry_run_gate.sh | KEEP | Validates live `scripts/self-host-check.sh` dry-run gating. |
| test_semver_lt.sh | KEEP | Exercises `_semver_lt` in live `lib/update_check.sh` (290 lines). |
| test_should_claim_notes.sh | KEEP | Exercises `should_claim_notes` in live `lib/notes.sh`. |
| test_specialist_ui.sh | KEEP | Exercises live `lib/specialists.sh` UI/UX specialist (M59). |
| test_specialists.sh | KEEP | Exercises live `lib/specialists.sh` framework. |
| test_stage_envelope.sh | KEEP | Exercises `emit_stage_envelope`/`stage_envelope_install_all` in live `lib/stage_envelope.sh` (207 lines, m18 wrapper helpers). |
| test_stage_summary_model_display.sh | KEEP | Exercises `_extract_stage_turns` in live `lib/metrics.sh` and STAGE_SUMMARY format. |
| test_startup_auto_migrate.sh | KEEP | Exercises live milestone-DAG auto-migration startup block in `tekhton.sh`. |
| test_startup_cleanup.sh | KEEP | Exercises `clear_completed_*`/`clear_resolved_*` in live `lib/notes.sh` and `lib/drift_artifacts.sh`. |
| test_state_cli_exit_codes.sh | KEEP | Tests `tekhton state read` CLI exit-code contract — explicitly the bash-layer coverage for the m03 Go wedge. |
| test_state_error_classification.sh | KEEP | Verifies state.sh shim writes JSON envelope via Go writer with correct error-classification fields. |
| test_state_roundtrip.sh | KEEP | Exercises state.sh shim → Go writer/reader; thin bash but valid integration smoke. |
| test_structured_index.sh | KEEP | Exercises live `lib/crawler.sh` structured index output (M67). |
| test_tekhton_dir_root_cleanliness.sh | KEEP | Verifies `_FILE` defaults in live `lib/config_defaults.sh` (shim, but defaults still emitted via Go). |
| test_test_audit_split.sh | KEEP | Verifies M95 module split of live `lib/test_audit*.sh` files. |
| test_test_baseline.sh | KEEP | Exercises live `lib/test_baseline.sh`. |
| test_tester.sh | KEEP | Exercises `_run_tester_write_failing` in live `stages/tester.sh`. |
| test_tester_timing_initialization.sh | KEEP | Verifies `_TESTER_TIMING_WRITING_S` init in live `stages/tester.sh`. |
| test_tester_upstream_error.sh | KEEP | Exercises UPSTREAM error handling in live `stages/tester.sh`. |
| test_trim_document_preamble.sh | KEEP | Exercises `_trim_document_preamble` in live `lib/plan_batch.sh`. |
| test_update_check_disabled.sh | KEEP | Exercises `check_for_updates` in live `lib/update_check.sh`. |
| test_usage_threshold_missing_arg.sh | KEEP | Exercises CLI arg parsing in `tekhton.sh` (dispatcher) — still bash entry point. |
| test_v4_env_contract.sh | KEEP | m26 cross-language parity test — composed env from Go EnvBuilder against bash readers. |
| test_validate_config.sh | KEEP | Exercises live `lib/validate_config.sh` (M83). |
| test_validate_config_arc.sh | KEEP | Exercises live `lib/validate_config_arc.sh` (M136). |
| test_validate_config_design_file.sh | KEEP | Exercises DESIGN_FILE checks in live `lib/validate_config.sh`. |
| test_wedge_audit.sh | KEEP | Exercises live `scripts/wedge-audit.sh` (m04 invariant gate). |
| test_wedge_audit_m10.sh | KEEP | Exercises m10 patterns in live `scripts/wedge-audit.sh`. |
| test_with_notes_flag.sh | KEEP | Exercises `--with-notes` flag parsing + `should_claim_notes` in live `lib/notes.sh`. |
| test_worktree_gitignore_coverage.sh | NEEDS-REVIEW | Verifies `.gitignore` pattern for `.claude/worktrees/` — no longer associated with a Tekhton subsystem post-V4; may be a stale concern depending on whether worktrees are still produced anywhere. |

## Coverage gaps noted

- `test_prompt_rendering.sh` and `test_state_roundtrip.sh` overlap heavily with Go-side tests now that the engines moved to `internal/prompt` and `internal/state`. Worth deciding whether the bash integration shims warrant their own smoke or whether the Go tests suffice.
- `test_v4_env_contract.sh` is the only bash-layer probe of `internal/runner.EnvBuilder.AsKV`; the briefing notes m26 is a Go-side concern but the contract has no equivalent Go integration test that crosses back into bash readers.
- `test_pipeline_runner.sh` (m18) is the only bash test exercising `tekhton pipeline run-attempt` end-to-end with stage stubs; worth ensuring `internal/pipeline/runner_test.go` covers the same envelope+verdict propagation.
- `test_quota_*` cluster still all-bash even though m08–m10 supervisor was the surface most likely to touch quota internals — confirm `internal/supervisor/quota_test.go` matches behavior exercised here.
- `test_state_cli_exit_codes.sh` is explicitly the bash-side proof for the m03 Go wedge's exit-code contract; should not be deleted with the rest of the state shim.
