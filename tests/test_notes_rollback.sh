#!/usr/bin/env bash
# =============================================================================
# test_notes_rollback.sh — m24 skip-stub.
#
# lib/notes_rollback.sh was deleted; the snapshot + restore APIs port
# to internal/notes/rollback.go and the user surface is
# `tekhton note rollback <SNAPSHOT_ID>`.
# =============================================================================
echo "test_notes_rollback.sh: skipped (m24 — see internal/notes/rollback.go)"
exit 0
