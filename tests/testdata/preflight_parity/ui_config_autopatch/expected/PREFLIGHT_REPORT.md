# Pre-flight Report — TIMESTAMP

## Summary
✓ 0 passed  ⚠ 1 warned  ✗ 0 failed  🔧 1 auto-fixed

## Checks

### ⚠ Tools (UI_TEST_CMD)
`playwright_test` (from UI_TEST_CMD) is not found in PATH.

### 🔧 UI Config (Playwright) — html reporter
Auto-patched reporter: 'html' → CI-guarded form in playwright.config.ts.
Original saved to: BACKUP_PATH
The gate will use 'dot' reporter in CI mode (no interactive server).
Review and commit the change when satisfied.

