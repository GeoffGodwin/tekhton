# Coder (env-contract fixture stub)

This is a stub agent role for `tests/testdata/env_contract/`. The m27.3
parity test never actually invokes this agent — `TEKHTON_AGENT_BINARY`
is repointed at `testdata/fake_agent.sh` before stages run.
