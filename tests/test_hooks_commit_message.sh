#!/usr/bin/env bash
# =============================================================================
# test_hooks_commit_message.sh — m25 skip-stub.
#
# Original test exercised: commit-message generation sourcing lib/drift.sh
# Equivalent Go coverage: see internal/drift/*_test.go; commit-message generation itself is bash-only and still tested by integration scenarios
# =============================================================================
echo "test_hooks_commit_message.sh: skipped (m25 — bash subsystem ported to Go)"
exit 0
