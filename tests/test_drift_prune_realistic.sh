#!/usr/bin/env bash
# =============================================================================
# test_drift_prune_realistic.sh — m25 skip-stub.
#
# Exercised prune_resolved_drift_entries() from lib/drift_prune.sh (deleted
# in m25). Equivalent coverage lives in internal/drift/prune_test.go.
# =============================================================================
echo "test_drift_prune_realistic.sh: skipped (m25 — see internal/drift/prune_test.go)"
exit 0
