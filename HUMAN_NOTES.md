# Human Notes
<!-- notes-format: v2 -->
<!-- IDs are auto-managed by Tekhton. Do not remove note: comments. -->

Add your observations below as unchecked items. The pipeline will inject
unchecked items into the next coder run and archive them when done.

Use `- [ ]` for new notes. Use `- [x]` to mark items you want to defer/skip.

Prefix each note with a priority tag so the pipeline can scope runs correctly:
- `[BUG]` — something is broken, needs fixing before new features
- `[FEAT]` — new mechanic or system, architectural work
- `[POLISH]` — visual/UX improvement, no logic changes


## Features

## Bugs
- [x] [BUG] The answer template file generated by `tekhton --plan` generates some lines with bad escapes where double quotes are used within the template. This causes the generated prompt files to have broken markdown formatting. The template needs to be fixed to properly escape double quotes. Check and ensure that this does not happen with the web mode of the `tekhton --plan` command as well.
One example is a guidance line `    guidance: "...```toml [core] output_format = "text" color = "auto"...```..."` which generates a broken markdown line in the prompt file. The template should be updated to escape the inner double quotes so that the generated prompt files have correct markdown formatting. Be thorough and complete in fixing this issue across all relevant lines in the template to prevent any broken formatting in the generated prompt files.
- [x] [BUG] When `tekhton --plan --answers FILE` is run the answers get overwritten with a blank template while loading, erasing all the answers before they can be used. The template loading logic needs to be fixed to preserve existing answers instead of overwriting them with a blank template. This is critical to ensure that the answers generated by the plan command are not lost and can be properly utilized in subsequent steps. Review the code responsible for loading the answer template and make necessary adjustments to prevent this data loss issue. 

## Polish
