#### Milestone 1.1.2: Cobra Root Command Wiring

Add the Cobra dependency and replace the stub `main()` with a real root command that exposes `--help` and exits non-zero on bare invocation. Still no version injection — `--version` lands in 1.1.3.

Files to create:
- `go.sum` (generated by `go mod tidy` after adding Cobra)

Files to modify:
- `go.mod` (add `github.com/spf13/cobra` dependency)
- `cmd/tekhton/main.go` (full Cobra root command setup)

Acceptance criteria:
- `bin/tekhton --help` produces standard Cobra output (Usage, Available Commands, Flags sections).
- Bare `bin/tekhton` invocation exits non-zero and prints help (use `SilenceUsage: false` and return an error from `RunE`, or rely on Cobra's no-subcommand behavior — pick one and make it deterministic).
- `go.sum` is committed and `go mod verify` passes.
- `make build` still produces `bin/tekhton` with `CGO_ENABLED=0`.
- `make test` exits 0.
- No file under `lib/`, `stages/`, `prompts/`, or `tools/` is modified.

Watch For:
- Cobra's default behavior on bare invocation is to print help and exit 0 — you must explicitly cause non-zero exit (e.g., `RunE` returning an error, or `Args: cobra.NoArgs` combined with a required subcommand).
- Do NOT add a `--version` flag yet; that's wired in 1.1.3 once the version package exists.
- Keep the root command's `Use` field set to `tekhton` (not the full module path) so help output reads correctly.

Seeds Forward:
- Milestone 1.1.3 depends on the root command struct being addressable so the `--version` flag can be attached.

