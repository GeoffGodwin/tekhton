## Relevant Files

- **CLAUDE.md** (lines 140, 167-416) — Contains two documentation issues reflected in unresolved drift observations
  - Line 140: Template Variables table lists `PLAN_DESIGN_CONTENT` but actual variable is `DESIGN_CONTENT` (exported in stages/plan_generate.sh:40)
  - Lines 167-416: "Completed Initiative" section structure and milestone descriptions

- **stages/plan_generate.sh** (lines 40-41) — Confirms actual variable name: exports `DESIGN_CONTENT`, not `PLAN_DESIGN_CONTENT`

- **prompts/plan_generate.prompt.md** (line 18) — Uses `{{DESIGN_CONTENT}}` in the template

- **lib/drift.sh** (lines 128-184) — Contains `resolve_drift_observations()` function that moves observations from Unresolved to Resolved section of DRIFT_LOG.md

- **DRIFT_LOG.md** (lines 7-9) — Contains the 2 unresolved observations that need to be formally marked as resolved

- **NON_BLOCKING_LOG.md** (line 24) — Shows that the second observation (CLAUDE.md stale milestones) was already addressed per "Resolved: Updated header..., updated summary paragraph..., marked all milestones [DONE]." but not yet formally moved in drift log

## Key Symbols

- `DESIGN_CONTENT` / `PLAN_DESIGN_CONTENT` — Variable naming mismatch between documentation and implementation
- `resolve_drift_observations()` — lib/drift.sh:128 — Function to move observations from Unresolved to Resolved in DRIFT_LOG.md
- `## Unresolved Observations` / `## Resolved` — Sections in DRIFT_LOG.md that partition observations by status

## Suspected Root Cause Areas

1. **Variable name inconsistency (Observation 1)**: CLAUDE.md Template Variables table was not updated when the variable was exported as `DESIGN_CONTENT` instead of `PLAN_DESIGN_CONTENT` in stages/plan_generate.sh. Simple documentation fix.

2. **Stale milestone section in documentation (Observation 2)**: The milestone descriptions in CLAUDE.md (lines 213-416) were updated to [DONE] markers and the header changed to "Completed Initiative", but the drift observation was never formally marked as resolved using `resolve_drift_observations()` in the drift log management. The work was done (per NON_BLOCKING_LOG.md item 24) but the drift log metadata wasn't updated.

## Complexity Estimate

**Files to modify**: 2 (CLAUDE.md for both fixes, optional: lib/drift.sh or script to call resolve_drift_observations)

**Estimated lines of change**:
- CLAUDE.md line 140: 1 line (change `PLAN_DESIGN_CONTENT` → `DESIGN_CONTENT`)
- DRIFT_LOG.md: 2 lines moved from Unresolved to Resolved section (can be done via shell script or manual edit)
- Total: ~3-5 lines

**Interconnected systems**: low (documentation changes only, no code logic changes)

**Recommended coder turns**: 5-10 (straightforward documentation fixes)

**Recommended reviewer turns**: 3-5 (simple verification of variable names and drift log moves)

**Recommended tester turns**: 5-10 (verify no broken references, ensure drift log format is valid)
