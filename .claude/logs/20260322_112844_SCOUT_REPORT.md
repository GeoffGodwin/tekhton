# Scout Report — Milestone 2: Sliding Window & Plan Generation Integration

## Relevant Files

- `lib/milestone_window.sh` — **To create** — New library providing `build_milestone_window(model)` function to assemble character-budgeted milestone context blocks with priority-based inclusion (active → frontier → on-deck)
- `stages/coder.sh` — Lines 239–249, 338 — Replace static MILESTONE_BLOCK assignment with call to `build_milestone_window()` when manifest exists
- `stages/plan_generate.sh` — Lines 70–99 — Post-processing of generated CLAUDE.md: extract milestone blocks into individual `.md` files in `.claude/milestones/`, generate MANIFEST.cfg, remove blocks from CLAUDE.md, add pointer comment
- `lib/orchestrate_helpers.sh` — Lines 12–50 — `_run_auto_advance_chain()` already uses `find_next_milestone()` but needs to call DAG-aware `dag_find_next()` when manifest exists
- `lib/context.sh` — Lines 70–85 — Already has `_add_context_component()` for budget accounting; milestone window will integrate here
- `tekhton.sh` — Lines 312–317 — Already sources milestone DAG libraries; add source for `milestone_window.sh` near line 318 (after milestone_split.sh); add auto-migration trigger at startup
- `lib/config.sh` — Path resolution (around line 50–100) — May need MILESTONE_DIR path resolution if not already present
- `lib/milestones.sh` — Lines 15–35 — `parse_milestones()` function extracts milestones; Milestone 2 needs `parse_milestones_auto()` wrapper to handle both file-based and inline milestones (already has dual-path logic in Milestone 1 infrastructure)
- `lib/milestone_dag_migrate.sh` — Already exists from Milestone 1 — Provides `migrate_inline_milestones()` function; tekhton.sh startup will call this for auto-migration
- `lib/config_defaults.sh` — Lines 102–108 — Config keys already defined: `MILESTONE_WINDOW_PCT=30`, `MILESTONE_WINDOW_MAX_CHARS=20000`, `MILESTONE_AUTO_MIGRATE=true`

## Key Symbols

- `build_milestone_window(model)` — `lib/milestone_window.sh` (to create) — Assembles budgeted milestone block from DAG manifest
- `_compute_milestone_budget(model)` — `lib/milestone_window.sh` (to create) — Calculates available characters: `min(available * MILESTONE_WINDOW_PCT/100, MILESTONE_WINDOW_MAX_CHARS)`
- `_milestone_priority_list()` — `lib/milestone_window.sh` (to create) — Returns ordered milestone IDs (active first, then frontier, then on-deck)
- `dag_find_next()` — `lib/milestone_dag.sh` (already exists) — DAG-aware next milestone lookup with dependency checks
- `dag_get_frontier()` — `lib/milestone_dag.sh` (already exists) — Returns milestones whose deps are all done
- `load_manifest()` — `lib/milestone_dag_io.sh` (already exists) — Loads manifest into parallel arrays
- `save_manifest()` — `lib/milestone_dag_io.sh` (already exists) — Writes manifest atomically
- `_add_context_component()` — `lib/context.sh` (already exists) — Adds named context component to budget report
- `render_prompt()` — `lib/prompts.sh` (already exists) — Template substitution with `{{IF:MILESTONE_BLOCK}}...{{ENDIF:MILESTONE_BLOCK}}`

## Suspected Root Cause Areas

- **`lib/milestone_window.sh` missing entirely** — New file must be created with full DAG integration and budget calculation logic
- **`stages/coder.sh` line 239–249** — MILESTONE_BLOCK is currently a static assignment; must be replaced with `build_milestone_window()` call when manifest exists, fallback to existing behavior when absent
- **`stages/plan_generate.sh` line 70–99** — Agent output CLAUDE.md must be post-processed to extract milestone blocks into files and generate manifest; shell handles extraction (agent output unchanged)
- **`tekhton.sh` startup** — Missing auto-migration trigger; when `MILESTONE_DAG_ENABLED=true`, `MILESTONE_AUTO_MIGRATE=true`, and no manifest exists but inline milestones detected, must call `migrate_inline_milestones()`
- **`lib/orchestrate_helpers.sh` line 15** — `find_next_milestone()` call doesn't use DAG-aware variant; should call `dag_find_next()` when manifest exists
- **Plan generation post-processing** — No existing code extracts milestones from generated CLAUDE.md into individual files; must implement block detection regex, file writing, manifest generation, and CLAUDE.md trimming

## Complexity Estimate

Files to modify: 6
Estimated lines of change: 450
Interconnected systems: medium
Recommended coder turns: 50
Recommended reviewer turns: 10
Recommended tester turns: 30
