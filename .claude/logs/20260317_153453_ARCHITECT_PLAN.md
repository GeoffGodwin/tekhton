# Architect Plan

## Staleness Fixes

- **`lib/turns.sh` missing `set -euo pipefail`**: Add `set -euo pipefail` on the line immediately after the `#!/usr/bin/env bash` shebang (line 1). All other sourced library files in `lib/` carry this directive — `turns.sh` is the only exception. This is a pre-existing omission surfaced by Milestone 9's reviewer. Specific action: insert `set -euo pipefail` at line 2 of `lib/turns.sh`.

## Dead Code Removal

None

## Naming Normalization

None

## Simplification

None

## Design Doc Observations

None

## Drift Observations to Resolve

- [2026-03-17] `lib/turns.sh` missing `set -euo pipefail` — addressed under Staleness Fixes above. Mark as RESOLVED once the line is added and `bash -n lib/turns.sh` passes.

## Out of Scope

None
