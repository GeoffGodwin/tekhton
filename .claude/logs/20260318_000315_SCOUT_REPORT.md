## Relevant Files

- `lib/milestones.sh` — Add `check_milestone_size()` to detect oversized milestones + `split_milestone()` to decompose them; add `record_milestone_attempt()` and `get_milestone_attempts()` for null-run recovery tracking
- `stages/coder.sh` — Add pre-flight sizing gate after scout runs; add null-run auto-split trigger with recursive depth tracking
- `lib/config.sh` — Add config defaults: MILESTONE_SPLIT_ENABLED, MILESTONE_SPLIT_MODEL, MILESTONE_SPLIT_MAX_TURNS, MILESTONE_SPLIT_THRESHOLD_PCT, MILESTONE_AUTO_RETRY, MILESTONE_MAX_SPLIT_DEPTH
- `tekhton.sh` — Add auto-split retry loop that re-invokes pipeline targeting smaller sub-milestone after null-run; prevent infinite recursion via split depth cap
- `lib/turns.sh` — Interaction point: `apply_scout_turn_limits()` already applies scout estimates; pre-flight gate checks if estimate exceeds cap
- `lib/agent.sh` — Interaction point: `was_null_run()` already detects null runs; auto-split path triggered here instead of immediate error exit
- `templates/pipeline.conf.example` — Add milestone split config section with descriptions and defaults
- `prompts/milestone_split.prompt.md` — **New file**: Prompt for the splitting model to decompose oversized milestones into 2-4 sub-milestones

## Key Symbols

- `check_milestone_size(milestone_num, scout_estimate)` — lib/milestones.sh — returns 1 if oversized (exceeds cap by 20%+)
- `split_milestone(milestone_num, claude_md)` — lib/milestones.sh — invokes opus model to produce sub-milestones N.1, N.2, ...
- `record_milestone_attempt(milestone_num, outcome, turns_used)` — lib/milestones.sh — logs attempt for splitter context
- `get_milestone_attempts(milestone_num)` — lib/milestones.sh — retrieves prior attempts
- `apply_scout_turn_limits(report_file)` — turns.sh — sets ADJUSTED_*_TURNS from scout recommendation (pre-flight gate hooks here)
- `was_null_run()` — agent.sh — detects null run; auto-split path triggered instead of exit
- `run_stage_coder()` — stages/coder.sh — adds two hooks: pre-flight sizing + null-run auto-split logic
- `MILESTONE_SPLIT_THRESHOLD_PCT` — lib/config.sh — split when estimate exceeds cap by this % (default: 120)
- `MILESTONE_MAX_SPLIT_DEPTH` — lib/config.sh — prevent infinite recursion (default: 3, N.1.1 max)

## Suspected Root Cause Areas

- `stages/coder.sh` lines 68-69 (apply_scout_turn_limits) — pre-flight sizing gate must run here immediately after scout
- `stages/coder.sh` lines 257-276 (null-run detection) — instead of immediate error exit, trigger auto-split for milestone mode
- `tekhton.sh` main pipeline loop — needs conditional auto-split retry: if null-run + milestone mode + depth < max, split and re-run scout+coder for N.1
- `lib/milestones.sh` — add split tracking state to MILESTONE_STATE.md to persist split depth across runs (resume safety)
- `prompts/milestone_split.prompt.md` — must receive SCOUT_ESTIMATE, TURN_CAP, and prior attempt history so splitter doesn't repeat oversized decomposition

## Complexity Estimate

Files to modify: 8
Estimated lines of change: 900
Interconnected systems: medium
Recommended coder turns: 70
Recommended reviewer turns: 12
Recommended tester turns: 35
