## Relevant Files

- lib/crawler_content.sh — Contains `local -n` nameref at line 82 (Bash 4.3+ requirement needs documentation) and `grep -qP` PCRE issue at line 98 (needs portable alternative)
- tekhton.sh — Comment at line 313 incorrectly describes source chain, should mention crawler_deps.sh explicitly
- lib/metrics.sh — Missing `set -euo pipefail` header, needs to be added alongside metrics_dashboard.sh and metrics_calibration.sh as a family
- lib/metrics_calibration.sh — Missing `set -euo pipefail` header, part of metrics family that was extracted in phases
- lib/metrics_dashboard.sh — Missing `set -euo pipefail` header, needs correction alongside sibling metrics libraries
- lib/detect.sh — `_find_source_files` function at line 126 uses `git ls-files` without depth constraint, returning all tracked files instead of top-2-level files per spec
- tests/test_dynamic_turn_limits.sh — Verified correct; only calls record_run_metrics() and _extract_stage_turns() which remain in metrics.sh (no changes needed)

## Key Symbols

- _add_candidate / line 81 — crawler_content.sh (uses nameref)
- _is_binary_file / line 95 — crawler_content.sh (uses grep -qP)
- _find_source_files / line 123 — lib/detect.sh (git ls-files depth issue)
- _count_source_files / line 144 — lib/detect.sh (calls _find_source_files)
- record_run_metrics / lib/metrics.sh (called by test_dynamic_turn_limits.sh)
- _extract_stage_turns / lib/metrics.sh (called by test_dynamic_turn_limits.sh)

## Suspected Root Cause Areas

- crawler_content.sh line 82: `local -n` requires Bash 4.3+ but CLAUDE.md specifies only "Bash 4+" without minor version; needs documentation in file header
- crawler_content.sh line 98: `grep -qP '\x00'` uses PCRE flag unavailable on macOS BSD grep; fallback exists but portability concern remains
- tekhton.sh line 313: Comment incomplete; should explicitly list all sourced libraries including crawler_deps.sh which is sourced transitively
- lib/metrics.sh/metrics_calibration.sh/metrics_dashboard.sh: All three extracted metric helper libraries lack `set -euo pipefail` declaration; should be fixed as a family unit
- lib/detect.sh line 126: `git ls-files` returns all tracked files regardless of depth while non-git fallback respects `-maxdepth 2`; spec requires "top 2 levels"
- tests/test_dynamic_turn_limits.sh: No code changes needed; test correctly sources metrics.sh and only uses functions that remain there (record_run_metrics, _extract_stage_turns)

## Complexity Estimate

Files to modify: 6
Estimated lines of change: 12
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 6
Recommended tester turns: 10
