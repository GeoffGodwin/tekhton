# Reviewer Report — Milestone 7: Specialist Reviewers (Cycle 2)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
None

## Simple Blockers (jr coder)
None

## Non-Blocking Notes
- `lib/specialists.sh` line 9 header comment still reads "Expects: `_ensure_nonblocking_log()`, `append_specialist_notes()` from drift.sh" — the function `append_specialist_notes()` (no underscore) does not exist in drift.sh; the local function is `_append_specialist_notes`. Fix the comment to read "Expects: `_ensure_nonblocking_log()` from drift.sh; `_append_specialist_notes()` defined locally."
- `AGENT_TOOLS_SPECIALIST` (line 17 of `specialists.sh`) is frozen at source time via `${AGENT_TOOLS_REVIEWER:-...}`. If `AGENT_TOOLS_REVIEWER` is exported after this file is sourced, the fallback is frozen. Works correctly in the current source order but is fragile.
- `sed -i` in `_append_specialist_notes` line 235: if a `[NOTE]` item's text contains a GNU sed escape sequence (e.g., `\n`, `\t`), it may be interpreted in the append text, corrupting the NON_BLOCKING_LOG.md entry. Consider wrapping with `printf '%s\n'` or using a here-doc append instead.
- `stages/review.sh` is 323 lines — exceeds the 300-line guideline. The specialist integration block (lines 239–318) could be extracted into a helper function `_route_specialist_rework()` to bring the file under the limit.

## Coverage Gaps
- `test_specialists.sh` has no test for `run_specialist_reviews()` returning 1 (the blocker path). Test 9 only covers the no-blocker path. Add a test where the specialist's findings file contains a `[BLOCKER]` item and verify the return code is 1 and `SPECIALIST_BLOCKERS` is populated.

## ACP Verdicts
None

## Drift Observations
None
