# Reviewer Report — Milestone 17: Tech Stack Detection Engine
Review cycle: 2 of 4

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
None

## Simple Blockers (jr coder)
None

## Non-Blocking Notes
- `detect_report.sh` line 23 calls `detect_project_type()`, which internally calls `detect_languages()`, `detect_frameworks()`, and `detect_entry_points()` — the same three functions already called at lines 19-22. All four detection functions run twice on every `format_detection_report()` call. Carried forward from cycle 1; still non-blocking, but worth noting for M18/M19 callers where latency matters.
- `detect.sh` line 92: `sort -t'|' -k2,2 -k1,1` sorts confidence alphabetically, producing `high` < `low` < `medium` order. Downstream consumers expecting highest-confidence first will be surprised. Carried forward from cycle 1; non-blocking but should be addressed before M19 consumers rely on this ordering.
- `detect.sh` line 62: `"$(basename "$(compgen -G "$proj_dir"/*.csproj 2>/dev/null | head -1)" 2>/dev/null || echo "*.sln")"` — the `2>/dev/null` suppresses errors from `basename`, not from `compgen`. Errors from `compgen` still propagate if the glob fails unexpectedly. Carried forward from cycle 1 drift observation; minor portability concern only.
- `detect.sh` line 113: `&>/dev/null 2>&1` is redundant — `&>` already redirects both stdout and stderr, making the trailing `2>&1` a no-op. Harmless but noisy; can be simplified to `&>/dev/null`.
- ARCHITECTURE.md Layer 3 still does not list `lib/detect.sh`, `lib/detect_commands.sh`, or `lib/detect_report.sh`. The ACP was ACCEPTED in cycle 1 — this documentation update remains outstanding.

## Coverage Gaps
- No tests for the detection functions. Milestone acceptance criteria list concrete scenarios (TypeScript from tsconfig.json + .ts files, npm test from package.json scripts, cargo test from Cargo.toml, etc.) that should be codified in `tests/test_detect.sh` to prevent regressions as M18/M19 build on this library.

## ACP Verdicts
- ACP: Add lib/detect.sh, lib/detect_commands.sh, and lib/detect_report.sh to ARCHITECTURE.md Layer 3 — **ACCEPT** — Legitimate documentation update. Three new library entries should be added with descriptions matching their header comments. Update is still outstanding.

## Drift Observations
- `detect.sh` `_find_source_files()`: the `git ls-files` path is not depth-bounded (finds files at any depth), while the non-git fallback uses `find -maxdepth 2`. Same inconsistency noted in cycle 1 — not a current blocker but detection results will differ between git and non-git repos with the same structure. Worth aligning when the crawler (M18) is built.
