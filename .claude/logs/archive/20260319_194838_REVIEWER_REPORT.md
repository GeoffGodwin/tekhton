# Reviewer Report — Milestone 15.1.1 (Re-review)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `tekhton.sh:152-153` — `HUMAN_MODE` and `HUMAN_NOTES_TAG` are still not exported at their initialization lines; `export HUMAN_MODE HUMAN_NOTES_TAG` only fires inside the `--human` case branch (line 540). This was noted in the previous cycle and remains unchanged. It is harmless in all current call paths (sourced libs use `${HUMAN_MODE:-false}`) but would break subprocess contexts if `--human` is never passed and a subprocess tries to read `HUMAN_MODE` as an environment variable. Adding `export` to the initialization lines (152-153) would be the clean fix.

## Coverage Gaps
- Arg-parser edge cases for `--human [TAG]` (e.g. `--human BUG` sets tag, `--human --complete` does not consume `--complete` as tag, `--human` as final argument) remain untested at the integration level. The unit-level `should_claim_notes` coverage is solid; the gap is in tekhton.sh argument parsing itself.

## Prior Blocker Verification
- `stages/coder.sh:330` — **FIXED.** The stale log message ("task does not reference them") has been replaced with the accurate "no notes flag set (--human, --with-notes, or --notes-filter) — skipping notes injection." The fix is correct and matches the new flag-only semantics.

## Drift Observations
- None
