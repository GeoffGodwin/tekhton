# Reviewer Report — Milestone 6: Brownfield Replan (Cycle 2)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `lib/plan.sh` remains at ~786 lines (2.5× the 300-line cap). The replan functions added here are architecturally correct but `lib/replan.sh` is already designated as the home for brownfield replan orchestration. Migrating the five new functions there in a future cleanup milestone would restore both files to within budget.
- Missing blank line between the Context Compiler section and Auto-advance section in `templates/pipeline.conf.example` (lines 244–245). All other section boundaries have a blank line separator.
- The append-only DESIGN.md delta strategy will cause DESIGN.md to grow on each replan cycle. Acceptable as an intentional decision, but worth noting in the replan prompt or docs so operators know to periodically consolidate.

## Coverage Gaps
- No test asserts that `--replan` preserves `[DONE]` milestones after regeneration. A regression test feeding a CLAUDE.md with a `[DONE]` milestone through `_apply_brownfield_delta` and asserting the milestone still appears as `[DONE]` in regenerated output would prevent future breakage.
- No test for `_generate_codebase_summary()` covering both the `tree`-available path and the `find`-fallback path with size-bounding behaviour.

## ACP Verdicts
None

## Drift Observations
- lib/plan.sh vs lib/replan.sh: The architecture document designates `lib/replan.sh` for "Brownfield replan orchestration (Milestone 6)" but all new replan functions reside in `lib/plan.sh`. `lib/replan.sh` holds mid-run replan (reviewer verdict path). The two files now serve distinct concerns without a naming convention that signals the distinction to future contributors.
