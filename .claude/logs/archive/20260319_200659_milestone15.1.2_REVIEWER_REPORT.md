# Reviewer Report — Milestone 15.1.1

## Verdict
APPROVED

## Complex Blockers (senior coder)
None

## Simple Blockers (jr coder)
None

## Non-Blocking Notes
- `lib/notes.sh:182-191` — The `_PIPELINE_EXIT_CODE` fallback path in `resolve_human_notes()` adds implicit coupling between `tekhton.sh` (which sets that variable) and `notes.sh` (which consumes it). The inline comment references a specific line number in `tekhton.sh` that will drift over time. Not a blocker — behavior is correct — but the coupling is invisible from the function signature.

## Coverage Gaps
- No test for `should_claim_notes` with `NOTES_FILTER` set (third branch). The two flag paths (WITH_NOTES, HUMAN_MODE) are verified by the existing test suite, but a direct unit test for the NOTES_FILTER branch would complete coverage of all three return-0 paths.
- `--human` with `--milestone` and `--human` with a positional task argument are documented as invalid combinations in the milestone spec but no guard or error message exists yet. Milestone 15.4 is the right place for those guards; noting here for tracking.

## Drift Observations
- `lib/notes.sh:4` — Header comment `"Expects: NOTES_FILTER, LOG_DIR, TIMESTAMP, log()"` no longer lists `HUMAN_MODE` as an expectation even though `should_claim_notes()` reads it. Minor doc drift.
- `stages/coder.sh:388-390` — Null-run path calls `resolve_human_notes` unconditionally even when no notes were claimed (no flag set). It is safe (no `[~]` items to reset) but the call is misleading — `should_claim_notes` guards are used everywhere else in the same file. A `should_claim_notes` guard here would be consistent.
