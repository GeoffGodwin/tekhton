# Reviewer Report — Milestone 2: Sliding Window & Plan Generation Integration

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `lib/milestone_dag_migrate.sh:221` — The pointer comment interpolates `${MILESTONE_DIR:-.claude/milestones}` directly. In the execution pipeline, `MILESTONE_DIR` is resolved to an absolute path by `config.sh`, so the comment reads `<!-- Milestones are managed as individual files in /abs/path/.claude/milestones/.` rather than the relative form. Cosmetic; tests only check for the string prefix and pass regardless.
- `tests/test_milestone_window.sh` — No dedicated test for `_extract_title_line` in isolation; it is exercised only indirectly through the last-resort budget exhaustion path. Low risk given the function is 8 lines, but explicit coverage would help.
- `tests/test_milestone_window.sh` — There is no test that verifies on-deck milestones are excluded when only frontier milestones fill the budget exactly. The budget exhaustion test exercises the title-only active milestone path, not the "frontier fits but on-deck does not" branch.
- `tests/test_milestone_dag_migrate.sh:194` — Comment says "Source plan_generate for the helper" but `_insert_milestone_pointer` is already available from the earlier `source milestone_dag_migrate.sh` at line 38. The sourcing of `plan_generate.sh` here is redundant for this test's purposes; it succeeds but the comment is misleading.

## Coverage Gaps
- No shellcheck run was possible in this review (tool approval denied). Static inspection of `lib/milestone_window.sh` and `lib/milestone_dag_migrate.sh` shows correct use of `[[ ]]`, quoted variables, `$(...)` substitution, and `set -euo pipefail`. No obvious shellcheck issues observed, but CI should confirm.
- The three bonus test files (`test_startup_auto_migrate.sh`, `test_find_next_milestone_dag.sh`, `test_plan_generate_integration.sh`) cover the main acceptance paths well and go beyond what the milestone spec required.

## Drift Observations
- None

---

### Implementation Summary

The coder reported no files were modified — all Milestone 2 components were pre-implemented during Milestone 1 work. After direct file inspection, this is confirmed: all components are present and correct.

Key correctness checks passed during static analysis:

**`lib/milestone_window.sh`** (292 lines — under limit):
- `_compute_milestone_budget()` correctly implements `min(available * PCT/100, MAX_CHARS)` with the arithmetic performed in pure bash integer math. No rounding issues for reasonable token window sizes.
- `_milestone_priority_list()` correctly excludes `done` milestones and separates active/frontier/on-deck using `dag_deps_satisfied()`.
- `build_milestone_window()` correctly checks `_DAG_LOADED` before proceeding, preventing silent failures when manifest was not loaded. The three-tier truncation strategy (full → summary → title-only → omit) aligns with the acceptance criteria.
- Header overhead (`_MILESTONE_WINDOW_HEADER_CHARS=350`) is subtracted before greedy fill, preventing over-budget output.
- Integration with `_add_context_component()` confirmed at line 287.

**`lib/milestone_dag_migrate.sh` — `_insert_milestone_pointer()`** (260 lines — under limit):
- Heading-level tracking correctly identifies when a non-milestone section heading is encountered (`this_level <= milestone_heading_level`) and exits the milestone block without double-writing the line. The `continue` at the end of the `in_milestone` branch comes after the conditional `echo`, so no double-write occurs.
- Idempotency check via `grep -q "Milestones are managed as individual files"` prevents duplicate pointer insertion.
- Atomic write via tmpfile + `mv -f` is consistent with the project's manifest write pattern.

**`stages/plan_generate.sh`** (post-processing block, lines 94–108):
- `parse_milestones` is used to detect milestone presence before calling migration, preventing a useless `migrate_inline_milestones` call on non-milestone output.
- `MILESTONE_DIR` path construction `"${PROJECT_DIR}/${MILESTONE_DIR:-.claude/milestones}"` handles both unset (planning flow bypasses config loading) and relative-default cases correctly. Absolute-path double-prefixing cannot occur in the planning flow since config.sh is not called.

**`tekhton.sh`** (auto-migration block, lines 861–885):
- Guard conditions (`MILESTONE_DAG_ENABLED`, `MILESTONE_AUTO_MIGRATE`, `! has_milestone_manifest`, `parse_milestones`) prevent spurious migration. Idempotency is guaranteed by the manifest-exists early-return in `migrate_inline_milestones()`.
- Manifest loading after migration (lines 882–884) ensures `_DAG_LOADED=true` for subsequent `build_milestone_window()` calls in the coder stage.

**`stages/coder.sh`** (lines 241–258):
- Triple guard (`MILESTONE_DAG_ENABLED`, `declare -f build_milestone_window`, `has_milestone_manifest`) ensures graceful fallback to the static `MILESTONE_BLOCK` when any condition is unmet. The `|| true` on `build_milestone_window` prevents pipeline abort on budget failures.

**Test files**:
- `test_milestone_window.sh`: 20 assertions covering budget cap, percentage calculation, priority ordering, full-budget window, header presence, frontier inclusion, tiny-budget exhaustion, last-resort title-only path, DAG-disabled fallback, and `_extract_first_paragraph_and_acceptance` extraction logic.
- `test_milestone_dag_migrate.sh`: 14 assertions covering complex CLAUDE.md migration, manifest creation, done-status preservation, multi-dependency detection, milestone file content integrity, re-migration idempotency, missing-file graceful failure, pointer insertion, content removal, non-milestone content preservation, and pointer idempotency.
- Three bonus tests cover the startup auto-migration path, `find_next_milestone()` DAG ordering, and `run_plan_generate()` post-processing — collectively exercising all major integration points.
