# Reviewer Report

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `lib/milestone_split.sh` header comment lists dependencies as "milestones.sh, milestone_archival.sh" but the DAG-aware split path calls `_slugify()` which is defined in `milestone_dag_migrate.sh`. Add `milestone_dag_migrate.sh` to the sourced-first list in the header. (No runtime risk — sourcing order in tekhton.sh is correct; documentation gap only.)
- `milestone_dag_validate.sh` relies on bash dynamic scoping to make `declare -A _visited` and `_in_stack` visible inside the nested `_dfs_cycle_check` function. This is correct bash behaviour but non-obvious. A brief comment explaining "declared in validate_manifest, visible via dynamic scoping" would help future maintainers.

## Coverage Gaps
- `archive_completed_milestone()` DAG path (lines 46–65 in milestone_archival.sh) is not exercised by any test. The acceptance criterion explicitly calls this function out. A test should: create a manifest with one `done` milestone, call `archive_completed_milestone`, and verify the entry appears in MILESTONE_ARCHIVE.md.
- `emit_milestone_metadata()` DAG path (milestone_metadata.sh) has no test coverage. Tests should verify: (a) metadata is written into the milestone file, (b) manifest status is updated, (c) an existing `<!-- milestone-meta` block is replaced rather than duplicated.
- `archive_all_completed_milestones()` has no DAG path — it scans CLAUDE.md for `[DONE]` markers which don't exist in DAG mode. In DAG mode the function is a silent no-op. Not in scope for Milestone 1 acceptance criteria but should be addressed in Milestone 2.

## Drift Observations
- `lib/milestone_archival.sh:138-171` — `archive_all_completed_milestones` has no DAG path. It discovers done milestones by grepping CLAUDE.md for `#### [DONE] Milestone` markers that do not exist in file-based DAG mode. The per-milestone function `archive_completed_milestone` correctly handles the DAG path; the bulk variant does not. Any caller relying on bulk archival will silently do nothing in DAG mode.
- `lib/milestones.sh` — File has grown beyond 300 lines. The DAG-aware wrapper variants of `get_milestone_count`, `get_milestone_title`, and `is_milestone_done` added this milestone are the primary contributors. Extraction to `lib/milestone_dag_helpers.sh` would bring the file back under the limit. Track for Milestone 2.
