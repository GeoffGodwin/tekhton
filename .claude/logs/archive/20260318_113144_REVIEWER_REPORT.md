# Reviewer Report — Architect Remediation (2026-03-18)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- **DRIFT_LOG.md not updated per plan requirements.** ARCHITECT_PLAN.md explicitly requires
  marking four drift observations as RESOLVED after the code changes are applied:
  - Observation 1 (`_first_sub` duplication) — resolved by P-1, not marked RESOLVED
  - Observation 2 (`milestone_split.sh`/`milestone_archival.sh` missing directive) — resolved
    by S-1, not marked RESOLVED
  - Observation 3 (NON_BLOCKING_LOG.md duplicate milestones.sh entries) — already resolved,
    ARCHITECT_PLAN.md says to mark RESOLVED with rationale; not done
  - Observation 4 (`lib/milestones.sh` multi-concern growth) — already resolved per plan
    rationale; not marked RESOLVED
  All four unresolved entries remain in the Unresolved Observations section of DRIFT_LOG.md.
  The code work is complete; only the housekeeping was skipped. Add RESOLVED entries for all
  four observations next cycle.

## Coverage Gaps
- None

## Drift Observations
- The DRIFT_LOG.md administrative gap noted above is the only systemic issue. Both coders
  correctly confined changes to the plan's specified scope: no scope creep observed.

---

## Review Notes

**S-1 (set -euo pipefail):** Verified. Both `lib/milestone_split.sh:2` and
`lib/milestone_archival.sh:2` now have `set -euo pipefail` immediately after the shebang.
JR coder reports `bash -n` and `shellcheck -x` pass on both files. ✓

**P-1 (_switch_to_sub_milestone helper):** Verified. The helper is placed at lines 9–29 of
`stages/coder.sh`, before `run_stage_coder()`. All three call sites (lines 106, 342, 466)
have been replaced with single-call invocations. No stale `_first_sub` references remain
outside the function body — the variable is properly `local` within the helper. The coder's
note about fixing "residual `_first_sub` references in post-split logging" is confirmed:
the depth/warn messages after each call site use `_CURRENT_MILESTONE`, not `_first_sub`. ✓

**Test results:** Coder reports 60/60 passing (up from 59/1 — the test that broke on the
stale `_first_sub` variable is now fixed). JR coder reports no test regressions. ✓

**Scope compliance:** No changes outside the plan-specified files. ARCHITECTURE.md was not
modified (no design doc changes were needed). Out-of-scope items (tekhton.sh `%%` strip,
systemic `set -euo pipefail` sweep) were correctly left untouched. ✓
