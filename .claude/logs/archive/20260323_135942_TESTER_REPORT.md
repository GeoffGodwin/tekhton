## Planned Tests
- [x] `tests/test_detect_brownfield_coverage.sh` — Ansible detection (ansible.cfg/playbooks/roles), pnpm/Nx workspace enumeration, _format_ci_section field rendering, doc-quality model selection in _generate_smart_config

## Test Run Results
Passed: 30  Failed: 0

## Bugs Found
- BUG: [lib/detect_workspaces.sh:97] _enum_pnpm_workspaces awk modifies $0 via gsub(/^  - /,"") before the exit condition (/^[^ ]/{exit}) is evaluated; the stripped line no longer starts with a space so the exit fires on the same record — only the first glob pattern in pnpm-workspace.yaml is ever enumerated; packages listed after the first pattern (e.g. apps/*) are silently ignored

## Files Modified
- [x] `tests/test_detect_brownfield_coverage.sh`
