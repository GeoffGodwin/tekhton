# Reviewer Report — Milestone 20: Incremental Rescan & Index Maintenance

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `rescan.sh` is sourced in the main execution pipeline (tekhton.sh line 277) even though no pipeline stage calls any rescan function at runtime. This adds sourcing overhead on every pipeline run. Consider sourcing only in the `--rescan` early-exit block, or document why runtime pipeline access is needed.
- `_generate_codebase_summary` (replan_brownfield.sh lines 20-21) duplicates the metadata extraction logic (`grep '<!-- Scan-Commit:'` + `sed`) rather than calling `_extract_scan_metadata` from rescan_helpers.sh. Acceptable here because `replan_brownfield.sh` sources `replan.sh`, not `rescan.sh`, so `_extract_scan_metadata` is unavailable in the `--replan` path. Minor code smell worth noting.
- No test file added for rescan functions (see Coverage Gaps).

## Coverage Gaps
- No `tests/test_rescan*.sh` added. The acceptance criteria includes "all existing tests pass" but the milestone's own logic (incremental update, significance detection, metadata round-trip, fallback to full crawl) has no test coverage. At minimum: a test that `rescan_project` falls back gracefully when PROJECT_INDEX.md is absent, and a test that `_detect_significant_changes` returns the correct significance strings for the manifest/new-dir/trivial cases.

## ACP Verdicts
- ACP: Rescan sources detect/crawler in early-exit block — ACCEPT — Follows the exact same minimal-source pattern as the existing `--init` and `--replan` early-exit blocks. Backward compatible; no existing behavior changed. ARCHITECTURE.md update note is correct.

## Drift Observations
- lib/rescan.sh:51, lib/rescan.sh:68, lib/rescan_helpers.sh:161 — `&>/dev/null 2>&1` is used (redirecting stdout+stderr with `&>`, then redundantly adding `2>&1`). This same pattern exists in crawler.sh:188 and crawler.sh:233, so it is pre-existing in the codebase. No action needed but worth standardizing on `&>/dev/null` alone when these files are next touched.
- lib/rescan_helpers.sh:_replace_section — The awk `-v body="$new_body"` assignment interprets backslash escape sequences. If a section contains literal backslashes (e.g., Windows paths in sampled files), the replacement body may be corrupted. The existing crawler.sh does not use this replacement pattern, so it is new surface area. Low probability in practice given the codebase context.
