# Coder Summary

## Status: COMPLETE

## What Was Implemented

Milestone 4: Follow-Up Interview Depth + Completeness Checker Upgrade

- Added depth scoring system to `lib/plan_completeness.sh` with `_score_section_depth()` (0–5 score based on line count, sub-headings, tables, code blocks) and `_is_section_shallow()` (threshold < 2)
- Updated `check_design_completeness()` to flag sections as `[SHALLOW]` (has content but lacks structural depth) or `[MISSING]` (empty/placeholder) — sections must now pass both non-empty AND depth checks
- Configurable minimum line count via `PLAN_SECTION_MIN_LINES` (default: 5)
- Rewrote `prompts/plan_interview_followup.prompt.md` to instruct Claude to expand shallow sections with sub-sections, tables, config examples, edge cases, and interaction notes — with explicit rules for `[SHALLOW]` (expand, don't replace) vs `[MISSING]` (create from scratch)
- Updated `stages/plan_followup_interview.sh` to parse `[SHALLOW]`/`[MISSING]` tags from `PLAN_INCOMPLETE_SECTIONS`, display tags in section headers, show up to 15 lines of existing content as context (with "needs more depth" guidance for SHALLOW sections), and pass tags through to the synthesis prompt
- Updated existing tests to use deep content (sub-headings, 5+ lines) for "complete" test cases
- Added 10 new depth-scoring tests covering: single-line (score 0), 5-line no structure (score 1), sub-headings (score 3), tables (score 2), code blocks (score 2), rich content (score 5), shallow detection via `check_design_completeness()`, and `[SHALLOW]` prefix verification

## Files Modified

- `lib/plan_completeness.sh` — Added `_score_section_depth()`, `_is_section_shallow()`, `PLAN_SECTION_MIN_LINES` constant; updated `check_design_completeness()` to flag `[SHALLOW]`/`[MISSING]`; updated success message in `run_plan_completeness_loop()`
- `prompts/plan_interview_followup.prompt.md` — Full rewrite with depth expansion instructions, `[SHALLOW]` vs `[MISSING]` handling, and structural requirements (sub-sections, tables, config examples, edge cases, interaction notes)
- `stages/plan_followup_interview.sh` — Updated to parse `[SHALLOW]`/`[MISSING]` tags, display tags in section headers, show existing content with depth guidance, increased preview from 10 to 15 lines with overflow indicator
- `tests/test_plan_completeness.sh` — Updated complete DESIGN.md to use deep content; added 10 depth-scoring tests
- `tests/test_plan_completeness_loop.sh` — Updated `make_complete_design()` to use deep content with sub-headings
