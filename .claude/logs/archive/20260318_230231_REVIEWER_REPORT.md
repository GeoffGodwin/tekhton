# Reviewer Report — Milestone 13.2.1 (Cycle 2)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `lib/agent_helpers.sh` is 330 lines, exceeding the 300-line ceiling. This is a direct consequence of the cycle-1 fix (extract retry envelope into agent_helpers.sh as directed). The milestone spec acknowledged this growth. The file should be split in a future debt sweep — e.g., a `lib/agent_retry.sh` containing `_run_with_retry`, `_classify_agent_exit`, and `_should_retry_transient` (≈195 lines), leaving the summary/null-run helpers in `agent_helpers.sh` (≈135 lines).

## Coverage Gaps
- None

## Drift Observations
- None

## Verified Blockers From Cycle 1

**Complex Blocker — `lib/agent.sh` over 300 lines:** FIXED. `lib/agent.sh` is now 254 lines. The ~140-line retry envelope was extracted into three helper functions in `lib/agent_helpers.sh`: `_run_with_retry()`, `_classify_agent_exit()`, `_should_retry_transient()`. `run_agent()` is now a thin orchestrator that delegates to `_run_with_retry()` and reads results via `_RWR_*` globals.

**Simple Blocker — `retry/retries` pluralization:** FIXED. Lines 143–150 of `lib/agent.sh` correctly produce `"retry"` when `LAST_AGENT_RETRY_COUNT -eq 1` and `"retries"` otherwise, e.g. "after 1 retry", "after 2 retries".

## Logic Review (informational)

Implementation is correct across all acceptance criteria:
- Retry loop resets `AGENT_ERROR_*` at the top of each iteration before re-classification ✓
- `TRANSIENT_RETRY_ENABLED=false` short-circuits before sleeping — 1.0-compatible path preserved ✓
- Permanent errors (`AGENT_SCOPE`, `PIPELINE`) reach `break` immediately without sleeping ✓
- `UPSTREAM` errors still bypass null-run classification in `run_agent()` ✓
- Exponential backoff: attempt 1 → delay=base×2⁰, attempt 2 → base×2¹, attempt 3 → base×2² — capped at `TRANSIENT_RETRY_MAX_DELAY` ✓
- Subcategory overrides: `api_rate_limit` (60s min with `retry-after` parse), `api_overloaded` (60s min), `oom` (15s fixed) applied after exponential calc ✓
- `trap - INT TERM` called after every `_invoke_and_monitor` invocation inside the loop ✓
- `_reset_monitoring_state()` called before `continue`, ensuring clean FIFO/ring-buffer re-init ✓
- `LAST_AGENT_RETRY_COUNT` set to `_retry_attempt` after increment — correct final count for 13.2.2 downstream consumption ✓
- `report_retry()` called before sleep with correct attempt/max/subcategory/delay arguments ✓
