## Verdict
NEEDS_CLARITY

## Confidence
30

## Reasoning
- "Watchtower dashboard" is not defined anywhere in the project documentation (CLAUDE.md, DESIGN files). A developer cannot implement a fix without knowing what this component is.
- No specific static UI files are named — the developer does not know which files must exist in `.claude/dashboard/`, what generates them, or where they come from.
- "Always present" is ambiguous: does this mean checked into the repo, generated during `tekhton.sh` startup, copied by an install step, or something else?
- The current failure mode is not described — knowing *when* or *how* files go missing is necessary to choose the right fix (e.g., missing from repo vs. stripped by `.gitignore` vs. not generated on first run).
- Without answers to the questions below, two developers would make entirely different assumptions about the mechanism and the files involved.

## Questions
- What is the "Watchtower dashboard"? Is it a Tekhton component, an optional add-on, or part of a target project? Where is it defined in the codebase?
- Which specific static UI files are expected to be present in `.claude/dashboard/`? (e.g., `index.html`, `dashboard.js`, asset bundles — list them)
- How should these files get there? Options: (a) checked into the repo, (b) copied by `tekhton.sh` or an init step, (c) generated at startup, (d) output of a build process.
- What is the current failure mode? When are the files missing — on fresh clone, after `--init`, after a pipeline run, or in some other scenario?
- Is `.claude/dashboard/` already in `.gitignore`, and is that part of the problem?
