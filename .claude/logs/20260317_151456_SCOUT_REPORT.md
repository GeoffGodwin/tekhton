## Relevant Files

- `lib/turns.sh` — Dynamic turn limit estimation. Contains `estimate_post_coder_turns()` function that currently uses a heuristic approach. Must be rewritten to use the formula-based recalibration.
- `stages/review.sh` — Review stage orchestration. At line 10, calls `estimate_post_coder_turns()` without parameters. Must pass `ACTUAL_CODER_TURNS` and log recalibration.
- `stages/coder.sh` — Coder stage implementation. Must export `ACTUAL_CODER_TURNS` after successful coder completion (after line 248).
- `tests/test_dynamic_turn_limits.sh` — Test suite for turn limit logic. Phase 6 tests (lines 293-326) test heuristic behavior. Must update to test formula behavior and add fallback tests.
- `lib/agent.sh` — Agent invocation wrapper. Sets `LAST_AGENT_TURNS` after each agent run (line 160). Needed to understand how to read actual turn counts.

## Key Symbols

- `estimate_post_coder_turns()` — `lib/turns.sh`, lines 134-181. Core function to rewrite.
- `LAST_AGENT_TURNS` — `lib/agent.sh`, line 160. Holds turn count from most recent agent run.
- `ACTUAL_CODER_TURNS` — Not yet defined. Will be exported in `stages/coder.sh` after coder completes.
- `clamp_turns()` — `lib/turns.sh`, lines 21-30. Helper function already exists, will be reused.
- `files_modified` counting logic — `lib/turns.sh`, lines 150-153. Extracts from CODER_SUMMARY.md.
- `diff_lines` counting logic — `lib/turns.sh`, lines 156-161. Extracts from `git diff --stat HEAD`.

## Suspected Root Cause Areas

- `estimate_post_coder_turns()` in `lib/turns.sh` (lines 134-181) — Currently skips recalibration if scout already set values (line 142). This guard must be removed per Milestone 9 spec. Function must be rewritten to use formula instead of heuristic.
- `stages/review.sh` at line 10 — Currently calls `estimate_post_coder_turns()` with no parameters. Must pass `ACTUAL_CODER_TURNS` as a parameter.
- `stages/coder.sh` — Missing export of `ACTUAL_CODER_TURNS`. After successful coder completion (line 248), must capture and export `LAST_AGENT_TURNS`.
- `tests/test_dynamic_turn_limits.sh` Phase 6 (lines 293-326) — Tests assume heuristic behavior and reset `SCOUT_REC_REVIEWER_TURNS=0` to bypass the scout check. Tests must be rewritten for formula behavior.

## Complexity Estimate

Files to modify: 4
Estimated lines of change: 110
Interconnected systems: medium
Recommended coder turns: 45
Recommended reviewer turns: 10
Recommended tester turns: 30
