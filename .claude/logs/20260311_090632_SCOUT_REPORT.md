## Relevant Files

- **lib/drift.sh** — Contains the duplicated AWK continuation-line joining logic in two functions (`append_drift_observations()` at lines 61-93 and `append_nonblocking_notes()` at lines 465-496). This is the core file to modify.
- **DRIFT_LOG.md** — Documents the unresolved observation describing this exact duplication issue. Will need to be updated with RESOLVED status after the fix.
- **stages/architect.sh** — Calls `resolve_drift_observations()` (line 210) to mark observations as RESOLVED. Helps understand the pattern for how to resolve this observation.
- **lib/config.sh** — Defines the NON_BLOCKING_LOG_FILE default (line 74). Relevant for understanding the configuration context.

## Key Symbols

- `append_drift_observations()` — lib/drift.sh:33
- `append_nonblocking_notes()` — lib/drift.sh:439
- `resolve_drift_observations()` — lib/drift.sh:117
- Duplicated AWK block — lib/drift.sh:61-93 (in append_drift_observations) and lib/drift.sh:465-496 (in append_nonblocking_notes)

## Suspected Root Cause Areas

- **lib/drift.sh:61-93 vs 465-496** — The AWK block that processes markdown bullet lists and joins multi-line continuation lines is duplicated verbatim between `append_drift_observations()` and `append_nonblocking_notes()`. The core logic (lines 66-92 in first function, 468-495 in second) handles splitting input, detecting new bullets vs continuation lines, and accumulating them into single-line entries. Extract into a shared helper variable or function to eliminate duplication.

## Complexity Estimate

**Files to modify:** 1
**Estimated lines of change:** ~40
**Interconnected systems:** low
**Recommended coder turns:** 20
**Recommended reviewer turns:** 8
**Recommended tester turns:** 15

### Rationale

This is a straightforward refactoring task affecting only `lib/drift.sh`. The AWK block can be extracted into a shell variable or function at the top of the library, then reused in both `append_drift_observations()` and `append_nonblocking_notes()`. The fix is low-risk (no behavior change, just code deduplication) and requires minimal cross-module coordination. After the deduplication, the observation in DRIFT_LOG.md can be marked RESOLVED using the existing `resolve_drift_observations()` function.
