# Architect Plan

**Date**: 2026-03-18
**Audit scope**: 2 unresolved drift observations

---

## Staleness Fixes

None.

---

## Dead Code Removal

None.

---

## Naming Normalization

None.

---

## Simplification

None.

---

## Design Doc Observations

None.

---

## Drift Observations to Resolve

### Observation 1 — `lib/milestone_split.sh:42` SC2155
**Status: INACCURATE — already correct**

The observation claims `local threshold=$((...))` uses arithmetic expansion inside
a `local` declaration (SC2155 pattern). The current code at lines 42–43 reads:

```bash
local threshold
threshold=$(( turn_cap * threshold_pct / 100 ))
```

The declaration and assignment are already on separate lines. SC2155 does not apply.
The observation was likely written before a prior fix separated them, or based on
a misread of the file. No remediation needed.

**Action**: Mark RESOLVED in drift log with the note that the code is already correct.

---

### Observation 2 — `tekhton.sh:1073` `%%` strip on multi-line string
**Status: OBSERVATION BASED ON INCORRECT PATTERN — no actionable fix**

The drift log describes the pattern as `${COMMIT_MSG%%$' '*}` (strip from first
space). The actual code at line 1087 is:

```bash
echo "  git add -A && git commit -m '${COMMIT_MSG%%$'\n'*}'"
```

The pattern is `%%$'\n'*` — strip from first newline — which correctly extracts
the first line of the commit message. This is standard bash string manipulation
with no known version-sensitive behavior. The milestone prefix (`[MILESTONE N ✓]`)
appears on the first line and is preserved correctly.

The observation's concern about "truncated suggestions" was predicated on the
space-strip pattern it described, which does not exist in the code. The actual
newline-strip pattern is correct.

**Action**: Mark RESOLVED in drift log with the note that the described pattern
was incorrect; actual code is correct.

---

## Out of Scope

Both drift observations have been accounted for above (inaccurate / observation
based on misread pattern). Neither requires a code change. No items are deferred
for scope reasons.
