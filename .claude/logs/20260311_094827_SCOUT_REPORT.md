# Scout Report: Fix NON_BLOCKING_LOG.md Items

## Task
Fix the 4 unchecked items in NON_BLOCKING_LOG.md and remove 3 duplicate entries.

## Relevant Files

- `NON_BLOCKING_LOG.md` — Contains 4 unchecked items and 3 duplicate entries that need cleanup
- `lib/plan.sh` — Contains `load_plan_config()` (line 21), `run_plan()` (line 121), needs inline comments explaining design decisions
- `lib/plan_state.sh` — Contains `write_plan_state()` (line 21) with heredoc that uses variable expansion
- `tests/test_plan_interview_stage.sh` — Contains `run_interview()` test helper (line 25) with confusing nested quote/variable expansion pattern (line 61)

## Key Symbols

- `load_plan_config()` — lib/plan.sh:21 — Sources entire pipeline.conf into planning environment. Needs comment clarifying scope.
- `run_plan()` — lib/plan.sh:121 — Step 5 (run_plan_review) runs unconditionally without `skip_to` guard. Needs comment explaining why.
- `run_interview()` — tests/test_plan_interview_stage.sh:25 — Test helper that exports PLAN_STATE_FILE with nested quoting pattern. Needs comment on line 61 explaining variable expansion order.
- `write_plan_state()` — lib/plan_state.sh:21 — Uses `<< EOF` heredoc with variable expansion. Already correct, but context matters.

## Suspected Root Cause Areas

1. **Missing inline comment in `lib/plan.sh` (load_plan_config)**
   - Line 24: Comment says "Source only planning-related keys" but actually sources ALL keys from pipeline.conf
   - Need to clarify that importing all keys is intentional and harmless (all planning keys have safe defaults)

2. **Missing inline comment in `lib/plan.sh` (run_plan)**
   - Line 169: `run_plan_review` lacks the `skip_to` guard that steps 2-4 have
   - Need to explain that review is the final step and intentionally runs unconditionally

3. **Missing inline comment in test helper `run_interview()`**
   - Line 61 in tests/test_plan_interview_stage.sh: PLAN_STATE_FILE export uses `'${project_dir}/...'` inside double-quoted bash -c
   - Variable gets expanded by outer shell before passing to inner bash, creating confusing nested quoting
   - Need comment explaining "outer shell expands ${project_dir}, inner bash receives literal path"

4. **Duplicate entries in NON_BLOCKING_LOG.md**
   - Lines 13-15 are exact duplicates of lines 10-12 (with line 12 marked [x] correctly)
   - These duplicates need to be removed to clean up the log

## Complexity Estimate

**Files to modify:** 3 (lib/plan.sh, tests/test_plan_interview_stage.sh, NON_BLOCKING_LOG.md)

**Estimated lines of change:** ~10
- lib/plan.sh: +3 comments (1-2 lines each)
- tests/test_plan_interview_stage.sh: +1 comment (1-2 lines)
- NON_BLOCKING_LOG.md: Remove 3 duplicate lines

**Interconnected systems:** low
- Comments only, no functional changes
- NON_BLOCKING_LOG.md cleanup is straightforward removal

**Recommended coder turns:** 15-20
**Recommended reviewer turns:** 5-8
**Recommended tester turns:** 10-15
