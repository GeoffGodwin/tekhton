# Scout Report: Milestone 6 — Brownfield Replan

## Relevant Files

- `tekhton.sh` — Entry point; needs early `--replan` flag handler before config loading (pattern: similar to `--plan` at line 229)
- `lib/plan.sh` — Planning phase orchestration; needs `run_replan()` function and `_generate_codebase_summary()` helper to generate directory tree and git log
- `lib/config.sh` — Config loader; needs defaults for `REPLAN_MODEL` and `REPLAN_MAX_TURNS` (lines 165-180 show where drift/planning defaults go)
- `lib/replan.sh` — Already exists with mid-run `_run_replan()` function (lines 103-237); will be sourced by the new `run_replan()` orchestrator
- `prompts/replan.prompt.md` — NEW FILE; needs to be created with variables: `{{DESIGN_CONTENT}}`, `{{CLAUDE_CONTENT}}`, `{{DRIFT_LOG_CONTENT}}`, `{{ARCHITECTURE_LOG_CONTENT}}`, `{{HUMAN_ACTION_CONTENT}}`, `{{CODEBASE_SUMMARY}}`
- `templates/pipeline.conf.example` — Config template; needs new keys: `REPLAN_MODEL`, `REPLAN_MAX_TURNS` (similar to lines 26-43 for model selection)
- `lib/prompts.sh` — Already provides `render_prompt()` and `_safe_read_file()` helpers (used by replan prompt rendering and file reading)
- `lib/common.sh` — Already provides `log()`, `warn()`, `error()`, `success()`, `header()` for output formatting

## Key Symbols

- `_call_planning_batch(model, max_turns, prompt, log_file)` — lib/plan.sh line 165; invokes claude in batch mode, returns output to stdout
- `render_prompt(template_name)` — lib/prompts.sh; substitutes `{{VAR}}` placeholders and `{{IF:VAR}}...{{ENDIF:VAR}}` conditionals
- `_safe_read_file(file_path, label)` — lib/prompts.sh line 50; reads file with size validation, used in lib/replan.sh for loading DESIGN.md and CLAUDE.md
- `load_plan_config()` — lib/plan.sh line 21; loads planning-specific keys from pipeline.conf
- `DESIGN_FILE`, `DRIFT_LOG_FILE`, `ARCHITECTURE_LOG_FILE`, `HUMAN_ACTION_FILE` — lib/config.sh lines 165-167; file paths for replan context
- `run_plan()` — lib/plan.sh line 281; orchestrates --plan flow; serves as template for `run_replan()` structure

## Suspected Root Cause Areas

- **tekhton.sh entry point** — Need to add `--replan` handler BEFORE config loading (line 227-245 shows the `--plan` pattern); must source required libraries (plan.sh, common.sh, prompts.sh, agent.sh, replan.sh) and call `run_replan()`
- **lib/plan.sh** — Missing `run_replan()` orchestrator function; needs to: (1) validate DESIGN.md and CLAUDE.md exist, (2) generate codebase summary via `_generate_codebase_summary()`, (3) load drift/architecture logs, (4) export variables for prompt template, (5) call `_call_planning_batch()`, (6) write DESIGN_DELTA.md, (7) present approval menu
- **lib/plan.sh** — Missing `_generate_codebase_summary()` helper; needs to run `tree` (or `find`) for directory structure and `git log --oneline -20` for recent commits, cap output at ~200 lines
- **prompts/replan.prompt.md** — Missing template file; must be created with instructions to: (1) identify sections contradicting current code, (2) propose updated milestones, (3) preserve completed history, (4) flag decisions needing review; use template variables from lib/plan.sh
- **lib/config.sh** — Missing config defaults for `REPLAN_MODEL` and `REPLAN_MAX_TURNS`; should default to `PLAN_GENERATION_MODEL` and `PLAN_GENERATION_MAX_TURNS` (similar to lines 152-153 in lib/replan.sh)
- **templates/pipeline.conf.example** — Missing replan config section; user-facing documentation for `REPLAN_MODEL` and `REPLAN_MAX_TURNS`

## Complexity Estimate

Files to modify: 5
Estimated lines of change: 180
Interconnected systems: medium
Recommended coder turns: 65
Recommended reviewer turns: 12
Recommended tester turns: 35
