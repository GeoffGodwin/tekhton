# Scout Report: Correct Next 2 Items in NON_BLOCKING_LOG.md

## Relevant Files

- lib/errors.sh — Primary file containing error classification, taxonomy, and helper functions. Needs two corrections: (1) missing `set -euo pipefail` at top of file (post-shebang), and (2) overlapping null-run detection logic consolidation at lines 227-241
- tests/test_state_error_classification.sh — Tests error classification integration in state.sh; validates redaction and state persistence but does not directly test classify_error() logic
- lib/agent.sh — Calls classify_error() from lib/errors.sh at line ~385; guards with command -v check; extracts and uses error category/subcategory/transient flags
- CLAUDE.md — Project rules define "All scripts use `set -euo pipefail`" as Non-Negotiable Rule 2
- NON_BLOCKING_LOG.md — Source document containing the two items to be corrected (lines 8 and 15 in Open section)

## Key Symbols

- `classify_error()` — lib/errors.sh, main function that classifies exit codes and error output into taxonomy categories
- `_match_pattern()` — lib/errors.sh:50-56, helper for case-insensitive regex matching
- `null_run` — Error subcategory when agent completes with turns ≤ 2 and no file changes
- `is_transient()` — lib/errors.sh, returns 0 (true) for transient errors (can be retried), 1 for permanent
- `suggest_recovery()` — lib/errors.sh, returns human-readable recovery suggestion for each error category/subcategory

## Suspected Root Cause Areas

- **Missing safety preamble**: lib/errors.sh lacks `set -euo pipefail` required by project rule 2, inconsistent with other lib/*.sh files and causes static analysis failures
- **Overlapping null-run logic**: Lines 232-235 and 238-241 both classify null_run but with redundant conditions (turns ≤ 2 + error + no changes vs turns = 0 + no changes). Can be consolidated into single if statement to reduce redundancy and improve readability
- **No functional impact**: Both issues are code quality only — the current logic is correct, but the overlapping conditions make maintenance harder and violate DRY principle

## Complexity Estimate

Files to modify: 1
Estimated lines of change: 10
Interconnected systems: low
Recommended coder turns: 12
Recommended reviewer turns: 6
Recommended tester turns: 10
