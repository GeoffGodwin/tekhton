## Relevant Files

- `lib/plan.sh` — Missing `set -euo pipefail` header; 118 lines (refactoring to extract completeness check to separate file already completed)
- `lib/plan_completeness.sh` — Missing `set -euo pipefail` header; extracted completeness logic to keep plan.sh under 300 lines (already accomplished)
- `stages/plan_interview.sh` — Missing `set -euo pipefail` header; 205 lines; needs CWD coupling comment before Milestone 6 state persistence work
- `stages/plan_generate.sh` — Missing `set -euo pipefail` header; 92 lines
- `ARCHITECTURE.md` — Already updated with agent.sh sourcing; no changes needed

## Key Symbols

- `set -euo pipefail` — Required header per CLAUDE.md rule #2 (missing in 4 files)
- `run_plan_interview()` — stages/plan_interview.sh:26; needs inline comment about implicit CWD usage
- `check_design_completeness()` — lib/plan_completeness.sh:85; validates DESIGN.md sections
- `select_project_type()` — lib/plan.sh:46; already has /dev/tty fallback (no issue)
- `run_plan_completeness_loop()` — lib/plan_completeness.sh:139; inner choice loop already eliminates redundant header (no issue)

## Suspected Root Cause Areas

1. **Missing `set -euo pipefail` headers** — 4 files (lib/plan.sh, lib/plan_completeness.sh, stages/plan_interview.sh, stages/plan_generate.sh) lack the required safety header. These files inherit `-euo pipefail` from sourcing script at runtime, but CLAUDE.md rule #2 requires explicit declaration in every shell file.

2. **CWD implicit coupling in run_plan_interview()** — The interview agent writes `DESIGN.md` to `${PROJECT_DIR}/DESIGN.md` without documenting the assumption about current working directory. Before Milestone 6 adds state persistence with potential `cd` operations, should document this coupling.

## Status of NON_BLOCKING_LOG Items

- ✅ **Refactoring split** — Completeness check extracted to lib/plan_completeness.sh; plan.sh at 118 lines
- ✅ **ARCHITECTURE.md update** — Already documents agent.sh in --plan sourcing (line 10)
- ❌ **set -euo pipefail** — 4 files still need headers added
- ✅ **Redundant check loop** — Current implementation of run_plan_completeness_loop has proper inner choice loop (no issue)
- ✅ **test_plan_completeness.sh TMPDIR** — Already renamed to TEST_TMPDIR (avoids shadowing)
- ✅ **wc -l portability** — Already uses `tr -d ' '` for whitespace stripping
- ✅ **Permissions warning** — Already implemented at stages/plan_interview.sh:48
- ❌ **CWD coupling comment** — Not yet added to run_plan_interview()
- ✅ **Redundant output in run_plan()** — Already removed; select_project_type() is single source of truth
- ✅ **/dev/tty fallback** — Already implemented in select_project_type() with TEKHTON_TEST_MODE support

## Complexity Estimate

**Files to modify:** 4
**Estimated lines of change:** ~8-12 (add 1-line header + 1-2 line comment per file)
**Interconnected systems:** low
**Recommended coder turns:** 20
**Recommended reviewer turns:** 5
**Recommended tester turns:** 15

### Changes Required

1. Add `set -euo pipefail` as line 2 in each of:
   - lib/plan.sh (after shebang)
   - lib/plan_completeness.sh (after shebang)
   - stages/plan_interview.sh (after shebang)
   - stages/plan_generate.sh (after shebang)

2. Add inline comment to `run_plan_interview()` in stages/plan_interview.sh documenting CWD assumption, e.g.:
   ```bash
   # Note: This writes DESIGN.md to PROJECT_DIR (implicitly uses CWD).
   # Before adding state persistence (Milestone 6), verify no cd operations occur.
   ```

