# Scout Report: NON_BLOCKING_LOG.md Items 1–2

## Task
Implement the next 2 unchecked items from NON_BLOCKING_LOG.md:
1. `lib/specialists.sh` line 9 header comment issue (function name and source mismatch)
2. `AGENT_TOOLS_SPECIALIST` frozen variable issue at line 17 (fragile config dependency)

## Relevant Files

### Direct Changes
- `lib/specialists.sh` — Lines 9 and 17 need fixes

### Referenced Files (no changes needed, context only)
- `lib/drift.sh` — Defines `_ensure_nonblocking_log()`; clarify that `_append_specialist_notes()` is NOT from here
- `lib/agent.sh` — Defines `AGENT_TOOLS_REVIEWER` (line 25); `AGENT_TOOLS_SPECIALIST` should reference this
- `tekhton.sh` — Sourcing order: line 273 sources `lib/agent.sh`, line 285 sources `lib/specialists.sh`

## Key Symbols

- `_ensure_nonblocking_log()` — `lib/drift.sh` (called at `lib/specialists.sh:218`)
- `_append_specialist_notes()` — `lib/specialists.sh` line 201 (local function, NOT from drift.sh)
- `AGENT_TOOLS_REVIEWER` — `lib/agent.sh` line 25 (export statement)
- `AGENT_TOOLS_SPECIALIST` — `lib/specialists.sh` line 17 (export statement)
- `run_specialist_reviews()` — `lib/specialists.sh` line 22 (main entry point calling `_append_specialist_notes`)

## Suspected Root Cause Areas

1. **Header comment at line 9**: References `append_specialist_notes()` (no underscore) but the actual function is `_append_specialist_notes()` (with underscore, defined locally). Also claims it comes from `drift.sh` but it doesn't.
   - Current: `# Expects: _ensure_nonblocking_log(), append_specialist_notes() from drift.sh`
   - Should be: `# Expects: _ensure_nonblocking_log() from drift.sh; _append_specialist_notes() defined locally.`

2. **Variable assignment at line 17**: Uses `${AGENT_TOOLS_REVIEWER:-...}` which is evaluated at source time. If `AGENT_TOOLS_REVIEWER` is exported after `lib/specialists.sh` is sourced, the fallback value is frozen. In the current sourcing order this works (agent.sh before specialists.sh), but it's fragile and would break if the order changed.
   - Current: `export AGENT_TOOLS_SPECIALIST="${AGENT_TOOLS_REVIEWER:-Read Glob Grep Write}"`
   - Issue: If `AGENT_TOOLS_REVIEWER` is not yet exported when this line runs, `AGENT_TOOLS_SPECIALIST` gets the fallback. If it IS exported, it copies the value. Either way, once set, it's frozen.

## Complexity Estimate

Files to modify: 1
Estimated lines of change: 3
Interconnected systems: low
Recommended coder turns: 15
Recommended reviewer turns: 5
Recommended tester turns: 10

## Rationale

- **Files to modify**: Only `lib/specialists.sh` requires changes
- **Lines of change**: 2–3 lines (one comment fix, one variable assignment fix)
- **Interconnected systems**: Low — changes are isolated to one file and affect only variable initialization and documentation
- **Turn estimates**: Simple fixes (comments and a single variable assignment pattern change). Low complexity, no algorithmic changes, no test rewrites needed beyond a visual inspection.
