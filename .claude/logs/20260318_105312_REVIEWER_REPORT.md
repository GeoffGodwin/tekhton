# Reviewer Report

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `lib/milestone_archival.sh:250` — In `_replace_milestone_block`, the `local awk_rc=$?`, `mv -f`, and `return "$awk_rc"` lines are unreachable on failure due to `set -e`: when awk exits 1 (heading not found), `set -e` fires before `local awk_rc=$?` is reached, bypassing `mv` and `rm -f "$rep_file"`. The behavior is correct (caller sees non-zero exit, `claude_md` stays intact), but the explicit failure-path code (`local awk_rc=$?` check + `return`) is dead code for that case. Consider removing the `local awk_rc` capture and just relying on `set -e` propagation, or suppress `set -e` around awk to make the explicit path live.
- `lib/milestone_archival.sh:252` — `$rep_file` is not cleaned up when awk exits 1 (set -e fires before `rm -f "$rep_file"`). Harmless since session temp dir cleanup handles it, but a `trap` or explicit cleanup before awk would be cleaner.

## Coverage Gaps
- None

## Drift Observations
- `stages/coder.sh` lines 190–196 (`split_milestone` call) and lines 327–350 (null-run auto-split): both paths compute `_first_sub="${_CURRENT_MILESTONE}.1"` independently after the split, then call `get_milestone_title`. This pattern is repeated in two auto-split paths (pre-flight at line 82–127 and null-run at 326–351 and turn-limit at 462–479). Minor duplication — not worth abstracting now but worth noting if a third call site appears.
