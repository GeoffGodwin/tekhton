# Scout Report: Next 2 NON_BLOCKING_LOG Items

## Relevant Files
- lib/common.sh — contains `_print_box_frame()` and `_setup_box_chars()` functions with hardcoded width
- lib/common.sh — callers: `report_error()` (line 123) and `report_retry()` (line 141) invoke `_print_box_frame()` without width control
- lib/agent_monitor_helpers.sh — contains `_reset_monitoring_state()` with PID cleanup logic
- lib/agent_retry.sh — calls `_reset_monitoring_state()` at line 196
- tests/test_print_box_frame.sh — existing test coverage for box rendering
- tests/test_run_with_retry_loop.sh — covers retry envelope but may not cover reset state path

## Key Symbols
- `_print_box_frame()` — lib/common.sh:87-99 (renders boxed message, calls `_setup_box_chars 60` with hardcoded width)
- `_setup_box_chars()` — lib/common.sh:72-81 (sets box character globals, accepts width parameter but always called with 60)
- `report_error()` — lib/common.sh:107-124 (calls `_print_box_frame()`)
- `report_retry()` — lib/common.sh:132-144 (calls `_print_box_frame()`)
- `_reset_monitoring_state()` — lib/agent_monitor_helpers.sh:18-45 (kills agent PID, clears variables, removes temp files)
- `_run_with_retry()` — lib/agent_retry.sh:16+ (calls `_reset_monitoring_state()` after transient error)

## Suspected Root Cause Areas

**Item 1: Variable-width box parameter**
- `_print_box_frame()` at lib/common.sh:88 hardcodes width to 60 via `_setup_box_chars 60`
- `_setup_box_chars()` accepts a width parameter but the caller always provides 60
- Callers (`report_error` and `report_retry`) cannot request different widths
- Refactor point: add width parameter to `_print_box_frame()` with default of 60, pass through to `_setup_box_chars()`

**Item 2: PID variable cleanup in retry loop**
- NON_BLOCKING_LOG.md (line 10) notes that `_reset_monitoring_state()` kills `_TEKHTON_AGENT_PID` but doesn't clear the variable
- Code inspection shows lib/agent_monitor_helpers.sh:26 DOES clear the variable (`_TEKHTON_AGENT_PID=""`)
- Possible scenarios:
  1. Note is outdated — fix was already applied when function was extracted to agent_monitor_helpers.sh
  2. Code I'm viewing is post-fix state — note describes a historical bug that's already resolved
  3. Note describes the intent but the clearing implementation was verified as correct
- Call site: lib/agent_retry.sh:196 invokes `_reset_monitoring_state()` once per retry attempt
- Current behavior: PID is cleared, FIFO cleaned, and API error flags reset — all correct

## Complexity Estimate
Files to modify: 2
Estimated lines of change: 25
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 8
Recommended tester turns: 15

## Notes
- **Item 1 (high confidence):** Straightforward refactor. Add optional width parameter to `_print_box_frame()`, default to 60, pass to `_setup_box_chars()`. No behavioral change for current callers. Test coverage exists; add test for non-default widths.
- **Item 2 (low confidence):** Code already contains the fix (PID clearing at line 26 of agent_monitor_helpers.sh). The NON_BLOCKING_LOG note appears to describe a bug that's already resolved. Recommend verifying the note is outdated and can be moved to Resolved section, or identify a hidden bug if PID leakage still occurs in specific retry scenarios.
