## Task Summary
Implement the next 2 items from NON_BLOCKING_LOG.md:
1. Migrate 5 brownfield replan functions from lib/plan.sh to lib/replan.sh to reduce lib/plan.sh from 786 lines to within 300-line cap
2. Add missing blank line separator in templates/pipeline.conf.example (lines 244–245)

---

## Item 1: Migrate Brownfield Replan Functions to lib/replan.sh

### Relevant Files
- `lib/plan.sh` — contains 786 lines, 9.4× over the 300-line cap. Currently houses brownfield replan functions `run_replan()` and four helpers alongside the interview/completeness/generation orchestration. Functions to migrate: `run_replan()` (line 328), `_generate_codebase_summary()` (line 287), `_replan_approval_menu()` (line 462), `_apply_brownfield_delta()` (line 512), `_archive_replan_delta()` (line 600).
- `lib/replan.sh` — designated architectural home for replan logic. Currently 267 lines with mid-run replan functions (`detect_replan_required()`, `trigger_replan()`, `_run_replan()`, `_apply_replan_delta()`). Will add brownfield replan functions to it.
- `tekhton.sh` — already sources both lib/plan.sh (line 254) and lib/replan.sh (line 283), so migration requires no source order changes.
- `lib/prompts.sh` — provides `render_prompt()`, called by brownfield replan for replan prompt rendering.
- `prompts/replan.prompt.md` — the replan system prompt template, rendered by `run_replan()`.

### Key Symbols
- `run_replan()` — entry point for `--replan` command (Milestone 6); invoked from tekhton.sh line 243
- `_generate_codebase_summary()` — builds codebase context (git log + directory tree) for replan prompt injection
- `_replan_approval_menu()` — displays delta, prompts user: `[a] Apply  [e] Edit  [n] Reject`
- `_apply_brownfield_delta()` — merges delta into DESIGN.md/CLAUDE.md
- `_archive_replan_delta()` — saves REPLAN_DELTA.md to logs/ for audit trail
- `_call_planning_batch()` — shared utility (used by both replan and plan_generate stages); stays in lib/plan.sh
- `_safe_read_file()` — helper for reading files into variables; location TBD (may be in common.sh or plan.sh)

### Suspected Root Cause Areas
- `lib/plan.sh` line 786 — file size regression from Milestone 6 brownfield replan addition. Functions were added to support `--replan` command but architectural design specified lib/replan.sh as home.
- `tekhton.sh` line 243 — `--replan` early-exit case already sources lib/replan.sh. Once functions are in lib/replan.sh, the call to `run_replan()` will resolve correctly.
- Function interdependencies: `run_replan()` → calls `_generate_codebase_summary()`, `_call_planning_batch()`, `_replan_approval_menu()`; `_replan_approval_menu()` → calls `_apply_brownfield_delta()`, `_archive_replan_delta()`.

### Complexity Estimate
Files to modify: 2
Estimated lines of change: 280
Interconnected systems: medium
Recommended coder turns: 35
Recommended reviewer turns: 8
Recommended tester turns: 20

**Rationale:** Requires extracting 5 functions + their helpers from one file to another, updating any cross-dependencies (checking if `_call_planning_batch()` and `_safe_read_file()` are called from both old and new homes), verifying tekhton.sh's sourcing still resolves all symbols, and running the full test suite to ensure no regressions. The functions are self-contained with clear boundaries, reducing risk.

---

## Item 2: Add Missing Blank Line in templates/pipeline.conf.example

### Relevant Files
- `templates/pipeline.conf.example` — configuration template file. Lines 244–245 lack the blank-line separator that all other section boundaries have. Line 244 ends `CONTEXT_COMPILER_ENABLED=false` (setting for context compiler section), and line 245 immediately starts `# --- Auto-advance (--auto-advance) ---` (section header for next section). All other section boundaries have a blank line between them (verified at lines 237, 248, 257 pattern).

### Key Symbols
- `CONTEXT_COMPILER_ENABLED` — configuration key for context compiler feature (Milestone 2)
- `# --- Auto-advance (--auto-advance) ---` — section header for auto-advance configuration (Milestone 3)
- File structure: section header → comment lines → configuration key=value → blank line separator → next section header

### Suspected Root Cause Areas
- `templates/pipeline.conf.example` lines 244–245 — formatting inconsistency introduced during Milestone 6 development (when Auto-advance section was added). The blank line separator was omitted between the CONTEXT_COMPILER_ENABLED setting and the Auto-advance section header.

### Complexity Estimate
Files to modify: 1
Estimated lines of change: 1
Interconnected systems: low
Recommended coder turns: 5
Recommended reviewer turns: 3
Recommended tester turns: 3

**Rationale:** Trivial formatting fix. Add one blank line between line 244 and 245. No logic changes, no dependencies, no testing required beyond visual inspection.
