## Relevant Files

- `lib/detect.sh` — Contains tech stack detection logic with minor error-handling and comment clarity issues at lines 62, 113 (potentially), and 266
- `lib/detect_commands.sh` — Command inference that depends on detect.sh; header already documents this dependency
- `lib/detect_report.sh` — Detection report formatting that composes output from detect.sh and detect_commands.sh
- `lib/common.sh` — Contains `report_orchestration_status()` function at line 212; uses `MAX_AUTONOMOUS_AGENT_CALLS` as a global instead of receiving it as parameter (line 225)
- `lib/finalize_summary.sh` — JSON output generation with incomplete escaping at line 95 (backslashes not escaped before quotes)
- `ARCHITECTURE.md` — Layer 3 documentation; already lists the detect files (lines 96-98), so item 4 from NON_BLOCKING_LOG is already resolved

## Key Symbols

- `detect_languages()` — lib/detect.sh:21
- `detect_frameworks()` — lib/detect.sh (manifest-based detection)
- `detect_commands()` — lib/detect_commands.sh:16
- `detect_entry_points()` — lib/detect_commands.sh
- `detect_project_type()` — lib/detect_commands.sh
- `format_detection_report()` — lib/detect_report.sh:15
- `report_orchestration_status()` — lib/common.sh:212
- `emit_run_summary_json()` — lib/finalize_summary.sh:88 (writes JSON with escaping)

## Suspected Root Cause Areas

1. **lib/detect.sh:62-71** — C#/.NET detection uses `2>/dev/null` on `basename` call, but the note indicates that `compgen` errors still propagate; minor portability concern.

2. **lib/detect.sh:266** — Comment "(which depends on detect.sh being sourced first)" is ambiguous about whether the dependency is on sourcing order or whether detect_commands.sh must be sourced after detect.sh; should be reworded for clarity.

3. **lib/common.sh:212-225** — `report_orchestration_status()` reads `MAX_AUTONOMOUS_AGENT_CALLS` as a global variable with `${MAX_AUTONOMOUS_AGENT_CALLS:-20}` fallback (line 225) instead of receiving it as the 5th parameter; parameter is documented in signature but not implemented.

4. **lib/finalize_summary.sh:95** — JSON escaping for milestone ID only escapes double quotes with `sed 's/"/\\"/g'` but does not escape backslashes first; should use `sed 's/\\/\\\\/g; s/"/\\"/g'` pattern (already correctly used at line 48 for file paths).

5. **ARCHITECTURE.md** — Item 4 from NON_BLOCKING_LOG is already resolved; lib/detect.sh, lib/detect_commands.sh, and lib/detect_report.sh are documented in Layer 3 at lines 96-98.

## Complexity Estimate

Files to modify: 3
Estimated lines of change: 15
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 5
Recommended tester turns: 10
