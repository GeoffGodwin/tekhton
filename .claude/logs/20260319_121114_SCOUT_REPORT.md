# Scout Report: Next 4 Non-Blocking Items

## Relevant Files

- **ARCHITECTURE.md** — lines 80-92 document lib/agent.sh dependencies; current lib/agent.sh description appears correct but needs verification against actual sourced files
- **lib/metrics.sh** — 481 lines, exceeds 300-line ceiling; contains summarize_metrics() (lines 194-320) with nested _pct() function and calibrate_turn_estimate() (lines 397-480) that should be extracted
- **lib/metrics_calibration.sh** — new file to be created; will house calibrate_turn_estimate() and related turn-estimation helper functions
- **lib/agent_helpers.sh** — 277 lines; contains build_continuation_context(), _append_agent_summary(), and other agent-run summary utilities; may need expansion to track agent identity
- **lib/common.sh** — contains log(), warn(), success() functions and box-drawing helpers; will need agent-activity reporting function (e.g., report_agent_generating())
- **lib/agent.sh** — ~250 lines; wraps claude CLI invocation; will need to inject agent-identity awareness and call agent-activity reporting function before/during agent run
- **stages/coder.sh** — calls run_agent() at multiple points; needs to display "Coder is generating..." message while agent is running
- **stages/review.sh** — calls run_agent() for reviewer; needs to display "Reviewer is generating..." message while agent is running
- **stages/tester.sh** — calls run_agent() for tester; needs to display "Tester is generating..." message while agent is running
- **lib/agent_monitor.sh** — 286 lines; handles FIFO-based activity detection and timeout management; may need to pass agent-identity to monitoring functions or report generating status
- **templates/pipeline.conf.example** — will need new config keys for usage threshold feature (e.g., USAGE_THRESHOLD_PCT, USAGE_CHECK_ENABLED)
- **lib/config_defaults.sh** — will need to add default values for usage threshold config keys

## Key Symbols

- `summarize_metrics()` — lib/metrics.sh line 194; nested _pct() function at line 236 pollutes global namespace
- `calibrate_turn_estimate()` — lib/metrics.sh line 397; should be extracted to new lib/metrics_calibration.sh
- `run_agent()` — lib/agent.sh; needs to accept agent identity parameter and call agent-activity reporting before turn counting
- `report_agent_generating()` — new function to add to lib/common.sh; displays "Agent X is generating..." with animated dots
- `_invoke_and_monitor()` — lib/agent_monitor.sh line 16; core invocation wrapper that runs claude CLI
- `_append_agent_summary()` — lib/agent_helpers.sh; appends structured run-summary block to log file; tracks turn count and file changes

## Suspected Root Cause Areas

1. **ARCHITECTURE.md documentation gap (Item #1)**: Line 85 description of lib/agent.sh appears correct in current version, but NON_BLOCKING_LOG dates to 2026-03-19 — verify if the description is actually stale or already fixed.

2. **metrics.sh line-count ceiling (Item #2)**: File at 481 lines vs. 300-line guideline. The file contains two extractable units: (a) calibrate_turn_estimate() + turn-history helpers (~100 lines starting line 397), and (b) nested _pct() function (lines 236-243) used only within summarize_metrics(). Extracting (a) to lib/metrics_calibration.sh would reduce metrics.sh to ~380 lines; extracting (b) by inlining or hoisting would save ~10 lines and eliminate namespace pollution.

3. **_pct() nested function (Item #3)**: Defined inside summarize_metrics() at line 236; after first invocation, _pct becomes a global function due to lack of function scoping in bash. It is only called 3 times within summarize_metrics() (lines 249, 252, 255). Solution: inline the arithmetic (simple `$(( success * 100 / total ))` or `printf` expression) or hoist _pct() to file-level helper before summarize_metrics().

4. **CODER_SUMMARY.md accuracy (Item #4)**: Agent produces CODER_SUMMARY.md via _append_agent_summary() in lib/agent_helpers.sh. The summary accurately records file changes detected AFTER the agent completes, not BEFORE. Issue likely stems from: (a) _detect_file_changes() logic misclassifying changes, (b) git diff not capturing uncommitted work in CODER_SUMMARY.md generation, or (c) timing issue where changes are detected post-run but not accurately counted in the summary. The fix is in lib/agent_helpers.sh _append_agent_summary() (~lines 70-150) which must correctly count and report modified files.

5. **Usage threshold feature (Human note)**: Requires new config keys (USAGE_THRESHOLD_PCT, USAGE_CHECK_ENABLED) and checking logic before pipeline invocation. Will need claude CLI call to `claude /usage` to fetch current session usage, compare against threshold, and abort if exceeded. Implementation point: early in tekhton.sh before any agent invocation, after config load.

6. **Agent-activity indicator feature (Human note)**: Requires: (a) new report_agent_generating() function in lib/common.sh that prints "Coder is generating..." with animated dots, (b) modification to run_agent() to pass agent name, (c) call to report_agent_generating() in stages (coder.sh, review.sh, tester.sh) right before run_agent() invocation. The dots pattern already used in retry reporting (lib/common.sh line 125 report_retry) provides the template.

## Complexity Estimate

Files to modify: 12
Estimated lines of change: 380
Interconnected systems: medium
Recommended coder turns: 35
Recommended reviewer turns: 8
Recommended tester turns: 25
