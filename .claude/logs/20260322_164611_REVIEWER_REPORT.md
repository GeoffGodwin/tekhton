# Reviewer Report
Review cycle: 2 of 4
Task: Implement Indexer Infrastructure & Setup Command

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `tools/setup_indexer.sh:204` — .gitignore advisory still hardcodes `.claude/indexer-venv` instead of using `$VENV_DIR_NAME`. Now that the venv path is configurable via `$2`, this check will give misleading advice when a custom path is used. Low impact (advisory only), but should track `$VENV_DIR_NAME` for consistency.
- `lib/indexer.sh:128` — `export REPO_MAP_CONTENT` inside `run_repo_map()` is still present; `REPO_MAP_CONTENT` is already declared `export` at module scope (line 24). Redundant — can be removed.
- `lib/indexer.sh:167` — `get_repo_map_slice()` still uses substring match (`"$path" == *"$f"*`) which produces false positives for similar-named files. Still a stub — tighten the match when wired into Milestone 5.

## Coverage Gaps
- No tests for `lib/indexer.sh`. `validate_indexer_config()` and `detect_repo_languages()` contain non-trivial logic testable without Python. Deferred from previous cycle — still worth adding in a follow-on pass.

## ACP Verdicts
- ACP: REPO_MAP_VENV_DIR as configurable path — ACCEPT — Reasonable extension; custom venv location is a valid operational need.
- ACP: Per-grammar installation (not bundle) — ACCEPT — Graceful degradation on platform failures is preferable to all-or-nothing.

## Drift Observations
- None

## Previous Blocker Verification
- SC2086 unquoted `$file_list` (`lib/indexer.sh:162`) — FIXED. Refactored to `read -ra file_array <<< "$file_list"` with `"${file_array[@]}"` iteration.
- `--setup-indexer` ignoring `REPO_MAP_VENV_DIR` (`tekhton.sh:656`, `tools/setup_indexer.sh:22`) — FIXED. `tekhton.sh` now passes `"${REPO_MAP_VENV_DIR:-.claude/indexer-venv}"` as second argument; `setup_indexer.sh` reads it via `VENV_DIR_NAME="${2:-.claude/indexer-venv}"`.
