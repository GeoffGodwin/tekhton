# Architect Plan

**Audit Date:** 2026-03-17
**Observations Reviewed:** 1 unresolved drift entry
**Runs Since Last Audit:** 5

---

## Staleness Fixes

Update `DRIFT_LOG.md` to mark the one unresolved observation as RESOLVED.

**Item 1 — DRIFT_LOG.md stale unresolved entry**

- **File:** `DRIFT_LOG.md`
- **Observation text:** `[2026-03-17 | "Implement Milestone 6: Brownfield Replan"] lib/plan.sh vs lib/replan.sh: The architecture document designates lib/replan.sh for "Brownfield replan orchestration (Milestone 6)" but all new replan functions reside in lib/plan.sh. lib/replan.sh holds mid-run replan (reviewer verdict path). The two files now serve distinct concerns without a naming convention that signals the distinction to future contributors.`
- **Current reality:** This concern has been fully resolved. `lib/plan.sh` no longer contains any replan functions (line 278 is a comment noting the extraction). Brownfield functions (`run_replan`, `_generate_codebase_summary`, `_brownfield_approval_menu`, `_apply_brownfield_delta`, `_archive_replan_delta`) live in `lib/replan_brownfield.sh`. Mid-run replan functions live in `lib/replan_midrun.sh`. `lib/replan.sh` is a thin shim that sources both. The naming convention requested by the observation (`_midrun` / `_brownfield` suffixes) is in place. `ARCHITECTURE.md` lines 97–99 accurately describe all three files.
- **Action:** Move the entry from `## Unresolved Observations` to `## Resolved` with the note: `[RESOLVED 2026-03-17] Brownfield replan functions extracted to lib/replan_brownfield.sh; mid-run replan in lib/replan_midrun.sh; lib/replan.sh is a thin shim. ARCHITECTURE.md lines 97-99 reflect the three-file structure. Naming convention (midrun/brownfield suffixes) is in place.`

---

## Dead Code Removal

None.

---

## Naming Normalization

None.

---

## Simplification

None.

---

## Design Doc Observations

None.

---

## Drift Observations to Resolve

| # | Observation | Disposition | Action |
|---|-------------|-------------|--------|
| 1 | `lib/plan.sh` vs `lib/replan.sh` naming/location confusion | **Already resolved in code** | Mark RESOLVED in DRIFT_LOG.md (Staleness Fix above) |

---

## Out of Scope

None. The single unresolved observation maps directly to a verifiable code state and has a clear remediation (DRIFT_LOG.md update).
