# Architect Plan

## Staleness Fixes

### CLAUDE.md — Current Initiative section describes shipped milestones in future tense

**File:** `CLAUDE.md:167–419`
**Observation:** The "Current Initiative: Planning Phase Quality Overhaul" section describes
Milestones 1–5 in future tense as pending work. Milestones 1 and 2 are confirmed shipped
(commits `dbaa380` and `0cd4660`). The section reads as a specification document for work
yet to be done, not an accurate record of what has been implemented.

**Verification:**
- `lib/plan.sh:39,41` — default model is already `opus` (Milestone 1 acceptance criteria)
- `lib/plan.sh:156–210` — `_extract_template_sections()` outputs `NAME|REQUIRED|GUIDANCE|PHASE`
  (Milestone 2 acceptance criteria)
- `PLAN_GENERATION_MAX_TURNS` default is `30` (Milestone 3 acceptance criterion is `50`) —
  Milestone 3 not yet fully implemented

**Action:** Update the "Current Initiative" section to reflect current state:
- Mark Milestones 1 and 2 as complete (e.g., prefix with `[DONE]` or restructure into
  a "Completed" subsection and a "Remaining" subsection)
- Milestone 3 is in progress — `PLAN_GENERATION_MAX_TURNS` defaults to 30, not 50; the
  prompt overhaul may be partial. Either mark it `[IN PROGRESS]` or leave it in the
  pending list depending on actual verification
- Milestones 4 and 5 remain pending — leave as-is

The fix is a documentation update only. No logic changes.

## Dead Code Removal

None

## Naming Normalization

None

## Simplification

None

## Design Doc Observations

None

## Drift Observations to Resolve

- **[2026-03-11 | "Implement Milestone 7: Tests + Documentation"]** `CLAUDE.md:167–323` —
  Included above under Staleness Fixes. The observation remains valid in updated form: the
  "Current Initiative" section now describes the Planning Phase Quality Overhaul initiative
  (Milestones 1–5) instead of the original 7-milestone plan, but the same inconsistency
  persists — shipped milestones (1 and 2) are still described as pending future work.
  Mark this observation RESOLVED after the staleness fix is applied.

## Out of Scope

None
