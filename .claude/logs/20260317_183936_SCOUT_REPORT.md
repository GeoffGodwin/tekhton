## Relevant Files
- tekhton.sh — Main pipeline entry point where milestone number parsing is conditionally gated
- lib/milestones.sh — Milestone state machine; functions called to initialize state are generic and auto-advance-agnostic
- lib/hooks.sh — Commit message generation that depends on _CURRENT_MILESTONE being populated

## Key Symbols
- _CURRENT_MILESTONE — shell variable, tekhton.sh, should be populated for both `--milestone` and `--auto-advance` modes
- MILESTONE_MODE — shell variable, tekhton.sh, already set correctly for both modes (lines 441, 447)
- init_milestone_state() — lib/milestones.sh, initializes MILESTONE_STATE.md; no auto-advance dependency
- get_milestone_commit_prefix() — lib/milestones.sh, generates prefix string `[MILESTONE N ✓]` or similar
- generate_commit_message() — lib/hooks.sh, uses _CURRENT_MILESTONE to apply milestone prefix

## Suspected Root Cause Areas
- tekhton.sh line 674: Milestone number parsing is gated inside `if [ "$AUTO_ADVANCE" = true ]`, preventing it from running in plain `--milestone` mode
- tekhton.sh line 901: Commit signature check requires `_CURRENT_MILESTONE` to be non-empty, but it never gets populated without auto-advance
- Logic asymmetry: MILESTONE_MODE is set for both `--milestone` and `--auto-advance`, but `_CURRENT_MILESTONE` parsing is tied only to AUTO_ADVANCE

## Complexity Estimate
Files to modify: 1
Estimated lines of change: 15
Interconnected systems: low
Recommended coder turns: 18
Recommended reviewer turns: 6
Recommended tester turns: 12
