# Scout Report: Correct Next 2 Items in NON_BLOCKING_LOG.md

## Task
Correct the next 2 open items in NON_BLOCKING_LOG.md (items 8–9).

## Relevant Files

- `lib/agent_monitor.sh` (359 lines) — Contains FIFO monitoring loop with ring buffer dump block that incorrectly uses `local` declarations inside a compound group (not a function). The declarations at lines 237, 239, 243 are invalid in bash 4 outside function scope.
- `lib/agent.sh` (415 lines) — Exceeds 300-line ceiling. Main function `run_agent()` is 237 lines; helper functions can be extracted to reduce file size.
- `lib/common.sh` — May need to verify it exports `count_lines()` helper for use by agent_summary extraction
- `SCOUT_REPORT.md` — Output file for this report (auto-generated per format)

## Key Symbols

- `run_agent()` — main agent invocation wrapper (lib/agent.sh, lines 74–309)
- `_append_agent_summary()` — structured run summary logging (lib/agent.sh, lines 312–373)
- `was_null_run()` and `check_agent_output()` — null-run detection helpers (lib/agent.sh, lines 378–415)
- `_invoke_and_monitor()` — agent process lifecycle and monitoring (lib/agent_monitor.sh)
- Ring buffer dump block — FIFO reader subshell with invalid `local` declarations (lib/agent_monitor.sh, lines 236–249)

## Suspected Root Cause Areas

1. **agent_monitor.sh:237–244** — `local` keyword used inside `{...}` compound group that is NOT a function. Bash 4 spec: `local` is only valid inside functions. With `set -e` active in the subshell, the first `local _rb_total=...` causes the block to abort; the `|| true` suppresses the error but the ring buffer is never written.

2. **agent.sh structure** — File is 415 lines due to:
   - Main `run_agent()` function: 237 lines (calls into agent_monitor.sh for monitoring)
   - Run summary functions: 62 lines (`_append_agent_summary()`)
   - Helper functions: 36 lines (`was_null_run()`, `check_agent_output()`)
   - Globals and imports: ~45 lines

   To get below 300-line ceiling, need to extract ~120+ lines into a companion file.

## Complexity Estimate

Files to modify: 3
Estimated lines of change: 140
Interconnected systems: medium
Recommended coder turns: 35
Recommended reviewer turns: 8
Recommended tester turns: 20

### Detailed Breakdown

**Item 8 (agent_monitor.sh:237–244):**
- Change: Replace 4 `local` declarations with plain variable assignments
- Impact: Single file, isolated fix, no downstream dependencies
- Risk: Low — purely syntactic fix; variables are already subshell-local by virtue of being in the `(...)` subshell

**Item 9 (agent.sh line count):**
- Change: Extract `_append_agent_summary()` (62 lines) and null-run helpers (36 lines) to new file `lib/agent_summary.sh`
- Impact: Creates new file, updates imports in agent.sh, slight refactoring of dependencies
- Risk: Medium — new file must be sourced in correct order, functions have interdependencies on `AGENT_ERROR_*` globals and `redact_sensitive()` function from lib/errors.sh

**Post-fix line counts:**
- agent.sh: 415 → ~310 (after extracting 105 lines)
- agent_monitor.sh: 359 (no change; pre-existing over-limit, no rework needed per NON_BLOCKING_LOG.md)
- agent_summary.sh: New file, ~110 lines
- tekhton.sh: +1 line (source agent_summary.sh)

### Files Involved

1. `lib/agent_monitor.sh` — Fix `local` declarations (4–5 lines changed)
2. `lib/agent.sh` — Extract `_append_agent_summary()` and null-run helpers to new file (remove 105 lines, update function call)
3. `lib/agent_summary.sh` — New file with extracted functions (~110 lines)
4. `tekhton.sh` — Add `source` line for agent_summary.sh (~1 line)

### Testing Strategy

- Unit test: `local` fix — verify `bash -n` and `shellcheck` pass on modified agent_monitor.sh
- Integration test: Extract verification — ensure all functions are sourced correctly and agent invocation works end-to-end
- Regression test: Existing agent tests should continue to pass with no behavior change
