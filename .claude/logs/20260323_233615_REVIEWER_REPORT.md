# Reviewer Report

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `tests/test_causal_log.sh` line 3: Header comment says "Test: Causal event log (lib/causality.sh)" — now covers `causality_query.sh` too; comment could be updated to reflect the split module.

## Coverage Gaps
- None

## Drift Observations
- None

---

### Review Notes

This continuation run made exactly two changes:

**tekhton.sh — double `_copy_static_files` fix:** The `else` branch correctly gates `sync_dashboard_static_files` to the case where the dashboard directory already exists, so `init_dashboard` (which copies files on creation) is never followed by a redundant second copy. Logic is sound and the intent is clearly documented in the inline comment.

**tests/test_causal_log.sh — missing source fix:** Adding `source "${TEKHTON_HOME}/lib/causality_query.sh"` on line 23 restores test coverage for the query functions (`events_for_milestone`, `events_by_type`, `trace_cause_chain`, `trace_effect_chain`, `cause_chain_summary`, `recurring_pattern`, `verdict_history`) that were extracted into that file. The file exists at `lib/causality_query.sh`. Fix is correct.

Both changes are minimal, correct, and shellcheck-clean as read. No regressions introduced.
