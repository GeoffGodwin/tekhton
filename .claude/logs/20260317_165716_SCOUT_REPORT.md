## Relevant Files
- tekhton.sh — lines 440-450 (flag parsing for --milestone and --auto-advance) and lines 673-690 (milestone initialization logic)
- lib/milestones.sh — get_milestone_commit_prefix() function for generating commit message prefixes
- lib/hooks.sh — generate_commit_message() function uses _MS_COMMIT_NUM and _MS_COMMIT_DISPOSITION to add milestone signatures

## Key Symbols
- MILESTONE_MODE — flag set to true by --milestone (line 441)
- AUTO_ADVANCE — flag set to true by --auto-advance (line 446)
- _CURRENT_MILESTONE — variable that holds the parsed milestone number (line 673)
- _MS_COMMIT_NUM — passed to generate_commit_message() at line 908 for commit signature
- get_milestone_commit_prefix() — in lib/milestones.sh, generates the `[MILESTONE N ✓]` prefix

## Suspected Root Cause Areas
- tekhton.sh lines 674-690: milestone number extraction is gated inside `if [ "$AUTO_ADVANCE" = true ]` block, so it never runs when using `--milestone` without `--auto-advance`
- tekhton.sh lines 901-904: the condition `if [ "$MILESTONE_MODE" = true ] && [ -n "$_CURRENT_MILESTONE" ]` will always be false when `--milestone` is used without `--auto-advance`, because `_CURRENT_MILESTONE` is never populated

## Complexity Estimate
Files to modify: 1
Estimated lines of change: 8
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 5
Recommended tester turns: 12
