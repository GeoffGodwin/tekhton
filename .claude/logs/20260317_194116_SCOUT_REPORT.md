## Relevant Files
- tekhton.sh — contains milestone number parsing logic (line 676) gated inside auto-advance block, and commit signature generation (line 901) that depends on `_CURRENT_MILESTONE` variable
- lib/milestones.sh — provides `get_milestone_commit_prefix()` and `get_milestone_disposition()` functions used by hooks.sh to generate commit signatures

## Key Symbols
- `_CURRENT_MILESTONE` — global variable in tekhton.sh, currently only populated when `AUTO_ADVANCE = true`
- `MILESTONE_MODE` — global flag set to true by both `--milestone` and `--auto-advance` CLI options (lines 441, 447)
- `get_milestone_commit_prefix()` — milestones.sh:419, generates `[MILESTONE N ✓]` or `[MILESTONE N — partial]` prefix
- `generate_commit_message()` — hooks.sh:63, uses milestone_num and disposition to create commit signatures

## Suspected Root Cause Areas
- tekhton.sh line 674-689: milestone number parsing is nested inside `if [ "$AUTO_ADVANCE" = true ]` block, making it unreachable for plain `--milestone` single-run mode
- tekhton.sh line 901: commit signature generation checks both `MILESTONE_MODE` and `_CURRENT_MILESTONE`, but `_CURRENT_MILESTONE` is empty for `--milestone` without `--auto-advance`

## Complexity Estimate
Files to modify: 1
Estimated lines of change: 25
Interconnected systems: low
Recommended coder turns: 18
Recommended reviewer turns: 6
Recommended tester turns: 12
