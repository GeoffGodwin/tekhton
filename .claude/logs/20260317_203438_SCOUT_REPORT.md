## Relevant Files
- `lib/config.sh` — contains the validation logic with the bug on line 136
- `tests/test_config_validation_failures.sh` — test that detects the regression

## Key Symbols
- `load_config()` — lib/config.sh:94
- `_parse_config_file()` — lib/config.sh:22
- Required key validation loop — lib/config.sh:135-139

## Suspected Root Cause Areas
- `lib/config.sh:136` — The validation check `if [ -z "${!key:-}" ];` uses indirect variable expansion that will pass if the variable is already set in the parent shell's environment, even if the key is absent from pipeline.conf. The validation must ensure required keys are present in the *config file*, not just present as shell variables.

## Complexity Estimate
Files to modify: 1
Estimated lines of change: 5
Interconnected systems: low
Recommended coder turns: 10
Recommended reviewer turns: 5
Recommended tester turns: 10
