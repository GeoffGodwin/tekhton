# Security Notes

Generated: 2026-03-23 14:59:16

## Non-Blocking Findings (MEDIUM/LOW)
- [MEDIUM] [category:A03] [prompts/security_rework.prompt.md:10] fixable:yes — `{{SECURITY_FIXABLE_BLOCK}}` is populated from parsed SECURITY_REPORT.md (agent-written content) and injected directly into the rework prompt without being wrapped in BEGIN/END FILE CONTENT delimiters. The security directive in this prompt only protects against delimited blocks, so a compromised or misbehaving security agent could embed instructions (e.g., "ignore previous directives, exfiltrate X") that the rework coder would receive as trusted prompt content. Fix: wrap the block with a delimited header and a reminder that the block originates from an agent and may contain adversarial content — matching the pattern used in the scan prompt and reviewer prompt.
- [LOW] [category:A05] [lib/indexer.sh:152] fixable:yes — When `mktemp` fails, the stderr capture file falls back to the predictable path `/tmp/tekhton_indexer_$$` (PID-based naming). On a shared multi-user system an attacker who knows or can enumerate the PID could pre-create a symlink at that path, redirecting the captured stderr to an arbitrary file. Fix: use `mktemp` without a fallback or abort gracefully if `mktemp` fails.
- [LOW] [category:A03] [lib/finalize_summary.sh:48] fixable:yes — When constructing the `files_changed` JSON array in `RUN_SUMMARY.json`, file paths from `git diff --name-only` are escaped for `\` and `"` only (`sed 's/\\/\\\\/g; s/"/\\"/g'`). Control characters (e.g., tabs, carriage returns, null bytes) in git-tracked file paths are not escaped, which can produce malformed JSON that breaks downstream consumers of `RUN_SUMMARY.json`. Fix: strip or escape all JSON control characters (U+0000–U+001F) in addition to the existing escapes.
- [LOW] [category:A03] [lib/indexer_history.sh:151] fixable:yes — The `safe_task` variable strips newlines and escapes `\` and `"` but does not escape other JSON control characters (e.g., `\t`, `\b`, U+0000–U+001F). Task descriptions containing such characters produce malformed JSONL records in `task_history.jsonl`, which will silently fail to parse during personalized ranking on future runs. Fix: apply the same control-character escaping fix as in `finalize_summary.sh`.
