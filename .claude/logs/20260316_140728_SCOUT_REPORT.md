# Scout Report: Milestone 2 - Context Compiler

## Relevant Files

- `lib/context.sh` — Milestone 1 (token accounting) is complete here; Milestone 2 adds three new functions: `extract_relevant_sections()`, `build_context_packet()`, and `compress_context()`. This file currently ends at line 148 and needs ~100–150 lines of new code.
- `lib/config.sh` — Need to add `CONTEXT_COMPILER_ENABLED=false` default around line 109 (after context budget defaults). Single line addition.
- `stages/coder.sh` — Lines 210–219 show current context component accumulation via `_add_context_component()`. When `CONTEXT_COMPILER_ENABLED=true`, replace raw block concatenation (lines 86–206) with `build_context_packet()` call. Architecture block stays full for coder. ~20–30 lines of integration logic.
- `stages/review.sh` — Lines 34–47 show context reporting. When enabled, need to filter `ARCHITECTURE_CONTENT` to sections referencing files in `CODER_SUMMARY.md`. ~15–20 lines of filtering logic.
- `stages/tester.sh` — Lines 24–33 show fresh-run context setup. When enabled, filter architecture to relevant sections for test scope. ~10–15 lines of filtering.
- `templates/pipeline.conf.example` — Document new `CONTEXT_COMPILER_ENABLED` config key around line 210 (after context budget section). ~5–10 lines of comments and key definition.

## Key Symbols

- `check_context_budget()` — lib/context.sh:131, used by new compression logic to know when compression is needed
- `_add_context_component()` — lib/context.sh:67, already exists; Milestone 2 calls it from new `build_context_packet()`
- `build_context_packet()` — new function to assemble and optionally compress context (will live in lib/context.sh)
- `extract_relevant_sections()` — new function to filter markdown sections by keyword (will live in lib/context.sh)
- `compress_context()` — new function to apply compression strategies: truncate, summarize_headings, omit (will live in lib/context.sh)
- `_safe_read_file()` — lib/prompts.sh:50, already exists; Milestone 2 will call it when reading architecture for filtering

## Suspected Root Cause Areas

- **Keyword extraction logic** — Task string parsing and file-list extraction from `CODER_SUMMARY.md` must handle varied formatting (bullets, dashes, markdown links, etc.). Fallback to full injection if zero matches found is critical.
- **Section filtering in markdown** — Simple awk-based filtering by `##` headings is fragile; edge cases: sections with no sub-content, code blocks containing markdown-like text, malformed markdown.
- **Compression priority order** — Which context components to compress first (prior tester context → non-blocking notes → prior progress) must not starve agents of critical architecture or task context.
- **Integration point in coder.sh** — Lines 210–219 accumulate blocks; new logic must not break the existing `_add_context_component()` → `log_context_report()` flow.
- **Fallback behavior** — If keyword extraction yields zero matches, must silently revert to full block injection without errors or truncation.

## Complexity Estimate

Files to modify: 6
Estimated lines of change: 220
Interconnected systems: medium
Recommended coder turns: 50
Recommended reviewer turns: 10
Recommended tester turns: 30
