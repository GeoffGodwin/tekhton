# Reviewer Report — Milestone 12.2 / 12.3

Review cycle: 2 of 4

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `stages/coder.sh:321` — `success "Coder agent finished."` still fires before the UPSTREAM error check at line 328 (carry-forward from cycle 1 non-blocking note). When the coder hits an API error the terminal shows `[✓] Coder agent finished.` immediately followed by `[✗] Coder agent hit an API error`. Consider guarding the success log or moving it after the UPSTREAM check.
- `lib/agent_monitor.sh` — File is 356 lines after dead code removal (down from 434). Still above the 300-line soft limit from reviewer.md. No rework needed given the directive on re-reviews, but worth splitting in a future cleanup sweep.

## Coverage Gaps
- No tests for `report_error()` in `lib/common.sh` — Unicode vs ASCII branch, box-drawing path, and no-recovery-string path are untested (carry-forward from cycle 1).
- Ring buffer dump and `agent_api_error.txt` flag file handoff from FIFO subshell are untested.
- `lib/state.sh` error classification block (redaction path and "no output captured" fallback) is untested.

## Drift Observations
- `lib/agent.sh:319` and `lib/common.sh:50` — Unicode/ASCII terminal detection (`grep -qi 'utf-\?8'` against `LANG/LC_ALL`) is duplicated between `_append_agent_summary` and `report_error`. A shared `_is_utf8_terminal()` helper in `lib/common.sh` would guarantee visual consistency. (carry-forward from cycle 1)
- `lib/agent_monitor.sh` — Comment exists at lines 55–60 explaining the subshell variable isolation (API error flags written to file for parent to read), but there is no equivalent comment near the ring buffer dump block (lines 232–245) explaining why the dump uses a `{...}` compound group rather than a function call. Future contributors may wonder why `_ring_buffer_add`-style functions were not used. A brief inline comment would close the gap.
