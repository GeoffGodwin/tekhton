## Relevant Files

### Files to Modify
- `tekhton.sh` (lines 96-164) — Add early-exit `--plan` block following the same pattern as `--init`. Must source only `lib/common.sh`, handle project type selection, resolve template path, then exit cleanly with `exit 0`.
- `templates/pipeline.conf.example` (Milestone 6) — Add planning configuration section with `CLAUDE_PLAN_MODEL`, `PLAN_INTERVIEW_MAX_TURNS`, etc. Not needed for Milestone 1.

### Files to Create
- `lib/plan.sh` — NEW. Planning phase orchestration library with:
  - `run_plan()` main function
  - `show_project_type_menu()` function to display 7 project types and capture user selection
  - `resolve_template_path(type)` function to map type to template file path
  - Template validation (ensure all 7 templates exist)
- `templates/plans/web-app.md` — NEW. Design template for web applications
- `templates/plans/web-game.md` — NEW. Design template for web-based games
- `templates/plans/cli-tool.md` — NEW. Design template for CLI utilities
- `templates/plans/api-service.md` — NEW. Design template for API services/backends
- `templates/plans/mobile-app.md` — NEW. Design template for mobile applications
- `templates/plans/library.md` — NEW. Design template for libraries/SDKs
- `templates/plans/custom.md` — NEW. Minimal fallback template for other project types

### Reference Files (Read-Only)
- `lib/common.sh` (lines 1-30) — Provides logging functions: `log()`, `success()`, `warn()`, `error()`, `header()`, `require_cmd()`
- `DESIGN.md` (lines 30-128) — Specification for planning phase architecture and template structure
- `CLAUDE.md` (lines 96-164 in tekhton.sh) — Entry point pattern for `--init` early-exit block

## Key Symbols

### Entry Point Pattern
- `tekhton.sh:98-164` — `if [ "${1:-}" = "--init" ]` block showing early-exit pattern for new CLI flags
  - Checks argument first
  - Sources only `lib/common.sh`
  - Does initialization work
  - Exits cleanly with `exit 0`
  - Must NOT source other libraries or load config

### Logging Utilities
- `lib/common.sh:19-23` — Logging functions: `log()`, `success()`, `warn()`, `error()`, `header()`
- `lib/common.sh:27-29` — `require_cmd()` for prerequisite validation

### Key Functions to Create
- `lib/plan.sh:run_plan()` — Main orchestration function; entry point called from tekhton.sh early-exit block
- `lib/plan.sh:show_project_type_menu()` — Interactive menu to display 7 project types and capture user selection
- `lib/plan.sh:resolve_template_path(type)` — Maps type string to template file path; validates path exists

## Suspected Root Cause Areas

1. **tekhton.sh early-exit block** — Must be added BEFORE library sources (line 166+) to avoid config loading. Pattern exists in lines 98-164 for `--init`; replicate for `--plan`.

2. **lib/plan.sh project type menu** — Menu loop must handle invalid input gracefully, re-prompt on error, and return selected type to caller.

3. **Template path resolution** — Must validate that all 7 templates exist in `templates/plans/` before returning path; fail loudly if any are missing.

4. **Template structure** — All 7 templates must follow markdown section structure with guidance comments; consistency across templates enables interview agent to process them uniformly.

## Complexity Estimate

**Files to modify:** 1 (tekhton.sh)
**Files to create:** 8 (lib/plan.sh + 7 templates)
**Estimated lines of change:** 150-200
  - tekhton.sh: ~50 lines (early-exit block + sourcing lib/plan.sh)
  - lib/plan.sh: ~80 lines (run_plan, menu, template resolution)
  - Each template: ~30-50 lines (7 × 35 = 245 lines, but templates are boilerplate)
**Interconnected systems:** low
  - Only touches tekhton.sh entry point
  - New lib/plan.sh is standalone (no dependencies on coder/review/tester logic)
  - Templates are static markdown files
  - No changes to execution pipeline, config system, or agent infrastructure
**Recommended coder turns:** 25-35
  - Menu UX and input validation (10 turns)
  - lib/plan.sh orchestration logic (5 turns)
  - 7 template implementations (10-15 turns, ~2 per template + QA)
  - Integration testing (5 turns)
**Recommended reviewer turns:** 5-8
  - Entry point pattern consistency (2 turns)
  - lib/plan.sh shellcheck compliance and error handling (2 turns)
  - Template completeness and structure (2 turns)
  - Overall architecture alignment (1 turn)
**Recommended tester turns:** 10-15
  - Menu selection and validation (3 turns)
  - Template path resolution and validation (3 turns)
  - Error cases: missing templates, invalid input (2 turns)
  - Integration with tekhton.sh early-exit pattern (2 turns)
  - Edge cases: typos, re-prompts, Ctrl+C handling (2 turns)
