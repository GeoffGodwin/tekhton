# Reviewer Report

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `repo_map.py:113` imports the private symbol `_EXT_TO_LANG` directly from `tree_sitter_languages.py`. A small public helper (e.g. `get_extensions_for_languages(requested: set[str]) -> set[str]`) would decouple the modules and avoid leaking an underscore-prefixed name across file boundaries.
- `repo_map.py:260-262` — the Python colon-detection condition `(not before.endswith(","))` is a weak guard that could spuriously truncate signatures containing commas before the final colon. Low risk given the 500-char window cap.
- `tools/tests/` has no direct tests for `tree_sitter_languages.py` (grammar loading, `supported_extensions`, `ext_to_language_name`). These are exercised indirectly only; basic unit tests using mocks would catch regressions in the mapping table.

## Coverage Gaps
- No test for `_extract_tags()` end-to-end with a real file (even a temp `.py` file), meaning the tree-sitter parse path is only exercised when the package is installed. A single integration-style test guarded by `pytest.importorskip("tree_sitter")` would close this gap.
- `get_repo_map_slice()` in `lib/indexer.sh` has no corresponding shell test covering the "partial match" and "no match" branches (returns 1 when result is empty).

## ACP Verdicts
None

## Drift Observations
- `repo_map.py:113` — direct import of `_EXT_TO_LANG` (private module-level dict in `tree_sitter_languages.py`) creates hidden coupling: if the internal data structure ever changes name or format, `repo_map.py` will break with an `ImportError` rather than a clear AttributeError at the call site. Consider exporting a public API function from `tree_sitter_languages.py` for this use.
