# Reviewer Report

## Verdict
APPROVED

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- None

## Coverage Gaps
- None

## Drift Observations
- None

---

**Review summary:**

Both changes are correct, minimal, and consistent with existing patterns.

**Item 1 (`stages/coder.sh` line 329–331):** `export ACTUAL_CODER_TURNS="${LAST_AGENT_TURNS:-0}"` placed immediately after `print_run_summary` / `success` and before the null-run detection block. This exactly mirrors the pattern from the primary coder run at line 252. Placement is correct — the export occurs before the null-run check so a null run still updates the variable to 0, consistent with how the primary run handles it. No issues.

**Item 2 (`lib/turns.sh` lines 161–163):** Three-line comment accurately describes the grep/tr pipeline semantics: (1) grep-no-match triggers `|| echo "0"` fallback, (2) grep match feeds non-digit-stripped output from `tr`, (3) `${var:-0}` guards arithmetic. Comment is accurate, concise, and placed immediately above the relevant code. No issues.

**NON_BLOCKING_LOG.md:** Items 1 and 2 correctly marked `[x]` with resolution notes. Remaining open item (ERE alternation portability note for awk) is intentionally untouched — not part of this task. Log state is consistent.
