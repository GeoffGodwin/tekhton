# Reviewer Report — Milestone 9 (Cycle 2)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `stages/security.sh` is 372 lines, exceeding the 300-line ceiling. Helper functions (`_parse_security_findings`, `_build_fixable_block`, `_build_unfixable_block`, `_build_notes_block`, `_write_security_notes`, `_handle_unfixable_findings`, `_security_is_docs_only`, `_severity_meets_threshold`) should be extracted to `lib/security_helpers.sh` at the next opportunity.
- `stages/security.sh` is missing `set -euo pipefail`. Every other `.sh` file in the project has it. Since stages are sourced by `tekhton.sh` (which has it), the parent's options apply at function call time, but the file-level declaration is still convention and should be added.
- `run_agent` for the security scan agent passes `"${AGENT_TOOLS_REVIEWER:-}"` as the tools parameter (security.sh:292). This is presumably intentional (read-only tools appropriate for a scan agent), but `AGENT_TOOLS_REVIEWER` is a borrowed name. If a dedicated `AGENT_TOOLS_SECURITY` variable is ever introduced, this should be updated.

## Coverage Gaps
- No self-tests for `stages/security.sh` helper functions (`_parse_security_findings`, `_severity_meets_threshold`, `_build_fixable_block`). These are pure bash functions with deterministic I/O and are good candidates for unit tests.

## ACP Verdicts
- None

## Drift Observations
- None

---

### Blocker Verification (S-1)

**S-1: `_indexer_resolve_cache_dir()` not added to `lib/indexer_helpers.sh`** — VERIFIED FIXED.
- Function added at `lib/indexer_helpers.sh:22–29`.
- `lib/indexer.sh:132` (`run_repo_map`) now calls `_indexer_resolve_cache_dir()`.
- `lib/indexer.sh:272–275` inline idiom replaced via same refactor.
- `lib/indexer_history.sh:25` (`_ensure_task_history_file`) calls `_indexer_resolve_cache_dir()`.
- `lib/indexer_history.sh:57` (`warm_index_cache`) calls `_indexer_resolve_cache_dir()`.
- `tests/test_indexer_history.sh:43` now sources `indexer_helpers.sh` to satisfy the dependency.

All four inline idioms from the prior report are eliminated.
