# Scout Report: Milestone 1 Token And Context Accounting

## Relevant Files

- `tekhton.sh` — Entry point; needs to source `lib/context.sh` (lines ~250-259 where libs are sourced)
- `lib/config.sh` — Config defaults; add context-related config keys (lines ~127-190 where defaults are set)
- `lib/agent.sh` — `print_run_summary()` function at line 140; add context size output to summary
- `lib/context.sh` — NEW FILE; must implement `measure_context_size()`, `log_context_report()`, `check_context_budget()`
- `stages/coder.sh` — Context assembly pattern at lines 86-206; call `log_context_report()` before `render_prompt()` at line 215
- `stages/review.sh` — Similar pattern; find context assembly and call `log_context_report()` before `render_prompt()`
- `stages/tester.sh` — Similar pattern; find context assembly and call `log_context_report()` before `render_prompt()` at line ~33
- `templates/pipeline.conf.example` — Document new config keys (`CONTEXT_BUDGET_PCT`, `CHARS_PER_TOKEN`, `CONTEXT_BUDGET_ENABLED`)

## Key Symbols

- `measure_context_size()` — NEW function in lib/context.sh
- `log_context_report()` — NEW function in lib/context.sh; writes structured breakdown to log
- `check_context_budget()` — NEW function in lib/context.sh; returns 0/1 for budget status
- `print_run_summary()` — lib/agent.sh:140; extend to include context line
- `render_prompt()` — lib/prompts.sh; context reporting called BEFORE this
- `run_agent()` — lib/agent.sh:160; invoked after prompt rendering
- Context blocks: `ARCHITECTURE_BLOCK`, `HUMAN_NOTES_BLOCK`, `GLOSSARY_BLOCK`, `MILESTONE_BLOCK`, `PRIOR_REVIEWER_CONTEXT`, `PRIOR_PROGRESS_CONTEXT`, `PRIOR_TESTER_CONTEXT`, `NON_BLOCKING_CONTEXT`

## Suspected Root Cause Areas

- lib/context.sh will need a model window lookup table (opus: 200k, sonnet: 200k, haiku: 100k tokens)
- Config defaults in lib/config.sh follow pattern `: "${VAR:=default}"` — add CONTEXT_BUDGET_PCT=50, CHARS_PER_TOKEN=4, CONTEXT_BUDGET_ENABLED=true
- Each stage (coder/review/tester) needs identical integration: assemble context blocks → call `log_context_report(block_name, content)` for each → render prompt
- print_run_summary needs to read from a context metrics variable after prompt is rendered (timing issue: context size known before agent runs)

## Complexity Estimate

Files to modify: 7
Estimated lines of change: 250
Interconnected systems: low
Recommended coder turns: 25
Recommended reviewer turns: 8
Recommended tester turns: 20
