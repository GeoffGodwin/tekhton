## Planned Tests
- [x] `tests/test_specialists.sh` — add escape sequence safety tests for `_append_specialist_notes` awk fix

## Test Run Results
Passed: 27  Failed: 2

## Bugs Found
- BUG: [lib/specialists.sh:240] `_append_specialist_notes` uses `awk -v block=` which still interprets escape sequences (`\n`→newline, `\U`→`U`); notes containing backslash sequences (e.g. Windows paths like `C:\Users\foo` or literal `\n`) are silently corrupted in NON_BLOCKING_LOG.md

## Files Modified
- [x] `tests/test_specialists.sh`
