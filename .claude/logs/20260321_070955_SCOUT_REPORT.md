## Relevant Files

- `NON_BLOCKING_LOG.md` — Accumulates reviewer non-blocking observations; currently has 2 unresolved items affecting init_synthesize.sh
- `stages/init_synthesize.sh` — Agent-assisted project synthesis stage (Milestone 21); contains two code issues at lines 247 and 256
- `lib/drift_cleanup.sh` — Non-blocking notes management; provides `_resolve_addressed_nonblocking_notes()` and functions to mark items as resolved; also provides the pattern for moving items between Open and Resolved sections
- `lib/drift.sh` — Parent drift log management module; provides `_awk_join_bullets()` helper used by drift_cleanup

## Key Symbols

- `_check_synthesis_completeness()` at stages/init_synthesize.sh:235 — Checks DESIGN.md section count and triggers re-synthesis on thin sections
- `section_count` at stages/init_synthesize.sh:247 — Counts `## ` headers; controls whether individual section depth inspection runs
- `section_name` at stages/init_synthesize.sh:256 — Loop variable being stripped of markdown prefix; redundantly processed
- `_resolve_addressed_nonblocking_notes()` at lib/drift_cleanup.sh:104 — Marks NON_BLOCKING_LOG.md items `[x]` when referenced files are modified
- `clear_completed_nonblocking_notes()` at lib/drift_cleanup.sh:172 — Removes completed items from ## Open section of NON_BLOCKING_LOG.md

## Suspected Root Cause Areas

- **Issue 1 (line 256):** Redundant string manipulation; the sed at line 262 already strips `## ` prefix before the loop, making `section_name="${section_name#\#\# }"` assignment a no-op. Safe to remove entirely.
- **Issue 2 (line 247):** Incomplete thin-section detection logic; when `section_count >= 5`, the block at lines 249–280 (which inspects individual section depth) is unreachable. A document with 6+ sections but several empty ones passes "completeness OK" without depth validation. The `PLAN_INCOMPLETE_SECTIONS` export is only set inside the `if` block, making it unavailable for the re-synthesis prompt when section count is sufficient but section quality is poor.

## Complexity Estimate

Files to modify: 2
Estimated lines of change: 8
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 5
Recommended tester turns: 15
