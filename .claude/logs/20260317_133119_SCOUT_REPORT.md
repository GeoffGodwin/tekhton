# Scout Report

## Relevant Files
- `lib/specialists.sh:241-244` — Variable cleanup issue with awk failure path
- `tests/test_specialists.sh` — Test coverage for `_append_specialist_notes()` function

## Key Symbols
- `_append_specialist_notes()` — lib/specialists.sh, exported variable cleanup needed
- `_SPECIALIST_INSERT_BLOCK` — lib/specialists.sh:241, not cleaned up on awk failure
- `awk` command — lib/specialists.sh:242, can exit non-zero without triggering cleanup

## Suspected Root Cause Areas
- The awk invocation at lib/specialists.sh:242-243 can fail (file read error, invalid data, etc.) but the unset at line 244 is unreachable due to `set -euo pipefail` causing immediate script exit if awk fails
- Variable `_SPECIALIST_INSERT_BLOCK` remains exported into the shell environment when awk fails, creating environment pollution (though contained to that shell session)

## Complexity Estimate
Files to modify: 1
Estimated lines of change: 8
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 5
Recommended tester turns: 15
