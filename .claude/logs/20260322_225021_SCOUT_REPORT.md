# Scout Report: Fix Outstanding Drift Observations

## Summary
The task is to resolve unresolved observations in DRIFT_LOG.md. The main outstanding issue is a code style repetition pattern in `lib/indexer_helpers.sh:detect_repo_languages()` that has exceeded the documented threshold for remediation.

## Relevant Files
- `lib/indexer_helpers.sh` — Contains the `detect_repo_languages()` function with 11 repetitive seen-set checks that were refactored from compact `&&`-chained style to verbose `if...then...fi` statements. This function detects programming languages by scanning file extensions at the project root.
- `DRIFT_LOG.md` — Currently documents "two occurrences" of the pattern as an unresolved observation. The file states the threshold is "if it spreads further" (i.e., a third occurrence), but the actual count is 11 occurrences within a single function.
- `lib/indexer.sh` — Parent file from which `indexer_helpers.sh` was extracted; may provide context on intended patterns (no similar pattern found here).

## Key Symbols
- `detect_repo_languages()` — lib/indexer_helpers.sh:21-60
  - Iterates over files in project root
  - Maps file extensions to language names using a case statement
  - 11 seen-set check patterns (lines 34, 36, 38, 40, 42, 44, 46, 48, 50, 52, 54)
- `seen` — associative array used to deduplicate languages (line 28)

## Suspected Root Cause Areas
- `lib/indexer_helpers.sh:30-55` — The case statement contains 11 near-identical multi-line blocks that test `seen[$lang]`, append to `languages[]` array, and set `seen[$lang]=1`. Each block uses the verbose `if [[ -z ... ]]; then ... fi` structure instead of the more compact `&&`-chained style from the previous commit.
- **Pattern mismatch**: Commit 58c3ea3 refactored the code from `[[ -z ... ]] && ... && ...` (one line per case) to `if...; then...; fi` (three lines per case), creating the verbose repetition. The DRIFT_LOG correctly flagged this as approaching a sweep threshold, but the count in the log is outdated (says "two occurrences" when there are actually 11).

## Complexity Estimate
Files to modify: 2
Estimated lines of change: 18
Interconnected systems: low
Recommended coder turns: 22
Recommended reviewer turns: 6
Recommended tester turns: 18
