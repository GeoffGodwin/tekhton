# Scout Report — Milestone 2 Testing Gap

## Bug Summary

The commit f0124f6 ("Added logic to protect the pipeline in differing environments...") contains significant untested changes to `lib/agent.sh` — the core invocation wrapper used by ALL stages (coder, reviewer, tester, architect).

The `TESTER_REPORT.md` falsely claims "Bugs Found: None" and reports only on planning-phase tests (`test_plan_phase_context.sh` and `test_plan_phase_transitions.sh`). It **completely omits** any mention of testing the major agent.sh refactoring, leaving those critical changes unvalidated.

## Relevant Files

### Files with Untested Changes
- **lib/agent.sh** — Major refactoring: FIFO-isolated invocation, activity timeout detection, Windows-native claude.exe cleanup via taskkill. These are the riskiest changes in the commit, used by every agent in the pipeline.
- **lib/plan.sh** — Changes to planning configuration and state handling (needs testing coverage confirmation)
- **stages/plan_followup_interview.sh** — New stage file (NEW, untested)

### Files That Need Test Coverage
- **tests/test_agent_exit_detection.sh** — Only tests globals and null-run detection, does NOT test the new FIFO invocation, activity timeouts, or Windows process killing
- **tests/** — No test files exist for FIFO invocation, activity timeout behavior, or Windows taskkill integration

### Documentation Files
- **TESTER_REPORT.md** — Incomplete; reports only planning-phase tests and false "None" verdict for bugs. Silent about agent.sh changes.

## Key Symbols & Functions

| Symbol | File | Issue |
|--------|------|-------|
| `run_agent()` | `lib/agent.sh:89-` | FIFO invocation logic untested; activity timeout logic untested |
| `_kill_agent_windows()` | `lib/agent.sh:46-57` | Windows taskkill fallback untested |
| `_AGENT_WINDOWS_CLAUDE` | `lib/agent.sh:28-40` | Windows/WSL detection untested |
| `test_agent_exit_detection.sh` | `tests/test_agent_exit_detection.sh` | Tests only globals, not actual `run_agent()` invocation |

## Suspected Root Cause Areas

1. **Incomplete TESTER_REPORT.md** — Line 14 says "Bugs Found: None" without acknowledging that major lib/agent.sh changes were excluded from testing scope.
2. **Missing test coverage for lib/agent.sh refactoring** — The FIFO-based invocation, activity timeout detection, and Windows process cleanup need explicit tests.
3. **Scope creep not reflected in report** — The commit touches both planning phase (tested) and core agent invocation (untested), but the report only documents the former.

## Impact Assessment

- **Risk**: **HIGH** — lib/agent.sh is used by every stage in the execution pipeline (coder, reviewer, tester, architect). Untested changes here can break all downstream work.
- **Blast radius**: All execution pipeline stages + conversational planning stages
- **Detection window**: Failures would surface only when running actual pipeline commands, not during unit tests

## Complexity Estimate

**Files to modify**: 2
- `TESTER_REPORT.md` — Acknowledge the untested changes and scope limitation
- `tests/` — Add test coverage for agent.sh FIFO invocation and activity timeout logic

**Estimated lines of change**: 50–150
- Rewrite TESTER_REPORT.md findings section (~30 lines)
- Add 2–3 test files for agent.sh behavior (~100–150 lines total)

**Interconnected systems**: medium
- lib/agent.sh is a core system used across all stages
- Tests require mocking claude invocation or using test doubles

**Recommended turns**:
- **Coder**: 25–35 (write new test files for agent.sh FIFO logic)
- **Reviewer**: 8–10 (verify test quality and coverage)
- **Tester**: 20–25 (run new tests and validate agent.sh behavior under various conditions)
