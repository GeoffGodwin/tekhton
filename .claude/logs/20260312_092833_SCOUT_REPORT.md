## Relevant Files

- **NON_BLOCKING_LOG.md** — Source of task; lists 4 unchecked accumulated items (lines 11, 12, 16, 19, 23)
- **stages/plan_followup_interview.sh** — Line 100-104: `req` variable extracted but display hardcodes `*required` unconditionally
- **tests/test_agent_fifo_invocation.sh** — Lines 182-187: Phase 6 sets `AGENT_TIMEOUT=0` but never resets it for Phases 7-9
- **lib/agent.sh** — Lines 355-365: Timeout warning message shows confusing "0s" when outer timeout disabled
- **CLAUDE.md** — Lines 167-419: "Current Initiative: Planning Phase Quality Overhaul" section describes 7 milestones marked `[DONE]` as if they're future work

## Key Symbols

- `run_plan_followup_interview()` — stages/plan_followup_interview.sh:31
- `section_required_map` — stages/plan_followup_interview.sh:100 (source), lib/plan_completeness.sh (populated)
- `AGENT_TIMEOUT` — lib/agent.sh (used in timeout logic), tests/test_agent_fifo_invocation.sh:187 (set but not reset)
- `_timeout` variable — lib/agent.sh:364 (expands to outer timeout value in warning message)

## Suspected Root Cause Areas

1. **stages/plan_followup_interview.sh:104** — Display hardcodes `*required` label instead of using `req` variable extracted on line 100. Should conditionally show `*required` or `(optional)` like plan_interview.sh does.

2. **tests/test_agent_fifo_invocation.sh:187** — `AGENT_TIMEOUT=0` disables outer timeout for Phase 6 activity-timeout testing, but is never reset before Phases 7-8-9, leaving subsequent mock agent calls operating with outer timeout disabled. Need `AGENT_TIMEOUT=30` reset after Phase 6 (around line 203).

3. **lib/agent.sh:364** — When `AGENT_TIMEOUT=0` (outer timeout disabled), the null-run warning prints `${_timeout}s` which expands to "0s", creating confusing output like "agent timed out after 0s". Should either show activity timeout message or clarify that outer timeout was disabled.

4. **CLAUDE.md:167-419** — "Current Initiative" section is entirely stale. All 7 milestones are marked `[DONE]` but section still describes them as future work. Should be replaced with a "Completed: Planning Phase" summary or removed entirely (per NON_BLOCKING_LOG line 23).

## Complexity Estimate

**Files to modify:** 4
**Estimated lines of change:** ~30
**Interconnected systems:** low
**Recommended coder turns:** 25-30
**Recommended reviewer turns:** 5-8
**Recommended tester turns:** 15-20

**Rationale:**
- All changes are localized bug fixes and documentation updates
- No cross-module dependencies affected
- Three fixes are single-line or few-line changes (display label, timeout reset, condition in warning)
- Documentation cleanup is straightforward (replace stale section)
- Low risk: minimal code paths affected, changes are defensive rather than feature-adding
