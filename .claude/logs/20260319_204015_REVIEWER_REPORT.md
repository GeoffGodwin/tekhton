# Reviewer Report — Milestone 15.2.1 (re-review)

## Verdict
APPROVED

## Complex Blockers (senior coder)
None

## Simple Blockers (jr coder)
None

## Non-Blocking Notes
- CODER_SUMMARY.md lists only `lib/milestone_ops.sh` under "Files Modified" but omits the newly created `lib/milestone_acceptance.sh`. Minor documentation gap — the extraction is correct and the file is well-structured.
- `milestone_ops.sh` header comment still reads "Expects: milestone_acceptance.sh, milestones.sh to be sourced first" but `milestone_ops.sh` now sources `milestone_acceptance.sh` itself (line 25). The "Expects" wording is now inaccurate for `milestone_acceptance.sh` — `milestones.sh` still needs to be sourced by the caller. Low-priority comment fix.
- Carried from prior review: `mark_milestone_done` defaults the CLAUDE.md path via `${2:-${PROJECT_RULES_FILE:-CLAUDE.md}}` which is superior to the bare `CLAUDE.md` used by sibling functions `check_milestone_acceptance` and `get_milestone_commit_body`. Consider back-filling `PROJECT_RULES_FILE` awareness to those functions in a future cleanup task.

## Coverage Gaps
- No test file for `mark_milestone_done`. Carried from prior review — still not required by the spec but useful for locking in dotted-number handling and idempotency behavior.

## ACP Verdicts
None.

## Drift Observations
None
