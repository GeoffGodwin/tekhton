# Architect Plan — 2026-03-19

## Summary

All 10 unresolved drift observations are addressed below. 9 are stale (the code was
corrected or restructured after the observation was written) and are moved to Out of
Scope with justification. 1 is informational (the Milestone 14 dual-touch note) and
is also moved to Out of Scope. One genuine staleness issue was discovered during
verification: ARCHITECTURE.md's Layer 3 library list is 9 files behind the actual
`lib/` directory — extracted helper modules from several prior milestones were never
back-ported into the architecture document.

---

## Staleness Fixes

Route to: **jr coder**

### SF-1: ARCHITECTURE.md Layer 3 library list is missing 9 real lib files

**File:** `ARCHITECTURE.md`, Layer 3 library section.

**Problem:** The architecture document lists 23 libraries under `lib/`. The actual
`lib/` directory contains 32. Nine modules extracted from larger files across
Milestones 2, 8, 10, 11, 12, and 13 were never added to the architecture doc.

**Action:** Add the following entries to the Layer 3 library list in `ARCHITECTURE.md`,
in sorted order alongside their existing neighbors:

| File | Description |
|------|-------------|
| `lib/agent_monitor_helpers.sh` | Post-invocation monitoring helpers: `_reset_monitoring_state()`, `_detect_file_changes()`, `_count_changed_files_since()`. Sourced by `agent.sh` after `agent_monitor.sh`. |
| `lib/agent_retry.sh` | Transient error retry envelope (Milestone 13.2.1). Wraps `_invoke_and_monitor()` in a retry loop with exponential backoff. Sourced by `agent.sh`. |
| `lib/config_defaults.sh` | Default values and hard upper-bound clamps for all pipeline config keys. Sourced by `config.sh` at the end of `load_config()`. Extracted to keep `config.sh` under the 300-line ceiling. |
| `lib/context_budget.sh` | Context budget enforcement (Milestone 2). Sourced by `context_compiler.sh` — do not run directly. |
| `lib/drift_artifacts.sh` | Architecture Decision Log, Human Action, and post-pipeline drift processing. Extracted from `drift.sh` for size management. Sourced by `tekhton.sh`. |
| `lib/drift_cleanup.sh` | Non-blocking notes management and drift cleanup helpers. Extracted from `drift.sh` for size management. Sourced by `tekhton.sh` after `drift.sh`. |
| `lib/errors_helpers.sh` | Recovery suggestions and sensitive data redaction. Sourced by `errors.sh` — do not run directly. |
| `lib/milestone_ops.sh` | Milestone acceptance checking, commit signatures, and auto-advance orchestration. Sourced by `tekhton.sh`. |
| `lib/milestone_split.sh` | Pre-flight milestone sizing and null-run auto-split (Milestone 11). Sourced by `tekhton.sh`. |

### SF-2: `lib/agent.sh` bullet description omits `agent_monitor_helpers.sh`

**File:** `ARCHITECTURE.md`, Layer 3 library section, `lib/agent.sh` entry.

**Problem:** The current text reads:
> Sources `agent_monitor_platform.sh`, `agent_monitor.sh`, `agent_retry.sh`, and `agent_helpers.sh`.

The actual `source` calls in `agent.sh` (lines 12–28) include `agent_monitor_helpers.sh`
between `agent_monitor.sh` and `agent_retry.sh`. The description omits it.

**Action:** Update the `lib/agent.sh` bullet in the architecture to:
> Sources `agent_monitor_platform.sh`, `agent_monitor.sh`, `agent_monitor_helpers.sh`, `agent_retry.sh`, and `agent_helpers.sh`.

---

## Dead Code Removal

None.

---

## Naming Normalization

None.

---

## Simplification

None.

---

## Design Doc Observations

None.

---

## Drift Observations to Resolve

All 10 observations are resolved (stale, informational, or already addressed in code).
The staleness fix above (SF-1, SF-2) addresses the gap these observations revealed —
the architecture document had not been updated to reflect extracted modules.

| # | Observation | Disposition |
|---|-------------|-------------|
| 1 | agent_monitor.sh header dual-touch (Milestone 14) | **Resolved — informational.** The observation itself states "No conflict, final state is correct." No action needed. |
| 2 | Empty `--` separator | **Resolved — not actionable.** Formatting artifact. |
| 3 | `stages/tester.sh:101` — grep\|tee duplicate-entry risk | **Resolved — stale.** Current tester.sh:101 is `_failed_paths=$(grep ... "$LOG_FILE" | sed ...)`. The result is captured to a variable, not piped back into LOG_FILE. No duplicate-write risk exists. |
| 4 | `lib/config.sh` is 342 lines | **Resolved — stale.** config.sh is 170 lines. The `config_defaults.sh` extraction (pattern from ADL-5) was completed; the line-count alarm no longer applies. |
| 5 | Empty `--` separator | **Resolved — not actionable.** Formatting artifact. |
| 6 | `lib/agent_monitor.sh` well over 300 lines | **Resolved — stale.** agent_monitor.sh is 286 lines, under the 300-line ceiling. |
| 7 | Empty `--` separator | **Resolved — not actionable.** Formatting artifact. |
| 8 | `lib/agent_monitor.sh:211` — dual kill path (subshell + outer trap) | **Resolved — already commented.** Line 165 of agent_monitor.sh contains: "This subshell cannot reach the outer `_run_agent_abort` trap — kill directly." The architectural intent is documented inline. |
| 9 | `lib/common.sh:61-65` — `_print_box_line` fallback is bare `echo "${_bv}"` | **Resolved — stale.** The current fallback is `echo "${_bv}  ${_content}  ${_bv}"` (content path) and `echo "${_bv} [60 spaces] ${_bv}"` (empty path). Both include the right border `${_bv}`. The observation's description of "no right border" does not match the code. |
| 10 | `lib/common.sh:77-86` vs `lib/common.sh:132-141` — `_box_line` / `_rbox_line` duplicates | **Resolved — stale.** These functions do not exist in the current common.sh. The file has been restructured; the current shared helpers are `_print_box_line` and `_build_box_hline`, with no duplication. |

---

## Out of Scope

All 10 drift observations are resolved above. No items require escalation to human action.
