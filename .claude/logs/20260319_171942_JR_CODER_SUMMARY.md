# Junior Coder Summary

## What Was Fixed

- **tekhton.sh:530** — Fixed `--usage-threshold` flag to follow the established codebase pattern for value-taking flags. Changed from `USAGE_THRESHOLD_PCT="$2"; shift 2` (which directly accesses `$2` before validation) to the standard pattern: `shift` first, then access `$1`, then `shift` again. This prevents crashes with `set -euo pipefail` when the flag is passed without a value.

## Files Modified

- `tekhton.sh` (line 530-534)

## Validation

- ✓ `bash -n` passed
- ✓ No new shellcheck warnings introduced
