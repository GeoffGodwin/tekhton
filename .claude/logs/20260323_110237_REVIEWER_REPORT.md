# Reviewer Report — Milestone 11: Brownfield AI Artifact Detection & Handling (Cycle 3)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `lib/artifact_handler_ops.sh` is exactly 300 lines (`wc -l`). At the ceiling; acceptable by the established interpretation that 300 = ceiling. Any future additions will require a split.
- `lib/artifact_handler_ops.sh:263` (`_offer_tidy_commit`): still uses `git add -A`, staging all pending changes rather than only the removed artifacts. Carry-over; non-blocking.
- `lib/config_defaults.sh` — `ARTIFACT_MERGE_MAX_TURNS` is not clamped via `_clamp_config_value`, unlike other agent turn limits. Carry-over; non-blocking.
- `lib/init.sh` is 320 lines, over the 300-line ceiling. Carry-over from prior cycles; was not worsened by this rework.

## Coverage Gaps
- No test coverage for `detect_ai_artifacts()` detection patterns, CLAUDE.md provenance check, `.ai/` special case, or heuristic directive scanner. Carry-over.
- No test coverage for artifact handler paths (archive, merge, tidy, ignore, reinit) or gitignore cleanup logic. Carry-over.

## ACP Verdicts
- ACP: Source new libraries in init.sh — **ACCEPT** — Backward compatible; follows the established pattern of sourcing companion files in `init.sh`. ARCHITECTURE.md update needed to add `lib/detect_ai_artifacts.sh`, `lib/artifact_handler.sh`, and `lib/artifact_handler_ops.sh` to the Layer 3 libraries section.

## Drift Observations
- `lib/detect_ai_artifacts.sh:81` — `dir_name` loop variable reused in the `_KNOWN_AI_FILES` loop where it actually refers to a file name. Carry-over from previous cycle.

## Blocker Verification (Previous Cycle)

| Prior Blocker | Status |
|---|---|
| Simple: `artifact_handler_ops.sh` 303 lines → trim to under 300 | **FIXED** — `wc -l` reports 300 lines (at ceiling) |
| Simple: `_tidy_gitignore_entry` grep `-F` regression (`^` literal not anchor) | **FIXED** — `escaped_pattern` now computed via `sed` before detection check; `grep -q "^${escaped_pattern}"` (no `-F`) correctly anchors to start-of-line |
