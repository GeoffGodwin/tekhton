# Scout Report: Milestone 1 — Milestone DAG Infrastructure

## Relevant Files

- `lib/milestones.sh` — Currently parses milestones from CLAUDE.md; needs `parse_milestones_auto()` dual-path wrapper
- `lib/milestone_ops.sh` — Contains `find_next_milestone()` and `mark_milestone_done()` that must support DAG mode
- `lib/milestone_archival.sh` — Currently archives milestones from CLAUDE.md; must adapt for file-based milestones
- `lib/milestone_archival_helpers.sh` — Provides `_extract_milestone_block()` helper that will be reused by migration
- `lib/milestone_split.sh` — Splits milestones in CLAUDE.md; must work with file-based milestones
- `lib/milestone_metadata.sh` — Writes metadata to CLAUDE.md; must adapt to write to individual milestone files
- `lib/config_defaults.sh` — Must add new config keys: `MILESTONE_DAG_ENABLED`, `MILESTONE_DIR`, `MILESTONE_MANIFEST`, `MILESTONE_AUTO_MIGRATE`, `MILESTONE_WINDOW_PCT`, `MILESTONE_WINDOW_MAX_CHARS`
- `tekhton.sh` — Entry point; must source new `milestone_dag.sh` and `milestone_dag_migrate.sh`, trigger auto-migration
- `templates/pipeline.conf.example` — Must document new milestone DAG config section

## Key Symbols

- `parse_milestones()` / `parse_milestones_auto()` — `lib/milestones.sh`
- `find_next_milestone()` — `lib/milestone_ops.sh`
- `mark_milestone_done()` — `lib/milestone_ops.sh`
- `_extract_milestone_block()` — `lib/milestone_archival_helpers.sh` (reused by migration)
- `_replace_milestone_block()` — `lib/milestone_archival_helpers.sh` (reused by migration)
- `load_manifest()` / `save_manifest()` — `lib/milestone_dag.sh` (NEW)
- `dag_get_frontier()` / `dag_deps_satisfied()` / `dag_find_next()` — `lib/milestone_dag.sh` (NEW)
- `validate_manifest()` — `lib/milestone_dag.sh` (NEW with cycle detection)
- `dag_id_to_number()` / `dag_number_to_id()` — `lib/milestone_dag.sh` (NEW)
- `migrate_inline_milestones()` — `lib/milestone_dag_migrate.sh` (NEW)

## Suspected Root Cause Areas

- `lib/milestones.sh` — `parse_milestones()` reads CLAUDE.md directly; needs dual-path wrapper that detects manifest and calls DAG loader
- `lib/milestone_ops.sh` — `find_next_milestone()` iterates all milestones from `parse_milestones()`; must use DAG queries when manifest present
- `lib/milestone_ops.sh` — `mark_milestone_done()` modifies CLAUDE.md via sed; must update manifest status when DAG enabled
- `lib/milestone_split.sh` — Uses `_extract_milestone_block()` and `_replace_milestone_block()` to modify CLAUDE.md; must create/insert milestone files + manifest rows instead
- `lib/milestone_archival.sh` — Extracts blocks from CLAUDE.md; must read from milestone files when DAG enabled
- `tekhton.sh` — Currently sources milestones after config; must add auto-migration early in startup sequence
- `lib/config_defaults.sh` — Missing new config keys for DAG mode; must add with sane defaults

## Complexity Estimate

Files to modify: 8
Estimated lines of change: 750
Interconnected systems: high
Recommended coder turns: 85
Recommended reviewer turns: 13
Recommended tester turns: 45
