# ARCHITECT_PLAN.md
## Audit Date: 2026-03-19
## Task: Post-Milestone 14 Architecture Audit (cleanup/NonBlockers branch)

---

## Simplification

None.

---

## Staleness Fixes

None.

---

## Dead Code Removal

None.

---

## Naming Normalization

None.

---

## Design Doc Observations

None.

---

## Drift Observations to Resolve

All 10 drift log entries were verified against the current codebase. All are
stale or self-described as non-issues. No coder action is required. Separator
entries (marked -- in the drift log) are blank formatting artifacts.

**OBS-1: agent_monitor.sh header touched by both Sr/Jr Coder**
Self-described in the drift log as expected coordination, not a process concern.
Resolution: Mark RESOLVED.

**OBS-3: stages/tester.sh:101 -- grep | tee -a LOG_FILE may duplicate log entries**
Verified: no tee -a pattern exists in stages/tester.sh. Current line 101 is
`local resume_flag`. The tee -a "$log_file" pattern lives in lib/agent_monitor.sh
line 264 (fallback path), where it already carries an explicit WARNING comment.
Observation was misattributed to tester.sh.
Resolution: Mark RESOLVED. Stale misattribution; underlying concern in
agent_monitor.sh fallback is pre-existing and documented.

**OBS-4: lib/config.sh is 342 lines, exceeds 300-line ceiling**
Verified: current lib/config.sh is 171 lines. lib/config_defaults.sh extraction
already occurred (confirmed at line 143 source call).
Resolution: Mark RESOLVED.

**OBS-6: lib/agent_monitor.sh remains well over 300 lines**
Verified: current lib/agent_monitor.sh is 287 lines, under the 300-line ceiling.
Prior extractions of agent_monitor_platform.sh and agent_monitor_helpers.sh
resolved this.
Resolution: Mark RESOLVED.

**OBS-8: lib/agent_monitor.sh inner kill lacks comment explaining why it is necessary**
Verified: line 165 already carries the explanatory comment: "This subshell cannot
reach the outer _run_agent_abort trap -- kill directly."
Resolution: Mark RESOLVED. Comment added in prior remediation.

**OBS-9: lib/common.sh:61-65 -- empty-line fallback missing right border**
Verified: both the empty-content fallback (line 65) and the content fallback
(line 62) include the right border character. Neither matches the bare echo
description in the observation.
Resolution: Mark RESOLVED. Observation does not match current code.

**OBS-10: lib/common.sh -- _box_line and _rbox_line are identical nested functions**
Verified: neither _box_line nor _rbox_line exists in lib/common.sh. Both were
superseded by the unified _print_box_line + _setup_box_chars pattern shared by
report_error() and report_retry().
Resolution: Mark RESOLVED. Functions no longer present.

---

## Out of Scope

OBS-2, OBS-5, OBS-7: Blank -- separator entries in the drift log. Formatting
artifacts with no associated defect or observation.

All remediation sections are empty this cycle. Every actionable drift observation
was verified stale against the current codebase. Milestone 14 work and prior
extractions in Milestones 12-13 resolved all flagged structural concerns. No
speculative audit was performed beyond the 10 documented observations.
