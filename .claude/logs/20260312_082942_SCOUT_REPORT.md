## Relevant Files

- **prompts/plan_generate.prompt.md** — Current generation prompt with 6 required sections. Must be rewritten to mandate 12 sections (Project Identity, Architecture Philosophy, Repository Layout, Key Design Decisions, Config Architecture, Non-Negotiable Rules, Implementation Milestones with Seeds Forward/Watch For blocks, Code Conventions, Critical System Rules, What Not to Build Yet, Testing Strategy, Development Environment).

- **stages/plan_generate.sh** — Generation stage implementation. Lines 48-49 log the current max turns; line 50 uses PLAN_GENERATION_MAX_TURNS. Must verify integration with config defaults.

- **lib/plan.sh** — Planning orchestration library. Line 42 sets `PLAN_GENERATION_MAX_TURNS` default to 30. Must change to 50 to match Milestone 3 requirement.

- **lib/prompts.sh** — Prompt template rendering engine (no changes needed). Used by run_plan_generate() to substitute {{VAR}} placeholders and {{IF:VAR}}...{{ENDIF:VAR}} conditionals.

- **lib/agent.sh** — Agent wrapper for batch invocation via run_agent(). Not directly used by planning phase (which uses _call_planning_batch() instead). Background reference only.

- **templates/plans/web-game.md** — Reference example of deep design doc structure with PHASE markers. Not modified in Milestone 3, but exemplifies the depth and section count the new CLAUDE.md prompt should demand.

- **CLAUDE.md** — Project instructions document. Line 39-42 shows current config defaults (defaults sonnet/30 turns). Will need updating after implementation to document new opus/50 defaults.

## Key Symbols

- `run_plan_generate()` — stages/plan_generate.sh:24. Main generation function; reads DESIGN.md, renders prompt, calls _call_planning_batch(), writes CLAUDE.md to disk.

- `PLAN_GENERATION_MODEL` — lib/plan.sh:41. Environment variable for model choice. Defaults to `${CLAUDE_PLAN_MODEL:-opus}` (already opus per Milestone 1).

- `PLAN_GENERATION_MAX_TURNS` — lib/plan.sh:42. Environment variable for turn limit. Currently defaults to 30; must become 50.

- `render_prompt()` — lib/prompts.sh. Substitutes {{VAR}} and processes {{IF:VAR}}...{{ENDIF:VAR}} blocks in prompt templates.

- `_call_planning_batch()` — lib/plan.sh:135. Invokes claude CLI in batch mode with `--output-format text` for planning agents. No --dangerously-skip-permissions used.

- `DESIGN_CONTENT` — Variable exported by run_plan_generate():41 before rendering prompt. Holds full text of DESIGN.md for template substitution.

## Suspected Root Cause Areas

- **prompts/plan_generate.prompt.md (lines 20-70)** — Current "Required Sections" list mandates only 6 sections; must be expanded to 12 with detailed guidance on milestone format (Seeds Forward, Watch For blocks), config examples, and architectural depth.

- **prompts/plan_generate.prompt.md (lines 45-54)** — Milestone section guidance is incomplete; must specify that each milestone includes Seeds Forward and Watch For blocks derived from DESIGN.md.

- **prompts/plan_generate.prompt.md (lines 33-39)** — Non-Negotiable Rules guidance says 5-10 rules; must be increased to 10-20 per Milestone 3 acceptance criteria.

- **lib/plan.sh (line 42)** — Turn limit default is 30; must be raised to 50 to give generation agent capacity for deeper output.

## Complexity Estimate

**Files to modify:** 3

**Estimated lines of change:** 250-300
- prompts/plan_generate.prompt.md: ~200 lines added (expand 94-line prompt to ~280-300 lines with new sections)
- stages/plan_generate.sh: 0 lines changed (logs reference PLAN_GENERATION_MAX_TURNS which is correctly used)
- lib/plan.sh: 1 line changed (line 42: default 30 → 50)

**Interconnected systems:** medium
- Prompt templating (lib/prompts.sh) — not modified but must work correctly
- Planning orchestration (lib/plan.sh, run_plan() function) — calls run_plan_generate() after completeness check
- Planning CLI (tekhton.sh --plan flow) — invokes planning phase but doesn't directly touch generation

**Recommended coder turns:** 25-35
- Prompt rewrite is iterative; coder may need multiple passes to hit the 12-section depth and get the milestone format (Seeds Forward, Watch For) clear enough for generation to succeed

**Recommended reviewer turns:** 6-10
- Reviewer should validate: all 12 sections present, milestone format includes required blocks, Non-Negotiable Rules count (10-20), config example guidance is clear

**Recommended tester turns:** 20-30
- Tester should run `--plan` flow end-to-end with various project types, verify CLAUDE.md output includes all 12 sections, spot-check milestone Seeds Forward/Watch For blocks are populated, verify config examples match DESIGN.md values
