# Reviewer Report — Milestone 18: Project Crawler & Index Generator (Re-review Cycle 2)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `crawler_content.sh:82` — `local -n` (nameref) requires Bash 4.3+, not Bash 4.0. CLAUDE.md specifies "Bash 4+" without a minor version. In practice Bash 4.3+ is ubiquitous, but worth documenting in the file header. (Carry-over from prior review — not introduced by rework.)
- `tekhton.sh:313` — Comment still says "also sources crawler_inventory.sh, crawler_content.sh" but the full transitive chain is crawler_content.sh → crawler_deps.sh. Should read "also sources crawler_inventory.sh, crawler_content.sh, crawler_deps.sh (via crawler_content.sh)". (Carry-over from prior review.)
- `crawler_content.sh:98` — `grep -qP '\x00'` requires PCRE (unavailable on macOS BSD grep). Failure is suppressed with `2>/dev/null` and the extension-check fallback handles it gracefully. Consider `LC_ALL=C grep -qc $'\x00'` for portable null-byte detection. (Carry-over from prior review.)

## Coverage Gaps
- No test cases in `tests/` for any crawler functions. The operator precedence bugs fixed in this cycle would have been caught by minimal unit tests for the pytest/coverage detection paths.
- Budget enforcement is verified manually but not by any automated assertion in the test suite.

## ACP Verdicts
- ACP: Crawler Companion File Architecture — ACCEPT — adding `lib/crawler.sh`, `lib/crawler_inventory.sh`, `lib/crawler_content.sh`, `lib/crawler_deps.sh` to ARCHITECTURE.md Layer 3 is a purely additive and accurate documentation update. No code changes required.

## Drift Observations
- `crawler_inventory.sh:173` uses `grep -oE '^[^/]/'` to find top-level test directories, then `grep -c "^${d}"` to count files in them. The `grep -c` pattern is duplicated from similar patterns in `_crawl_test_structure` and the inventory function — a shared `_count_files_in_dir(file_list, prefix)` helper would reduce duplication across future milestone work. (Carry-over from prior review.)
- `crawler_deps.sh` has an implicit runtime dependency on `_extract_json_keys` (defined in `detect.sh`). This is documented in the file header but not validated at source time. Consistent with how other companion files handle cross-library deps in this codebase. (Carry-over from prior review.)

## Blocker Verification

All three simple blockers from the previous review are confirmed fixed:

1. **pytest detection (`crawler_inventory.sh:207-211`)** — FIXED. Operator precedence bug resolved by converting to `if [[ -f "${project_dir}/pytest.ini" ]] || [[ -f "${project_dir}/conftest.py" ]] || grep -q 'pytest' ...; then ... fi`. All three conditions now evaluate correctly.

2. **NYC/Istanbul coverage (`crawler_inventory.sh:224-227`)** — FIXED. Converted to `if [[ -f "${project_dir}/.nycrc" ]] || [[ -f "${project_dir}/.nycrc.json" ]]; then ... fi`. Both files now correctly trigger the output.

3. **Python coverage (`crawler_inventory.sh:228-231`)** — FIXED. Converted to `if [[ -f "${project_dir}/.coveragerc" ]] || [[ -f "${project_dir}/coverage.xml" ]]; then ... fi`. Both files now correctly trigger the output.

No regressions introduced by the rework.
