# Scout Report

## Relevant Files
- `stages/cleanup.sh` — Contains two issues: (1) unquoted regex pattern in grep call at line 270, (2) undocumented pre-cleanup snapshot at line 86
- `lib/notes.sh` — Mark_note_resolved and mark_note_deferred functions that need consistency review with cleanup.sh grep pattern

## Key Symbols
- `run_stage_cleanup` — stages/cleanup.sh:39
- `_resolve_cleanup_by_file_changes` — stages/cleanup.sh:257 (iterates over modified files, checks if any match cleanup notes)
- `mark_note_resolved` — lib/notes.sh:255 (marks items [x] in NON_BLOCKING_LOG.md)
- `mark_note_deferred` — lib/notes.sh:279 (marks items [DEFERRED] in NON_BLOCKING_LOG.md)

## Suspected Root Cause Areas
- **stages/cleanup.sh:270** — `grep -q "$basename_mod"` uses unquoted variable that could contain regex metacharacters (e.g., filenames with dots like `foo.sh`). Should use `grep -qF` for literal string matching.
- **stages/cleanup.sh:86** — Comment at lines 83-84 exists but is generic. The specific rationale (snapshotting to preserve primary pipeline's uncommitted changes and only revert cleanup-introduced changes) needs explicit inline documentation on line 86 itself for clarity.

## Complexity Estimate
Files to modify: 1
Estimated lines of change: 3
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 6
Recommended tester turns: 15
