# Scout Report: Resolve NON_BLOCKING_LOG.md Observations

## Task
Resolve all 20 open observations in NON_BLOCKING_LOG.md by applying fixes and marking them resolved.

## Relevant Files

### Core Files Needing Changes
- **lib/metrics.sh** — Pre-existing size violation (~395 lines); extraction to `metrics_dashboard.sh` already noted in header. Verify complete.
- **lib/notes.sh** — Three observations: unused `_escape_sed_pattern` needs intent comment; unreachable fallback if blocks (~8 lines dead code); file size 547 lines over guideline.
- **lib/milestone_archival.sh** — File at 383 lines (28% over 300-line guideline); AWK block computes `safe_num` on every line instead of BEGIN block.
- **lib/milestone_ops.sh** — Header comment mentions `milestone_acceptance.sh` as "expected to be sourced first" but now sources it itself; needs comment fix. Missing `PROJECT_RULES_FILE` awareness in `check_milestone_acceptance()` and `get_milestone_commit_body()`.
- **tekhton.sh** — Lines 152-153 initialize `HUMAN_MODE` and `HUMAN_NOTES_TAG` but don't export them; export only happens inside `--human` branch (line 540). Harmless in current call paths but breaks subprocess contexts.
- **lib/hooks.sh** — Lines 110, 113: `grep -qi "fix|bug"` and `grep -qi "^n/a|^none|^(fill"` use BRE `|` (GNU extension). Should use `-Eqi` for POSIX ERE portability on BSD grep.
- **lib/orchestrate_helpers.sh** — Line 44: `run_complete_loop` recursive call in `_run_auto_advance_chain()` bounded only by `AUTO_ADVANCE_LIMIT`. Comment documenting recursion depth bound would prevent future confusion.
- **stages/coder.sh** — Line 322: `build_context_packet()` called before `should_claim_notes` gate (327-332). Subtly coupled ordering; comment explaining the safe pattern would help.

### Comment/Documentation Fixes
- **lib/drift_cleanup.sh** — Lines 253, 262: Edge case of blank lines between resolved items in rewrite is cosmetic only; optional comment.
- **lib/config_defaults.sh** — Line 129: `AUTO_COMMIT=true` unconditional default; M15.1 spec requires it conditional (true in milestone mode, false otherwise). Comment marking for future M15.1 update.
- **lib/common.sh** — Line 187: `check_usage_threshold()` uses `grep -oE '[0-9]+(.[0-9]+)?%' | head -1` to extract usage. If format changes to include multiple percentages, `head -1` may capture wrong value. Comment explaining expected percentage would help maintainers.
- **tekhton.sh** — Indirectly: CODER_SUMMARY.md line 15 observation notes that "Files Modified" lists only `lib/milestone_ops.sh` but omits newly created `lib/milestone_acceptance.sh`. Documentation/workflow issue, not code.

### Test Files
- **tests/test_human_notes_lifecycle.sh** — Header comment (lines 8–13) lists 7 test groups but Phases 10–11 (exit-code awareness tests) not reflected in summary. Update to include.
- **tests/test_auto_commit_conditional_default.sh** — Line 103: Dead `reload_defaults` call (contradicts comment "should NOT override") because `reload_defaults` unsets `AUTO_COMMIT` first. Remove dead line; the subsequent `AUTO_COMMIT=false` + `source config_defaults.sh` is the correct test.

### Minor Code Quality
- **lib/notes.sh** — Line 26: Pattern `'human.?notes|HUMAN_NOTES'` redundant with `-i` (case-insensitive) since `human.?notes` already matches case-insensitively. Slightly noisy; can be simplified.

## Key Symbols

### Functions Needing Changes
- `_escape_sed_pattern()` in lib/notes.sh — Unused; needs intent comment or mark for future use
- `_run_auto_advance_chain()` in lib/orchestrate_helpers.sh (line 12) — Recursive; needs recursion depth comment
- `build_context_packet()` in lib/context_compiler.sh (called from stages/coder.sh:322) — Current ordering is safe but subtly coupled
- `check_milestone_acceptance()` in lib/milestone_acceptance.sh — Missing `PROJECT_RULES_FILE` param backfill
- `get_milestone_commit_body()` in lib/milestone_ops.sh — Missing `PROJECT_RULES_FILE` param backfill
- `mark_milestone_done()` in lib/milestone_ops.sh (contrast: uses `${2:-${PROJECT_RULES_FILE:-CLAUDE.md}}`)

### Configuration
- `AUTO_COMMIT` in lib/config_defaults.sh (line 129) — Currently unconditional `true`; should be conditional per M15.1
- `HUMAN_MODE` and `HUMAN_NOTES_TAG` in tekhton.sh (lines 152–153) — Not exported at initialization

## Suspected Root Cause Areas

1. **lib/metrics.sh (lines 1–20)** — Header comments indicate extraction to `metrics_dashboard.sh` already done; verify metrics.sh is now <300 lines or identify remaining functions to extract.

2. **lib/notes.sh (lines 10, 351–356, 26, entire file)** — Dead code (fallback if blocks in `extract_note_text`), unused function (`_escape_sed_pattern`), redundant pattern literal, file size concern (547 lines).

3. **lib/milestone_archival.sh (lines 70–100 estimated)** — AWK block recomputes `safe_num` on every line; move to BEGIN block for efficiency. File size at 383 lines (pre-existing violation).

4. **lib/milestone_ops.sh (lines 1–50)** — Header comment wording inaccurate; functions `check_milestone_acceptance()` and `get_milestone_commit_body()` lack `PROJECT_RULES_FILE` awareness that `mark_milestone_done()` has.

5. **lib/hooks.sh (lines 110, 113)** — Grep uses BRE `|` instead of ERE for portability across GNU and BSD grep.

6. **tekhton.sh (lines 152–153, 540)** — Variables initialized without export; export only happens inside conditional branch.

7. **stages/coder.sh (lines 322, 327–332)** — Ordering of `build_context_packet()` before `should_claim_notes` gate is safe but worth documenting.

8. **lib/orchestrate_helpers.sh (line 44)** — Recursive `run_complete_loop()` call needs recursion depth bound comment.

9. **tests/test_auto_commit_conditional_default.sh (line 103)** — Dead `reload_defaults` call contradicts its own comment; remove.

10. **lib/config_defaults.sh (line 129)** — `AUTO_COMMIT` unconditional; M15.1 will change this to conditional.

## Complexity Estimate

Files to modify: 13
Estimated lines of change: 180
Interconnected systems: medium
Recommended coder turns: 45
Recommended reviewer turns: 8
Recommended tester turns: 20
