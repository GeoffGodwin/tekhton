# SCOUT_REPORT.md — Milestone 18: Documentation Site

## Relevant Files

- `mkdocs.yml` — MkDocs configuration with Material theme, navigation structure, markdown extensions, and GitHub Pages site URL
- `.github/workflows/docs.yml` — GitHub Actions workflow for automatic deployment on push to main (only when docs/ or mkdocs.yml change)
- `tekhton.sh` — Already implements `--docs` flag (lines 209-219) that opens documentation URL in default browser
- `.gitignore` — Already contains `site/` entry (line 58) for MkDocs build output directory
- `docs/requirements.txt` — Pinned dependency: `mkdocs-material>=9.5.0`
- `tests/test_docs_site.sh` — Comprehensive test suite verifying: required files exist, mkdocs.yml structure, theme config, navigation coverage, Material markdown extensions, GitHub Actions workflow setup, .gitignore, --docs flag, and each doc file has top-level heading
- `docs/index.md` — Landing page with "One intent. Many hands." tagline, quick start, feature highlights, audience statement
- `docs/getting-started/*.md` (4 files) — Installation, first project setup, first milestone walkthrough, output understanding
- `docs/guides/*.md` (6 files) — Greenfield/brownfield/monorepo guides, security config, Watchtower dashboard, planning phase
- `docs/reference/*.md` (5 files) — Commands, configuration, pipeline stages, agent roles, template variables
- `docs/concepts/*.md` (4 files) — Pipeline flow, milestone DAG, health scoring, context budget
- `docs/troubleshooting/*.md` (3 files) — --diagnose guide, common errors, FAQ
- `docs/changelog.md` — Version history with 3.18.0 release notes
- `.claude/milestones/m18-documentation-site.md` — Milestone specification and acceptance criteria

## Key Symbols

- `run_agent()` — lib/agent.sh, invokes Claude agents with JSON parsing
- `render_prompt()` — lib/prompts.sh, substitutes {{VAR}} in prompt templates
- `load_config()` — lib/config.sh, loads and validates pipeline.conf
- GitHub Pages Site URL — `https://geoffgodwin.github.io/tekhton/` (from mkdocs.yml)
- MkDocs Material theme — `name: material` with dual dark/light palette and navigation features
- Deployment action — `actions/deploy-pages@v4` with GitHub Pages artifact upload

## Suspected Root Cause Areas

The milestone is marked as `in_progress`, suggesting core implementation is complete but verification is pending. Likely remaining work:

1. **Test Suite Validation** — `tests/test_docs_site.sh` performs 40+ checks on file presence, mkdocs.yml structure, workflow setup, .gitignore, tekhton.sh flags, and heading requirements. Any failures would reveal missing content or configuration errors.

2. **Content Completeness** — While all 25 doc files exist and have top-level headings, spot-checks confirm content is present but depth/coverage against milestone spec should be verified (e.g., platform-specific install notes for macOS/Linux/Windows, screenshot assets for Watchtower guide).

3. **Configuration Reference Exhaustiveness** — Milestone spec notes this is "the most maintenance-heavy page." Current docs/reference/configuration.md should be cross-checked against lib/config_defaults.sh to ensure every config key is documented.

4. **GitHub Actions Permissions** — Workflow requires `pages: write` and `id-token: write` permissions. These must be correctly set in .github/workflows/docs.yml for deployment to work.

5. **Version Bump** — TEKHTON_VERSION in tekhton.sh is currently 3.6.0 but should be bumped to 3.18.0 after milestone completion (per Tekhton versioning rules: MAJOR.MINOR.PATCH, MINOR = last completed milestone number).

## Complexity Estimate

Files to modify: 1
Estimated lines of change: 5
Interconnected systems: low
Recommended coder turns: 20
Recommended reviewer turns: 5
Recommended tester turns: 20
