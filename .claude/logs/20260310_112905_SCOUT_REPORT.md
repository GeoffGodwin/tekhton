# Scout Report — Fix Bugs Found in TESTER_REPORT.md

## Relevant Files

- `stages/plan_interview.sh` — Missing `set -euo pipefail` header; `wc -l` output has unstripped whitespace at lines 91 and 102; missing user warning for `--dangerously-skip-permissions`
- `lib/plan.sh` — Missing `set -euo pipefail` header
- `prompts/plan_interview.prompt.md` — Implicit CWD coupling not documented; needs clarification on file path behavior
- `tests/test_plan_interview_stage.sh` — PATH export bug already fixed (line 51 has correct `\"${mock_bin}:\${PATH}\"` with double quotes for variable expansion)
- `tekhton.sh` — Line 175 already fixed (`run_plan || true`); no changes needed

## Key Symbols

- `run_plan_interview()` — stages/plan_interview.sh:25
- `select_project_type()` — lib/plan.sh:48
- `run_plan()` — lib/plan.sh:91
- `wc -l < "${PROJECT_DIR}/DESIGN.md"` — stages/plan_interview.sh:91, 102

## Suspected Root Cause Areas

1. **Missing `set -euo pipefail` headers in `stages/plan_interview.sh` and `lib/plan.sh`** — Non-negotiable per CLAUDE.md rule #2. Currently inherited from tekhton.sh at runtime, but files sourced independently (e.g., during tests) lack safety guarantees. Flagged in reviewer cycle 2 as "for Milestone 7 cleanup."

2. **Whitespace in `wc -l` output at lines 91 and 102 in `stages/plan_interview.sh`** — Platform-dependent leading spaces in line count output. Should use `$(wc -l < "$file" | tr -d ' ')` for strict portability. Flagged as cosmetic/low-priority but affects user-visible log and error messages.

3. **Missing user warning before `--dangerously-skip-permissions` session** — Users should be explicitly informed before the interview agent gains write access. Simple one-line `warn()` call at start of `run_plan_interview()` needed. Flagged in reviewer cycle 2 as "good UX practice."

4. **Implicit CWD coupling in prompt and code** — `prompts/plan_interview.prompt.md` line 52 instructs agent to write to "current working directory"; `stages/plan_interview.sh` does not `cd` or document CWD assumptions. Reviewer flagged this as important documentation before Milestone 6 state-resume work introduces directory management. Add a comment in both files noting the CWD dependency.

## Complexity Estimate

**Files to modify:** 3 (stages/plan_interview.sh, lib/plan.sh, prompts/plan_interview.prompt.md)

**Estimated lines of change:** ~10
- 2 lines: add `set -euo pipefail` to two .sh files
- 2 lines: strip whitespace from two `wc -l` calls via piped `tr -d ' '`
- 1 line: add user `warn()` message before claude invocation
- 5 lines: add clarifying comments re: CWD coupling in two files and prompt template

**Interconnected systems:** low (planning phase only; no changes to execution pipeline)

**Recommended coder turns:** 10–15 (straightforward edits)

**Recommended reviewer turns:** 3–5 (sanity check on header additions, whitespace stripping logic)

**Recommended tester turns:** 10–15 (re-run planning tests, manual verification of warning message timing)
