# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [ ] [2026-04-08 | "[BUG] When `tekhton --plan --answers FILE` is run the answers get overwritten with a blank template while loading, erasing all the answers before they can be used. The template loading logic needs to be fixed to preserve existing answers instead of overwriting them with a blank template. This is critical to ensure that the answers generated by the plan command are not lost and can be properly utilized in subsequent steps. Review the code responsible for loading the answer template and make necessary adjustments to prevent this data loss issue. "] `stages/plan_interview.sh` is 468 lines, already over the 300-line ceiling before this fix. The 13 lines added here are justified, but the file warrants a future split (e.g., extracting `_run_cli_interview` and helpers into a `plan_interview_cli.sh` sub-stage).
- [ ] [2026-04-07 | "[BUG] The answer template file generated by `tekhton --plan` generates some lines with bad escapes where double quotes are used within the template. This causes the generated prompt files to have broken markdown formatting. The template needs to be fixed to properly escape double quotes. Check and ensure that this does not happen with the web mode of the `tekhton --plan` command as well."] `lib/plan_answers.sh` is 515 lines, well over the 300-line soft ceiling. Pre-existing condition worsened by +26 lines for the new helpers. Consider splitting `_yaml_escape_dq`/`_yaml_unescape_dq` and related helpers into a `plan_answers_helpers.sh` in a future cleanup pass.
- [ ] [2026-04-07 | "[BUG] The answer template file generated by `tekhton --plan` generates some lines with bad escapes where double quotes are used within the template. This causes the generated prompt files to have broken markdown formatting. The template needs to be fixed to properly escape double quotes. Check and ensure that this does not happen with the web mode of the `tekhton --plan` command as well."] In `export_question_template`, `s_guide` is written as a YAML comment (`# Guidance: ${s_guide}`) without HTML or YAML escaping. If guidance text contains a newline it would break the comment line, though this hasn't occurred in practice. Informational only.

## Resolved
