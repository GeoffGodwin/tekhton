# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [ ] [2026-03-20 | "Fix Milestone 17 TESTER_REPORT bugs — see TESTER_REPORT.md ## Bugs Found"] `lib/metrics_dashboard.sh` is missing `set -euo pipefail` — consistent with `lib/metrics.sh` and `lib/metrics_calibration.sh` (the prior extraction from the same family). All three metrics sourced-library files share this gap. Fix all three together rather than patching only the new file.
- [ ] [2026-03-20 | "Fix Milestone 17 TESTER_REPORT bugs — see TESTER_REPORT.md ## Bugs Found"] `test_dynamic_turn_limits.sh` was correctly not updated — it only calls `record_run_metrics()` and `_extract_stage_turns()` which remained in `metrics.sh`. Scout report was overcautious; coder made the right call.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `_find_source_files` uses `git ls-files` in git repos (returns all tracked files at all depths) while the non-git fallback uses `find -maxdepth 2`. The spec and the `_count_source_files` docstring both say "top 2 levels" — the git path over-counts source files in deep repos, inflating confidence. Impact is minor (over-counting → higher confidence, which is usually correct direction) but worth aligning.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `_DETECT_EXCLUDE_DIRS` contains literal dots (`.git`, `.dart_tool`, `.next`, etc.) used directly in `grep -Ev` ERE patterns without escaping. A dot in ERE matches any character, so `.git` would also match `XgitY`-style directory names. In practice harmless, but a pedantic shellcheck / reviewer would flag it.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `detect_frameworks` labels Node.js frameworks (react, next.js, vue, angular, svelte, express, fastify) with `typescript` or `javascript` lang based on framework convention, not on what the detection engine actually found. A TypeScript+Express project would be labeled `javascript|express` while being detected as `typescript` in languages. Consider passing detected language through or using `node` as a neutral label. Minor cosmetic inconsistency.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `_extract_json_keys` is defined in `detect.sh` but called by `detect_commands.sh`. The dependency is documented in the header comment, and the test correctly sources `detect.sh` first (with an explanatory comment). No action needed — just noting the cross-file function dependency for future readers.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `detect_report.sh` line 23 calls `detect_project_type()`, which internally calls `detect_languages()`, `detect_frameworks()`, and `detect_entry_points()` — the same three functions already called at lines 19-22. All four detection functions run twice on every `format_detection_report()` call. Carried forward from cycle 1; still non-blocking, but worth noting for M18/M19 callers where latency matters.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `detect.sh` line 92: `sort -t'|' -k2,2 -k1,1` sorts confidence alphabetically, producing `high` < `low` < `medium` order. Downstream consumers expecting highest-confidence first will be surprised. Carried forward from cycle 1; non-blocking but should be addressed before M19 consumers rely on this ordering.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `detect.sh` line 62: `"$(basename "$(compgen -G "$proj_dir"/*.csproj 2>/dev/null | head -1)" 2>/dev/null || echo "*.sln")"` — the `2>/dev/null` suppresses errors from `basename`, not from `compgen`. Errors from `compgen` still propagate if the glob fails unexpectedly. Carried forward from cycle 1 drift observation; minor portability concern only.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] `detect.sh` line 113: `&>/dev/null 2>&1` is redundant — `&>` already redirects both stdout and stderr, making the trailing `2>&1` a no-op. Harmless but noisy; can be simplified to `&>/dev/null`.
- [ ] [2026-03-20 | "Implement Milestone 17: Tech Stack Detection Engine"] ARCHITECTURE.md Layer 3 still does not list `lib/detect.sh`, `lib/detect_commands.sh`, or `lib/detect_report.sh`. The ACP was ACCEPTED in cycle 1 — this documentation update remains outstanding.
- [ ] [2026-03-20 | "Implement Milestone 16: Outer Orchestration Loop (Milestone-to-Completion)"] `report_orchestration_status()` in `common.sh` reads `MAX_AUTONOMOUS_AGENT_CALLS` as a global instead of receiving it as a fifth parameter. The spec signature is `report_orchestration_status(attempt, max, elapsed, agent_calls)` — `max` refers to pipeline attempt max. Agent call ceiling leaks in as a global side-channel. Consider adding a fifth parameter or making the dependency explicit via a comment.
- [ ] [2026-03-20 | "Implement Milestone 16: Outer Orchestration Loop (Milestone-to-Completion)"] `finalize_summary.sh` JSON string escaping via `sed 's/"/\"/g'` only escapes double-quotes, not backslashes. A milestone ID or task string containing a backslash would produce invalid JSON. Use `sed 's/\/\\/g; s/"/\"/g'` for robustness, matching the pattern already used for `files_json` path escaping in the same file.
- [ ] [2026-03-20 | "Implement Milestone 16: Outer Orchestration Loop (Milestone-to-Completion)"] `lib/metrics.sh` remains ~395 lines — a pre-existing violation that predates M16. Extraction of `_avg_field`, `_scout_accuracy`, and `summarize_metrics` into `metrics_dashboard.sh` would bring it under the ceiling. Not blocking this merge.
- [ ] [2026-03-20 | "Implement Milestone 16: Outer Orchestration Loop (Milestone-to-Completion)"] `_run_auto_advance_chain()` in `orchestrate_helpers.sh` calls `run_complete_loop` recursively (line 44). Recursion depth is bounded only by `AUTO_ADVANCE_LIMIT` and the function stack. This is the pre-existing pattern from M16's original design. No action required, but a comment noting the recursion depth bound would prevent future confusion.
- [ ] [2026-03-19 | "Implement Milestone 15.4.1: Single-Note Utility Functions in lib/notes.sh"] `_escape_sed_pattern` is defined but not called anywhere in the current implementation (exact string matching is used instead). If it is intended for future use by 15.4.2/15.4.3, a brief comment to that effect would prevent it from appearing orphaned.
- [ ] [2026-03-19 | "Implement Milestone 15.4.1: Single-Note Utility Functions in lib/notes.sh"] `extract_note_text` fallback `if` blocks (lines ~358–366) are unreachable dead code — the glob-based primary strip already handles all checkbox variants. Harmless but could be removed in a future cleanup pass.
- [ ] [2026-03-19 | "Implement Milestone 15.4.1: Single-Note Utility Functions in lib/notes.sh"] File is 547 lines, over the 300-line guideline. Coder acknowledged this; a split into `lib/notes_single.sh` would address it in a future milestone.
- [ ] [2026-03-19 | "Implement Milestone 15.2.2.1: Archive Function — Remove [DONE] Lines Instead of One-Liner Summaries"] `lib/milestone_archival.sh` is now 383 lines (28% over the 300-line guideline), up from 363 in round 1. Pre-existing concern; no new split is warranted until another helper is added.
- [ ] [2026-03-19 | "Implement Milestone 15.2.2.1: Archive Function — Remove [DONE] Lines Instead of One-Liner Summaries"] AWK block inside `archive_completed_milestone()` still computes `safe_num = num` and `gsub(/./, "\.", safe_num)` on every input line. Moving to `BEGIN` is a minor efficiency improvement with no correctness impact. Pre-existing from round 1.
- [ ] [2026-03-19 | "Implement Milestone 15.2.1: mark_milestone_done() Function"] CODER_SUMMARY.md lists only `lib/milestone_ops.sh` under "Files Modified" but omits the newly created `lib/milestone_acceptance.sh`. Minor documentation gap — the extraction is correct and the file is well-structured.
- [ ] [2026-03-19 | "Implement Milestone 15.2.1: mark_milestone_done() Function"] `milestone_ops.sh` header comment still reads "Expects: milestone_acceptance.sh, milestones.sh to be sourced first" but `milestone_ops.sh` now sources `milestone_acceptance.sh` itself (line 25). The "Expects" wording is now inaccurate for `milestone_acceptance.sh` — `milestones.sh` still needs to be sourced by the caller. Low-priority comment fix.
- [ ] [2026-03-19 | "Implement Milestone 15.2.1: mark_milestone_done() Function"] Carried from prior review: `mark_milestone_done` defaults the CLAUDE.md path via `${2:-${PROJECT_RULES_FILE:-CLAUDE.md}}` which is superior to the bare `CLAUDE.md` used by sibling functions `check_milestone_acceptance` and `get_milestone_commit_body`. Consider back-filling `PROJECT_RULES_FILE` awareness to those functions in a future cleanup task.
- [ ] [2026-03-19 | "Implement Milestone 15.1.2.1: Resolved Cleanup Function for NON_BLOCKING_LOG.md"] `lib/drift_cleanup.sh:253` — The while-read filter uses `^- ` (any list item) rather than `^- [x]` (only checked items). This is actually correct — the Resolved section should only contain `[x]` items in practice, and using a broader match is more robust. No action needed, just noting the intentional deviation from the spec's exact phrasing.
- [ ] [2026-03-19 | "Implement Milestone 15.1.2.1: Resolved Cleanup Function for NON_BLOCKING_LOG.md"] `lib/drift_cleanup.sh:262` — If resolved items contain blank lines between them (edge case), those blank lines would survive the rewrite and remain in the empty Resolved section. Cosmetic only — doesn't affect correctness.
- [ ] [2026-03-19 | "Implement Milestone 15.1.1: Notes Gating — Flag-Only Claiming, Coder Cleanup, and --human Flag"] `tekhton.sh:152-153` — `HUMAN_MODE` and `HUMAN_NOTES_TAG` are still not exported at their initialization lines; `export HUMAN_MODE HUMAN_NOTES_TAG` only fires inside the `--human` case branch (line 540). This was noted in the previous cycle and remains unchanged. It is harmless in all current call paths (sourced libs use `${HUMAN_MODE:-false}`) but would break subprocess contexts if `--human` is never passed and a subprocess tries to read `HUMAN_MODE` as an environment variable. Adding `export` to the initialization lines (152-153) would be the clean fix.
- [ ] [2026-03-19 | "Implement the items in the HUMAN_NOTES.md"] `AUTO_COMMIT=true` is set as an unconditional default in `config_defaults.sh:129`. The CLAUDE.md Milestone 15.1 spec says this should be conditional (`true` in milestone mode, `false` otherwise). The HUMAN_NOTES request is satisfied by the current unconditional default, but M15.1 will need to change `config_defaults.sh` to make it conditional. A comment marking this as "to be revisited in M15.1" would prevent future confusion.
- [ ] [2026-03-19 | "Implement the items in the HUMAN_NOTES.md"] `check_usage_threshold()` in `lib/common.sh:187` uses `grep -oE '[0-9]+(.[0-9]+)?%' | head -1` to extract the usage percentage. If the `claude usage` output format changes to include multiple percentages (e.g., per-model breakdowns), `head -1` may capture the wrong value. The existing warning-and-allow fallback mitigates this, but a comment explaining which percentage is expected would help future maintainers.
- [ ] [2026-03-19 | "Fix the two bugs in the HUMAN_NOTES.md"] `tests/test_human_notes_lifecycle.sh` header comment (lines 8–13) lists 7 test groups but Phases 10 and 11 are not reflected in the summary — update to include exit-code awareness tests for completeness.
- [ ] [2026-03-19 | "Implement the items in the HUMAN_NOTES.md"] `lib/hooks.sh:110,113` — `grep -qi "fix|bug"` and `grep -qi "^n/a|^none|^(fill"` use BRE `|` alternation (GNU grep extension, not portable to macOS BSD grep). Use `-Eqi` with unescaped `|` for POSIX ERE portability.
- [ ] [2026-03-19 | "Implement the items in the HUMAN_NOTES.md"] `tests/test_auto_commit_conditional_default.sh:102-104` — The `reload_defaults` call at line 103 is dead: the comment "Should NOT override since AUTO_COMMIT is already set" is incorrect because `reload_defaults` unsets `AUTO_COMMIT` first (as the next comment acknowledges). Remove the dead `reload_defaults` line; the `AUTO_COMMIT=false` + `source config_defaults.sh` sequence that follows is the correct test.
- [ ] [2026-03-19 | "Fix the items in the NON_BLOCKING_LOG.md"] `lib/notes.sh:26` — `HUMAN_NOTES` literal in pattern `'human.?notes|HUMAN_NOTES'` is redundant with `-i` (case-insensitive) since `human.?notes` already matches `HUMAN_NOTES`. Not a bug, but slightly noisy pattern.
- [ ] [2026-03-19 | "Fix the items in the NON_BLOCKING_LOG.md"] `stages/coder.sh:322` — `build_context_packet` is called before the `should_claim_notes` gate (lines 327-332). If `build_context_packet` ever reads `HUMAN_NOTES_BLOCK` as a global, it would see the pre-gate value. Currently safe since the function takes explicit args, but the ordering is subtly coupled and worth a comment.

## Resolved
