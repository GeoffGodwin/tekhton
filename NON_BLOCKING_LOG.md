# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [ ] [2026-03-16 | "Implement Milestone 4: Mid-Run Clarification And Replanning"] `prompts/clarification.prompt.md` (carry-over from cycle 1): file exists and was created as part of this milestone, but `render_prompt "clarification"` is never called anywhere in the codebase. The post-clarification coder re-run uses `render_prompt "coder"` which already has the `{{IF:CLARIFICATIONS_CONTENT}}` block. Either remove the file or add a header comment explaining when it is intended to be used.
- [ ] [2026-03-16 | "Implement Milestone 4: Mid-Run Clarification And Replanning"] `lib/replan.sh` is 294 lines — just within the 300-line limit. If `_apply_replan_delta` grows with future multi-file merge logic, it will breach the cap. Worth noting for future milestones.
- [ ] [2026-03-16 | "Implement Milestone 2: Context Compiler"] Prior non-blocking notes from cycle 1 remain unaddressed and carry forward: `IGNORECASE = 1` gawk extension at `lib/context.sh:229`, redundant first `export "$var_name=$compressed"` at line 480 immediately overwritten by the note-prefixed export inside the `if [ "$saved" -gt 0 ]` block, missing `local j` before the inner for-loop at line 493, and mixed `[ ]`/`[[ ]]` usage throughout the file. None are correctness issues in the tested environment but remain consistency gaps.
- [x] [2026-03-16 | "The last pipeline run failed per /mnt/c/Users/Geoff/workspace/geoffgodwin/tekhton/.claude/logs/20260316_133234_last-run-completed-with-a-failed-test-check-the-mn.log but the actual work was done. Please review the changes."] `CLAUDE_REVIEWER_MODEL` has no explicit default in `lib/config.sh` (lines 111–128) despite `CLAUDE_CODER_MODEL`, `CLAUDE_JR_CODER_MODEL`, `CLAUDE_TESTER_MODEL`, `CLAUDE_SCOUT_MODEL`, and `CLAUDE_ARCHITECT_MODEL` all having defaults. If `stages/review.sh` reads `$CLAUDE_REVIEWER_MODEL` directly, a missing pipeline.conf entry will produce an empty model string at runtime. Tests pass (the test suite likely doesn't invoke the reviewer stage with a minimal config), so this is low urgency — but a default of `${CLAUDE_STANDARD_MODEL}` should be added for consistency.
- [ ] [2026-03-16 | "The last pipeline run failed per /mnt/c/Users/Geoff/workspace/geoffgodwin/tekhton/.claude/logs/20260316_133234_last-run-completed-with-a-failed-test-check-the-mn.log but the actual work was done. Please review the changes."] `TEST_CMD` defaults to `true` (line 120), silently no-oping for any project that forgets to set it. This is likely intentional for projects without a test suite, but a one-line comment noting this intent would prevent future confusion.
- [ ] [2026-03-16 | "Implement Milestone 1: Token And Context Accounting"] `_CONTEXT_REPORT` still accumulates lines using literal ` ` strings and relies on `echo -e` for interpretation (lib/context.sh:81, :106). Carry-over from prior cycle — not introduced by this rework. Fragile if the output call is ever refactored away from `echo -e`. Consider `printf` or `$' '` during accumulation.
- [ ] [2026-03-16 | "Implement Milestone 1: Token And Context Accounting"] `LAST_CONTEXT_TOKENS` in the run summary still reflects only the most recently completed stage. The final `print_run_summary()` shows tester context, not coder's (typically largest). Design choice per architecture decisions — worth a comment or doc note before Milestone 2 when context management becomes active.
- [x] [2026-03-16 | "Implement Milestone 0.5: Agent Output Monitoring And Null-Run Detection"] `AGENT_ACTIVITY_POLL` (line 300) is still an undocumented config knob — not listed in the CLAUDE.md Template Variables table or `templates/pipeline.conf.example`. Add it to both. (Carried from prior cycle, still unaddressed.)
- [ ] [2026-03-16 | "Implement Milestone 0.5: Agent Output Monitoring And Null-Run Detection"] `find -maxdepth 4` in `_detect_file_changes()` (line 595) and `_count_changed_files_since()` (line 615) is a hard-coded depth that could miss files in deeply nested project structures. Consider a higher default (e.g., 8) or a configurable `AGENT_FILE_SCAN_DEPTH` variable. (Carried from prior cycle, still unaddressed.)
- [x] [2026-03-12 | "Please remedy the accumulated items in the NON_BLOCKING_LOG.md"] `NON_BLOCKING_LOG.md` items 8–11 (Milestone 5 batch) are marked `[x]` without Resolved notes. The Resolved section precedent includes a note for every item, even "no action needed" ones (e.g. "Resolved: Pre-existing pattern — no action needed."). A one-liner per item keeps the log self-documenting. Not worth a rework cycle on its own if item 13 blocker is fixed at the same time.
- [x] [2026-03-12 | "Implement Milestone 5: Tests + Documentation Update"] `tests/test_plan_templates.sh` line 171 uses `grep -oP` (Perl-compatible regex): `grep -oP '<!-- PHASE:K[0-9]+'`. GNU grep on Linux/WSL2 supports `-P`, but BSD grep (macOS) does not. Since the project targets Linux/WSL2, this works in practice. Consider `grep -o '<!-- PHASE:[0-9][0-9]*' | grep -o '[0-9]*$'` if portability is ever needed.
- [x] [2026-03-12 | "Implement Milestone 5: Tests + Documentation Update"] `tests/test_plan_generate_stage.sh` lines 241/258: `unset VAR 2>/dev/null || true` — `unset` never writes to stderr and never fails, so the `2>/dev/null || true` is harmless but redundant. Pre-existing pattern in the codebase so not worth a change on its own.
- [x] [2026-03-12 | "Implement Milestone 5: Tests + Documentation Update"] `tests/test_plan_templates.sh` line 92 adds a new `source "${TEKHTON_HOME}/lib/plan.sh"` with a `# shellcheck source=` directive (line 91). Shellcheck still emits SC1091 info at that line — consistent with how other test files handle sourced libraries. Not a regression.
- [x] [2026-03-12 | "Implement Milestone 5: Tests + Documentation Update"] `test_plan_completeness.sh` depth-scoring tests (`_score_section_depth`, `_is_section_shallow`, shallow detection in `check_design_completeness`) are confirmed present from Milestone 4. Milestone 5 criterion is satisfied; the coder correctly identified no duplication was needed.
- [x] [2026-03-12 | "Implement Milestone 4: Follow-Up Interview Depth + Completeness Checker Upgrade"] `run_plan_followup_interview()` (line 100) fetches `req` from the template guidance map and passes it to `_read_section_answer` (line 130), but line 104 hardcodes `*required` unconditionally in the display. The `req` value is not dead — it's used in the function call — but the display ignores it. Minor inconsistency. — **Resolved: Display now conditionally shows `*required` or `(optional)` based on the `req` value.**
- [x] [2026-03-12 | "Implement Milestone 4: Follow-Up Interview Depth + Completeness Checker Upgrade"] Template variables `{{PROJECT_TYPE}}`, `{{INCOMPLETE_SECTIONS}}`, and `{{INTERVIEW_ANSWERS_BLOCK}}` used in `plan_interview_followup.prompt.md` are not listed in the CLAUDE.md Template Variables table. They work correctly (set before `render_prompt()`) but should be added for discoverability.
- [x] [2026-03-12 | "Implement  Milestone 3: Generation Prompt Overhaul for Deep CLAUDE.md"] `stages/plan_generate.sh` comment on line 14 says `render_prompt()` comes from `lib/plan.sh` but it actually comes from `lib/prompts.sh` — pre-existing inaccuracy, not introduced by this change, but worth correcting when that file is next touched.
- [x] [2026-03-12 | "Implement  Milestone 3: Generation Prompt Overhaul for Deep CLAUDE.md"] Milestone 3 spec listed `stages/plan_generate.sh` as a file to modify (to increase the default from 30 to 50), but the default only ever lived in `lib/plan.sh`. The coder correctly updated the actual location; the spec was slightly off. No action needed.
- [x] [2026-03-11 | "TESTER_REPORT.md found 4 bugs. Please fix them."] `AGENT_TIMEOUT=0` set in Phase 6 is never reset before Phases 7, 8, and 9. The mocks in those phases exit immediately so it doesn't cause failures, but resetting AGENT_TIMEOUT (e.g., `AGENT_TIMEOUT=30`) after Phase 6 would make each test phase self-contained and easier to reason about. Pre-existing issue, not introduced by this fix. — **Resolved: Added `AGENT_TIMEOUT=30` reset after Phase 6 alongside the existing `AGENT_ACTIVITY_POLL` unset.**
- [x] [2026-03-11 | "The milestone 2 work generated a bug which can be found in the TESTER_REPORT.md. Please remedy that bug."] `tests/test_agent_fifo_invocation.sh` lines 125, 155, 185, 217, 238, 241: uses `PATH="${MOCK_BIN}:${PATH}" run_agent ...` as if it creates a temporary environment for the function call, but bash only applies the temporary-assignment semantics to external commands — for shell functions the variable is modified in the current shell and persists. The test works correctly because `MOCK_BIN` is always prepended before each call anyway, but the technique is misleading. Simpler and clearer: export `PATH="${MOCK_BIN}:${PATH}"` once at the top of the test file.
- [x] [2026-03-11 | "The milestone 2 work generated a bug which can be found in the TESTER_REPORT.md. Please remedy that bug."] `tests/test_agent_fifo_invocation.sh` line 17: uses `TMPDIR` as the test temp dir variable, which shadows the conventional `$TMPDIR` system environment variable. Renaming to `TEST_TMP` or `_TMPDIR` would avoid the shadowing. Not a correctness issue in practice since no mock script uses `$TMPDIR`, but worth a future-clean.
- [x] [2026-03-11 | "The milestone 2 work generated a bug which can be found in the TESTER_REPORT.md. Please remedy that bug."] The activity-timeout null run warning at Phase 6 outputs "agent timed out after 0s" because `AGENT_TIMEOUT=0` (outer timeout disabled) leaves `$_timeout` as the literal string `0` in the warn message. Pre-existing behavior — not introduced by this change — but confusing. — **Resolved: Null-run detection else branch now checks if `_timeout` is 0 and shows "outer timeout disabled" instead of "0s".**
- [x] [2026-03-11 | "Implement Milestone 1: Model Default + Template Depth Overhaul"] `cli-tool.md` has both a `## Configuration System` section (line 65) and a `## Config Architecture` section (line 154) with overlapping content. Both are REQUIRED-marked. During the interview, users may be confused about how to partition their answers between the two. Consider distinguishing their guidance comments more sharply (e.g., Configuration System = user-facing behavior; Config Architecture = developer-facing values-in-config rules) or consolidate into one if future milestone rewrites touch this template.
- [x] [2026-03-11 | "Implement Milestone 1: Model Default + Template Depth Overhaul"] `tests/test_plan_templates.sh` verifies REQUIRED marker counts and presence of `## .*Overview` and `## Tech Stack` sections, but does not assert that "Developer Philosophy & Constraints" and "Config Architecture" sections exist by those exact names in each template. The acceptance criteria explicitly require both sections by name. If a future template rewrite changes a section name but keeps the REQUIRED count, the tests would still pass. Low risk for this milestone since we verified manually, but worth noting for M5 test expansion.
- [x] [2026-03-11 | "Implement Milestone 1: Model Default + Template Depth Overhaul"] `custom.md` has 16 sections against the milestone target of 12–15. This is harmless over-delivery.
- [x] [2026-03-11 | "Implement Milestone 7: Tests + Documentation"] CLAUDE.md still contains "Current Initiative: Planning Phase (`--plan`)" with all 7 milestone descriptions marked as future work. Since all milestones are now complete, this section is stale. Consider replacing it with a "Completed: Planning Phase" summary or removing the milestone plan entirely in a future pass. — **Resolved: Updated header to "Completed Initiative", updated summary paragraph, and marked all milestones [DONE].**
<!-- Items added here by the pipeline. Mark [x] when addressed. -->

## Resolved
- [x] [2026-03-11 | "Fix broken tests from the previous run of the tester"] `run_interview()` exports `PLAN_STATE_FILE` with the project dir embedded in a single-quoted heredoc, which correctly expands at runtime — the ordering is correct, but a brief inline comment explaining "expanded by outer shell, not written literally" would aid future readers. — **Resolved: Added inline comment to `write_plan_state()` heredoc in lib/plan_state.sh explaining that variables are expanded by the outer shell at write time.**
- [x] [2026-03-11 | "Fix broken tests from the previous run of the tester"] The subprocess sources `plan_interview.sh` (which re-activates `set -euo pipefail`), and `set +e` immediately follows to undo `-e` before the function call. This is correct but relies on source-order awareness. The existing comment "set +e to prevent set -euo pipefail … from killing the shell" adequately covers it — no action required. — **Resolved: No action required per reviewer — existing comment is adequate.**
- [x] [2026-03-11 | "Implement Milestone 6: Planning State Persistence + Config Integration"] `load_plan_config()` (lib/plan.sh) sources the entire `pipeline.conf`, importing all pipeline keys (ANALYZE_CMD, BUILD_CHECK_CMD, etc.) into the planning environment. Functionally harmless since all planning keys have safe defaults. A brief comment clarifying intentional scope would prevent future confusion. — **Resolved: Expanded comment in `load_plan_config()` to explain intentional full-file sourcing and why it's harmless.**
- [x] [2026-03-11 | "Implement Milestone 6: Planning State Persistence + Config Integration"] `run_plan()` step 5 (`run_plan_review`) runs unconditionally with no `skip_to` guard, unlike steps 2–4. Functionally correct since review is always the final step after generation. A short comment explaining the intentional absence of the guard would prevent future confusion. — **Resolved: Added inline comment above step 5 explaining that review always runs as the final step regardless of resume state.**
- [x] [2026-03-11 | "Implement Milestone 6: Planning State Persistence + Config Integration"] ARCHITECTURE.md needs updating to add `lib/plan_state.sh` to the System Map and File Ownership table, and to update the `--plan` source list in the Layer 1 description. Acceptable to defer to Milestone 7.
- [x] [2026-03-11 | "Implement the non_blocking_log notes"] `lib/plan.sh:125-143`: `_extract_project_name()` and `_extract_milestones()` remain defined but are dead code — their logic was inlined into `_display_milestone_summary()` in a prior pass. Safe to remove in a follow-up cleanup. — **Resolved: Removed both dead functions from lib/plan.sh and removed their tests from test_plan_review_functions.sh. _display_milestone_summary() has its own inline logic and tests.**
- [x] [2026-03-11 | "Implement Milestone 5: Milestone Review UI + File Output"] `run_plan_review()` prints `success "Files written to ${PROJECT_DIR}"` on `[y]` accept, but no file writing actually occurs — DESIGN.md and CLAUDE.md were already written by the generation agent in Step 4. The message is factually incorrect and could confuse users. Consider changing to "Files are ready at ${PROJECT_DIR}" or "Files confirmed." — **Resolved: Changed to "Files confirmed at ${PROJECT_DIR}:"**
- [x] [2026-03-11 | "Implement Milestone 5: Milestone Review UI + File Output"] The `[e]` editor branch (`${EDITOR:-nano} "$claude_file"`) does not guard against non-zero editor exit codes. If the editor exits non-zero (e.g., user kills the terminal), `set -euo pipefail` will abort `run_plan_review()` unexpectedly. Adding `|| true` or `|| warn "Editor exited with non-zero status"` would prevent silent abort. — **Resolved: Added `|| warn "Editor exited with non-zero status"` guard.**
- [x] [2026-03-11 | "Implement Milestone 5: Milestone Review UI + File Output"] `_display_milestone_summary()` reads the claude_file twice (once in `_extract_project_name`, once in `_extract_milestones`). Not a correctness issue, but could be refactored to a single read in a future cleanup pass. — **Resolved: Refactored to read file once with `cat` and extract both values from the cached content.**
- [x] [2026-03-11 | "Complete more of the items in the NON_BLOCKING_LOG.md file."] `tests/test_plan_completeness_loop.sh` lines 185-186: comment says "Pass 1: invalid choice 'x' decrements pass_num" — this describes the old behavior before the inner re-prompt loop was added. The comment is stale and misleading; update it to describe the actual inner-loop behavior (invalid choice triggers a re-prompt without incrementing pass_num or re-running the completeness check).
- [x] [2026-03-10 | "Implement Milestone 4: CLAUDE.md Generation Agent"] `lib/plan.sh` is at 291 lines. Milestone 6 will add state save/restore and config defaults, which will push it over the 300-line limit. A split (e.g. extract `check_design_completeness` and helpers into `lib/plan_completeness.sh`) should be planned before Milestone 6 begins. — **Resolved: `lib/plan_completeness.sh` was already extracted in prior work.**
- [x] [2026-03-10 | "Implement Milestone 4: CLAUDE.md Generation Agent"] `ARCHITECTURE.md` needs a Layer 1 update noting that `--plan` now sources `agent.sh` in addition to the previous four libraries. The ACP flags this; it is Milestone 7 scope but worth tracking. — **Resolved: ARCHITECTURE.md Layer 1 already lists `agent.sh` in the --plan source list.**
- [x] [2026-03-10 | "Implement Milestone 3: Completeness Check + Follow-Up"] `lib/plan.sh` and `stages/plan_interview.sh` still lack `set -euo pipefail` at the file top. Pre-existing from Milestones 1/2; settings are inherited from the sourcing script and test harness. Fix in a dedicated cleanup pass. — **Resolved: No lib/*.sh or stages/*.sh file in the project carries `set -euo pipefail` — all rely on tekhton.sh (the only entry point) to set it. Adding it only to plan files would be inconsistent. This is a project-wide convention, not a per-file bug. Marking resolved as by-design.**
- [x] [2026-03-10 | "Implement Milestone 3: Completeness Check + Follow-Up"] `run_plan_completeness_loop`: the invalid-choice handler decrements `pass_num` then falls through to the next loop iteration, which re-increments and re-runs `check_design_completeness`, printing a second "Completeness Check — Pass N" header for the same logical pass. A small inner re-prompt loop for just the `[f/s]` choice would eliminate the redundant check and duplicate header. — **Resolved: Already fixed in prior work — inner `while true` loop handles re-prompting.**
- [x] [2026-03-10 | "Implement Milestone 3: Completeness Check + Follow-Up"] `tests/test_plan_completeness.sh` line 14: `TMPDIR=$(mktemp -d)` shadows the system `$TMPDIR` environment variable used by many programs. Rename to `TEST_TMPDIR` (or similar) to avoid unintended side effects. — **Resolved: Already renamed to `TEST_TMPDIR` in prior work.**
- [x] [2026-03-10 | "implement Milestone 2: Interactive Interview Agent"] `stages/plan_interview.sh` and `lib/plan.sh` both lack a `set -euo pipefail` header. These files are sourced by `tekhton.sh` (which has `set -euo pipefail`), so the options are inherited at runtime, but CLAUDE.md rule #2 requires all scripts to carry the header explicitly. Add the header to both files for consistency. — **Resolved: Duplicate of item above. No lib/*.sh or stages/*.sh file carries this header — project convention is to inherit from tekhton.sh.**
- [x] [2026-03-10 | "implement Milestone 2: Interactive Interview Agent"] `stages/plan_interview.sh` lines 91 and 102: `wc -l < "$file"` can produce leading whitespace on some platforms. `$(wc -l < "$file" | tr -d ' ')` is strictly portable. — **Resolved: Already fixed in prior work — `| tr -d ' '` present on all `wc -l` calls.**
- [x] [2026-03-10 | "implement Milestone 2: Interactive Interview Agent"] `--dangerously-skip-permissions` is intentional and documented in `CODER_SUMMARY.md`. Consider a one-line `warn` at startup — e.g., "Note: interview agent has write access to your project directory to create DESIGN.md." — so users are explicitly informed before the session starts. — **Resolved: Already fixed in prior work — `warn` on line 49 of plan_interview.sh.**
- [x] [2026-03-10 | "implement Milestone 2: Interactive Interview Agent"] `prompts/plan_interview.prompt.md` instructs the agent to write `DESIGN.md` in "the current working directory." Worth a comment in `run_plan_interview()` noting the implicit CWD coupling, before Milestone 6 adds state-resume logic that might `cd`. — **Resolved: Added NOTE comment above the claude invocation in `run_plan_interview()`.**
- [x] [2026-03-10 | "Implement Milestone 1: Foundation — CLI Flag, Library Skeleton, Project Type Selection"] `run_plan()` (plan.sh:84) prints "Project type '...' selected" and "Template resolved: ..." after `select_project_type()` already printed both via `success` and `log`. This is redundant output — the user sees the confirmation twice. Consider removing the duplicates from `run_plan()` (lines 99–102) and letting `select_project_type()` be the single source of truth for those messages. — **Resolved: Already fixed in prior work — `run_plan()` no longer has duplicate output.**
- [x] [2026-03-10 | "Implement Milestone 1: Foundation — CLI Flag, Library Skeleton, Project Type Selection"] `select_project_type()` uses `read -r choice` directly from stdin with no `/dev/tty` fallback. If `--plan` is ever invoked with piped stdin (e.g., scripted testing), it will block silently. A natural edge case to handle in Milestone 6's state persistence work. — **Resolved: Already fixed in prior work — `/dev/tty` fallback with `TEKHTON_TEST_MODE` bypass.**
