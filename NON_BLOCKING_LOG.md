# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [ ] [2026-03-19 | "Implement Milestone 14: Turn Exhaustion Continuation Loop"] `ARCHITECTURE.md` line 83: `lib/agent.sh` description still reads "Sources `agent_monitor.sh` and `agent_helpers.sh`" — `agent_monitor_platform.sh` is not listed. The new file has its own correct entry on line 84, so the gap is minor, but the agent.sh blurb is mildly stale.
- [ ] [2026-03-19 | "Implement Milestone 13.2.1: "] `metrics.sh` is 477 lines — well over the 300-line ceiling. This is a pre-existing issue from prior milestones; this milestone added ~25 lines. The file should be split (e.g., extract `calibrate_turn_estimate()` + helpers into `lib/metrics_calibration.sh`) in a future cleanup pass.
- [ ] [2026-03-19 | "Implement Milestone 13.2.1: "] `_pct()` is defined as a nested function inside `summarize_metrics()`. Bash does not scope inner functions — `_pct` becomes a global after first call. Not a bug here (it's only called within `summarize_metrics`), but it pollutes the global namespace. Consider inlining the arithmetic or hoisting it to a top-level helper.
- [ ] [2026-03-18 | "Implement Milestone 13.2.1: Core Retry Envelope in run_agent()"] CODER_SUMMARY.md states "No files modified" but git status shows `M lib/agent_helpers.sh` and `?? lib/agent_retry.sh`. The summary is factually incorrect — changes were made. Future coders: the summary must accurately report what was changed.
- [ ] [2026-03-18 | "Implement Milestone 13.2.1: Core Retry Envelope in run_agent()"] UPSTREAM errors still hit an early `return` in `run_agent()` before `_append_agent_summary()` is called (line ~180), so exhausted transient retries do not produce a tail-friendly log summary block. Pre-existing gap from M12.2/12.3, carry-forward from first review.
- [ ] [2026-03-18 | "Implement Milestone 13.2.1: Core Retry Envelope in run_agent()"] OOM retry delay remains pinned at 15s flat (overrides exponential backoff). Intentional per spec but never backs off on repeated OOM. Acceptable for now.
- [ ] [2026-03-18 | "Implement Milestone 13.2.1: "] `lib/agent_helpers.sh` is 330 lines, exceeding the 300-line ceiling. This is a direct consequence of the cycle-1 fix (extract retry envelope into agent_helpers.sh as directed). The milestone spec acknowledged this growth. The file should be split in a future debt sweep — e.g., a `lib/agent_retry.sh` containing `_run_with_retry`, `_classify_agent_exit`, and `_should_retry_transient` (≈195 lines), leaving the summary/null-run helpers in `agent_helpers.sh` (≈135 lines).
- [ ] [2026-03-18 | "Continue Implementing Milestone 13.1: Retry Infrastructure — Config, Reporting, and Monitoring Reset"] `lib/agent_monitor.sh` is 401 lines, exceeding the 300-line ceiling. This is pre-existing (the rework added only 1 line) — but the file should be split as part of a future cleanup. `_reset_monitoring_state()` and the file-change detection helpers could form a `lib/agent_monitor_reset.sh` or similar shim.
- [ ] [2026-03-18 | "Continue Implementing Milestone 13.1: Retry Infrastructure — Config, Reporting, and Monitoring Reset"] `report_retry` uses an em-dash (`—`) in the format string (`"Attempt ${attempt}/${max} — retrying in ${delay}s..."`). On a non-UTF-8 terminal the box chars fall back to ASCII, but the em-dash in the message string does not. Low impact since the message is informational, but for strict ASCII safety the em-dash could be replaced with a hyphen in the non-UTF-8 branch.
- [ ] [2026-03-18 | "Implement Milestone 13.1: Retry Infrastructure — Config, Reporting, and Monitoring Reset"] `lib/common.sh`: `report_retry` duplicates ~20 lines from `report_error` (box-drawing variable setup, `_hline` loop, and the nested `_box_line`/`_rbox_line` helper are structurally identical). A shared `_build_box_components()` helper extracting these into reusable output variables would eliminate the duplication while keeping both functions independent.
- [ ] [2026-03-18 | "Implement Milestone 13.1: Retry Infrastructure — Config, Reporting, and Monitoring Reset"] `lib/agent_monitor.sh`: file is now 400 lines, exceeding the 300-line ceiling. Pre-existing condition — the file was ~373 lines before this milestone, so the added `_reset_monitoring_state()` (~27 lines) is not the root cause. Flagging for the next opportunity to split (e.g., extract `_reset_monitoring_state` and `_detect_file_changes`/`_count_changed_files_since` into `lib/agent_monitor_helpers.sh`).
- [ ] [2026-03-18 | "Implement Milestone 13.1: Retry Infrastructure — Config, Reporting, and Monitoring Reset"] `_reset_monitoring_state()` in `agent_monitor.sh` (line 339): kills `_TEKHTON_AGENT_PID` but does not clear it afterward (`_TEKHTON_AGENT_PID=""`). If called twice in succession (e.g., two retries), the second call attempts to kill the same dead PID. Harmless due to `|| true`, but leaving a stale PID in the variable is inconsistent with the reset intent.
## Resolved
- [x] [2026-03-18] `split_milestone()` in `lib/milestone_split.sh` — changed `export` to plain shell variable assignment for `MILESTONE_DEFINITION`, `SCOUT_ESTIMATE`, `TURN_CAP`, `PRIOR_RUN_HISTORY`. `render_prompt()` reads these via `${!var_name}` indirect expansion in the same shell scope, so export was unnecessary environment bloat.
- [x] [2026-03-18] `lib/milestone_archival.sh` now explicitly listed in CODER_SUMMARY.md Files Modified section. Process tracking issue resolved.
- [x] [2026-03-18] Em-dash (U+2014) removed from all 5 regex character classes in `lib/milestone_archival.sh` (lines 35, 80, 101, 163, 225). Replaced `([:.\ -]|—)` with portable `[^[:alnum:]]` which matches any non-alphanumeric delimiter.
- [x] [2026-03-18] Recursion depth log line added to null-run auto-split path in `stages/coder.sh` at lines 348-350 and 470-473: `warn "Auto-split complete — re-running coder stage for milestone ... (depth N/M)..."`.
- [x] [2026-03-18] Comment added to `_switch_to_sub_milestone` in `stages/coder.sh` lines 21-22 explaining `get_milestone_title` fallback behavior when split agent uses a non-matching heading format.
- [x] [2026-03-18] `MILESTONE_SPLIT_MAX_TURNS` hard cap of 50 documented in `templates/pipeline.conf.example` line 285: "Hard cap: 50 (values above 50 are clamped automatically)".
- [x] [2026-03-18] Integration tests added to `tests/test_milestone_split.sh` covering 3 coder stage wiring paths: pre-flight sizing gate (split + re-scout), null-run auto-split (handle_null_run_split + recursive coder), and turn-limit minimal-output auto-split. Tests stub `run_agent`, `split_milestone`, `init_milestone_state`, and verify call sequences.
- [x] [2026-03-18] `lib/milestone_split.sh:42` — Split `local threshold=$((...))` into separate declaration and assignment to avoid SC2155.
- [x] [2026-03-18] `lib/milestone_archival.sh:250` — Dead `local awk_rc=$?` code path already resolved in prior run: `_replace_milestone_block` now uses `|| { rm -f ...; return 1; }` pattern which catches awk failure before `set -e` fires.
- [x] [2026-03-18] `lib/milestone_archival.sh:252` — `$rep_file` cleanup already resolved in prior run: the `|| { }` block now cleans up both `$tmp_file` and `$rep_file` on awk failure.
- [x] [2026-03-18] `handle_null_run_split()` `git diff HEAD` comment already present in prior run at lines 233-237 explaining intentional use of `git diff HEAD` vs bare `git diff`.
- [x] [2026-03-17] Coder scope drift — audited all items instead of task-specified quantity. Resolved by adding scope-adherence directive to `prompts/coder.prompt.md` and softening the "address what you can" language in `stages/coder.sh` non-blocking injection to defer to task scope.
- [x] [2026-03-17] (consolidated) `milestones.sh` exceeded the 300-line guideline. Resolved by extracting acceptance checking, commit signatures, and auto-advance helpers into `lib/milestone_ops.sh`. `milestones.sh` is now ~312 lines, `milestone_ops.sh` ~260 lines.
- [x] [2026-03-17] Three duplicate "milestones.sh too long" entries consolidated into a single resolved entry above.
