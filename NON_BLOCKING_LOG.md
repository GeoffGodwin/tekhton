# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `lib/indexer_helpers.sh:32-43` — The `&&`-chained array-append pattern (`[[ -z ... ]] && languages+=(...) && seen[x]=1`) is still present. Style note from prior cycles — prefer explicit `if/then/fi` blocks per project shell style. Not a blocker.
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `stages/review.sh` — The `if [[ -z "${REPO_MAP_CONTENT:-}" ]]` guard after clearing `REPO_MAP_CONTENT` is still redundant (always true at that point). Consider removing for clarity. Not a blocker.
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `repo_map.py:113` imports the private symbol `_EXT_TO_LANG` directly from `tree_sitter_languages.py`. A small public helper (e.g. `get_extensions_for_languages(requested: set[str]) -> set[str]`) would decouple the modules and avoid leaking an underscore-prefixed name across file boundaries.
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `repo_map.py:260-262` — the Python colon-detection condition `(not before.endswith(","))` is a weak guard that could spuriously truncate signatures containing commas before the final colon. Low risk given the 500-char window cap.
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `tools/tests/` has no direct tests for `tree_sitter_languages.py` (grammar loading, `supported_extensions`, `ext_to_language_name`). These are exercised indirectly only; basic unit tests using mocks would catch regressions in the mapping table.
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `tools/setup_indexer.sh:204` — .gitignore advisory still hardcodes `.claude/indexer-venv` instead of using `$VENV_DIR_NAME`. Now that the venv path is configurable via `$2`, this check will give misleading advice when a custom path is used. Low impact (advisory only), but should track `$VENV_DIR_NAME` for consistency.
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `lib/indexer.sh:128` — `export REPO_MAP_CONTENT` inside `run_repo_map()` is still present; `REPO_MAP_CONTENT` is already declared `export` at module scope (line 24). Redundant — can be removed.
- [ ] [2026-03-22 | "Implement Indexer Infrastructure & Setup Command then carry on to future milestones."] `lib/indexer.sh:167` — `get_repo_map_slice()` still uses substring match (`"$path" == *"$f"*`) which produces false positives for similar-named files. Still a stub — tighten the match when wired into Milestone 5.
- [ ] [2026-03-22 | "Implement Milestone 2: Sliding Window & Plan Generation Integration"] `lib/init_synthesize_ui.sh:8` header comment says "Sourced by init_synthesize_helpers.sh and stages/init_synthesize.sh" but `stages/init_synthesize.sh` only sources `init_synthesize_helpers.sh` directly; the UI module is transitively loaded. The comment is misleading but behavior is correct.
- [ ] [2026-03-22 | "Implement Milestone 2: Sliding Window & Plan Generation Integration"] `lib/milestone_archival.sh:43` (carried from cycle 1) — hardcodes `initiative="Milestone DAG"` for DAG-path archival; all DAG-mode archived milestones get the same section header regardless of which initiative they belong to.
- [ ] [2026-03-22 | "Implement Milestone 2: Sliding Window & Plan Generation Integration"] `lib/milestone_archival.sh:8` (carried from cycle 1) — module-level comment still references `is_milestone_done()` and `get_milestone_title()` from `milestones.sh`; those functions now live in `lib/milestone_dag_helpers.sh`.
- [ ] [2026-03-22 | "Implement Milestone 2: Sliding Window & Plan Generation Integration"] `lib/milestone_dag_migrate.sh:221` — The pointer comment interpolates `${MILESTONE_DIR:-.claude/milestones}` directly. In the execution pipeline, `MILESTONE_DIR` is resolved to an absolute path by `config.sh`, so the comment reads `<!-- Milestones are managed as individual files in /abs/path/.claude/milestones/.` rather than the relative form. Cosmetic; tests only check for the string prefix and pass regardless.
- [ ] [2026-03-22 | "Implement Milestone 2: Sliding Window & Plan Generation Integration"] `tests/test_milestone_window.sh` — No dedicated test for `_extract_title_line` in isolation; it is exercised only indirectly through the last-resort budget exhaustion path. Low risk given the function is 8 lines, but explicit coverage would help.
- [ ] [2026-03-22 | "Implement Milestone 2: Sliding Window & Plan Generation Integration"] `tests/test_milestone_window.sh` — There is no test that verifies on-deck milestones are excluded when only frontier milestones fill the budget exactly. The budget exhaustion test exercises the title-only active milestone path, not the "frontier fits but on-deck does not" branch.
- [ ] [2026-03-22 | "Implement Milestone 2: Sliding Window & Plan Generation Integration"] `tests/test_milestone_dag_migrate.sh:194` — Comment says "Source plan_generate for the helper" but `_insert_milestone_pointer` is already available from the earlier `source milestone_dag_migrate.sh` at line 38. The sourcing of `plan_generate.sh` here is redundant for this test's purposes; it succeeds but the comment is misleading.
- [ ] [2026-03-22 | "Implement Milestone 1: Milestone DAG Infrastructure"] `lib/milestone_split.sh` header comment lists dependencies as "milestones.sh, milestone_archival.sh" but the DAG-aware split path calls `_slugify()` which is defined in `milestone_dag_migrate.sh`. Add `milestone_dag_migrate.sh` to the sourced-first list in the header. (No runtime risk — sourcing order in tekhton.sh is correct; documentation gap only.)
- [ ] [2026-03-22 | "Implement Milestone 1: Milestone DAG Infrastructure"] `milestone_dag_validate.sh` relies on bash dynamic scoping to make `declare -A _visited` and `_in_stack` visible inside the nested `_dfs_cycle_check` function. This is correct bash behaviour but non-obvious. A brief comment explaining "declared in validate_manifest, visible via dynamic scoping" would help future maintainers.
- [ ] [2026-03-21 | "Resolve all observations in NON_BLOCKING_LOG.md. For each unresolved item, apply the fix, then mark it resolved. Continue until no unresolved observations remain."] `stages/init_synthesize.sh` is 533 lines — well over the 300-line ceiling. Pre-existing from Milestone 21, not introduced here, but a cleanup pass was a natural opportunity to split it.

## Resolved
