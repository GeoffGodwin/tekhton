# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] `lib/causality.sh` is 475 lines, well over the 300-line soft ceiling. The file is logically well-organized into sections (init, emit, ID management, cap enforcement, query functions, archival), but `trace_cause_chain`, `trace_effect_chain`, and the query layer could be extracted to a `causality_query.sh` module in a future cleanup pass.
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] `_hook_causal_log_finalize` is registered 4th in the finalization sequence (after metrics, before mark_done/archive_reports/emit_run_summary). This means `archive_causal_log()` runs before `mark_milestone_done()` emits `milestone_advance` events. Those events land in the live `CAUSAL_LOG.jsonl` but not in the per-run archive. Data is not lost (the live log accumulates all runs), but the per-run archive snapshot is incomplete for finalization-phase events. No fix needed now — log for future archival refactor.
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] `trace_effect_chain` uses `grep -F ""$current""` to find events that reference `current`. This is a broad string match — it will match any JSON field containing the event ID string, not just `caused_by`. An event with `"detail":"error near coder.001"` would false-positive as an effect of `coder.001`. No impact on correctness for the primary use case (diagnostic exploration), but worth documenting the limitation.
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] `dashboard.sh` sources `dashboard_parsers.sh` using `"$(dirname "${BASH_SOURCE[0]}")/..."` rather than the project-standard `"${TEKHTON_HOME}/lib/..."` pattern. Both work; the inconsistency is minor.
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] `_STAGE_BUDGET[intake]` is never set in `tekhton.sh`. All other stages have an explicit budget assignment; intake shows `0` in the dashboard. Consider adding `_STAGE_BUDGET[intake]="${INTAKE_MAX_TURNS:-10}"` after the intake stage block.
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] The dashboard generates `.claude/dashboard/data/*.js` files on every run. The hook comment in `hooks.sh` notes these are not archived, but neither `templates/pipeline.conf.example` nor any `.gitignore` template is updated to exclude `.claude/dashboard/`. Downstream projects could accidentally commit these generated files. Add `.claude/dashboard/` to the example `.gitignore` patterns referenced in `_check_gitignore_safety()`.
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] `tests/test_finalize_run.sh` header comment still says "11 hooks in deterministic sequence" (line 8) despite the 12th hook (`_hook_causal_log_finalize`) now being registered. The test logic was updated but the comment was not.
- [ ] [2026-03-23 | "Implement Milestone 13: Watchtower Data Layer & Causal Event Log"] Hook implementation letter labeling in `finalize.sh` skips `k` (jumps from `j` to `l`). The new hook is defined as `# l.` but registered 4th (between `c` and `d`). The letters are decorative and don't affect behavior, but the skip and out-of-order registration make the labels misleading.
- [ ] [2026-03-23 | "Fix the bug found in the TESTER_REPORT.md and then fix all of the observations in the NON_BLOCKING_LOG.md"] `lib/detect_ci.sh:173` — `_detect_dockerfile_langs` iterates `Dockerfile Dockerfile.*` without `nullglob`; if no `Dockerfile.*` exists the literal string passes through the loop, but the `-f` guard silently skips it. Harmless currently; worth noting if the function expands.
- [ ] [2026-03-23 | "Fix the bug found in the TESTER_REPORT.md and then fix all of the observations in the NON_BLOCKING_LOG.md"] NON_BLOCKING_LOG items intentionally deferred (init_config.sh hardcoded models, artifact_handler_ops.sh at ceiling, security.sh tool reuse, run_tests.sh naming) — coder's rationale is sound (observation-only, actionable only on next structural change). Carry forward.

## Resolved
