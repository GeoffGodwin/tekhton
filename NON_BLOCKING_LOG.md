# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [x] [2026-03-18 | "Address the next 2 items in the NON_BLOCKING_LOG.md"] `lib/drift.sh` is 424 lines — still above the 300-line ceiling after extraction. Pre-existing (was 578); this run made real progress. A future sweep could extract the ADL or Human Action sections to push below 300.
- [x] [2026-03-18 | "Address the next 2 items in the NON_BLOCKING_LOG.md"] `lib/drift.sh` lacks `set -euo pipefail` at the top — pre-existing, not introduced here. Other lib files sourced in the same shell inherit the parent shell's `pipefail`, so runtime impact is zero, but it's inconsistent with the repo convention.
- [ ] [2026-03-18 | "Address the next 2 items in the NON_BLOCKING_LOG.md"] `_resolve_addressed_nonblocking_notes` (drift_cleanup.sh:132,136) uses `[ ]` single-bracket conditionals while the rest of drift_cleanup.sh uses `[[ ]]`. Pre-existing style from the moved code; shellcheck does not error on this in bash mode, but it's inconsistent within the same file.
- [x] [2026-03-18 | "Tackle the last feature in the HUMAN_NOTES.md"] `lib/drift.sh` is 677 lines, well over the 300-line ceiling. The four new functions (lines 490–588) would be good candidates to extract into a `lib/drift_cleanup.sh` shim in a future sweep.
- [x] [2026-03-18 | "Tackle the last feature in the HUMAN_NOTES.md"] Inconsistent boolean pattern in the new functions: `clear_completed_nonblocking_notes` and `clear_resolved_drift_observations` use `local in_open=false` / `[ "$in_open" = true ]`, while the existing `resolve_drift_observations` in the same file uses `local in_unresolved=0` / `[ "$in_unresolved" -eq 1 ]`. One style should be used throughout.
- [ ] [2026-03-18 | "Tackle the last feature in the HUMAN_NOTES.md"] The `command -v get_completed_nonblocking_notes` guard in `hooks.sh` (line 130) is never false — `drift.sh` is always sourced before `hooks.sh` is called. The guard is harmless but slightly misleading; a comment explaining why it's defensive would clarify intent.
- [x] [2026-03-18 | "Address the first 3 items in the NON_BLOCKING_LOG.md"] `lib/agent_monitor.sh:86-87` — Comment says stderr is teed "to both a dedicated file and the FIFO (for existing monitoring)." This is inaccurate. In `> "$_fifo" 2> >(tee -a "$_stderr_file" >&1)`, the `>(...)` process substitution is expanded before the `> "$_fifo"` redirect is applied to `claude`; `>&1` inside the substitution inherits the outer subshell's fd 1 (the caller's stdout), not the FIFO. Stderr goes to `$_stderr_file` + shell stdout, not the FIFO. The primary goal (dedicated stderr file) is achieved and classification works correctly; this is a comment accuracy issue only.
- [x] [2026-03-18 | "Implement Milestone 12.3: Metrics Integration & Structured Log Summaries"] `test_agent_summary.sh` header comment (lines 6-10) lists 5 test scenarios but only 4 are implemented — tests for "Summary block is redacted via redact_sensitive" (#4) and "Unicode/ASCII fallback works correctly" (#5) are missing. The redaction acceptance criterion ("Sensitive values are redacted in log summaries") has no corresponding test. Low risk given redaction was implemented and tested upstream in 12.2, but the comment creates a false impression of coverage.
- [x] [2026-03-18 | "Implement Milestone 12.2: Agent Exit Analysis, Real-Time Detection & Structured Reporting"] `lib/common.sh` `report_error()`: Only the `_transient_label` line uses `printf` with a closing `$_box_v` border character. All other content lines (`ERROR:`, empty separator, `message`, `Recovery:`) use bare `echo "${_box_v}  ..."` with no right border. The rendered block has an open right side on most lines, which looks unfinished in UTF-8 mode. Low priority cosmetic fix.
- [x] [2026-03-18 | "Implement Milestone 12.2: Agent Exit Analysis, Real-Time Detection & Structured Reporting"] `lib/agent_monitor.sh` / `lib/agent.sh`: The milestone spec says "redirect agent stderr to `$SESSION_TMP/agent_stderr.txt`." The actual implementation uses `> "$_fifo" 2>&1`, merging stderr into the FIFO stream. A dedicated stderr file is never written; instead a synthetic one-line hint is created only when `_API_ERROR_DETECTED=true`. For non-API errors that produce real stderr output, `agent_stderr.txt` remains empty and classification relies entirely on `agent_last_output.txt`. This works in practice (the ring buffer captures everything via `2>&1`) but diverges from the spec intent. Worth noting for Milestone 12.3 if per-stream log files are expected.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `api_auth` is still correctly marked transient per the M12.1 spec, but authentication failures won't self-resolve on retry. M13's retry envelope should check for `api_auth` and skip auto-retry, surfacing an immediate human-actionable error instead of backing off.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `_match_pattern()` uses `echo "$text"` (errors.sh:37), which can mishandle inputs beginning with `-n` or containing `	`/` ` escape sequences on some `echo` implementations. Consider `printf '%s ' "$text"` for consistency with the now-fixed `redact_sensitive()`.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `no_summary` subcategory classification is based on `file_changes=0` rather than actual CODER_SUMMARY.md presence. M12.2 callers must pass `file_changes=0` when the summary file is absent — not just when no code changed — to get the intended classification.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `CODER_SUMMARY.md` omits `lib/errors_helpers.sh` from its "Files Modified" section; the file was created as part of the split but is not listed. Minor documentation gap, no code impact.
- [x] [2026-03-18 | "Correct the next 2 items in the NON_BLOCKING_LOG.md file."] `lib/agent_helpers.sh:85` — `_append_agent_summary()` calls `suggest_recovery()` directly without a `command -v suggest_recovery` guard, unlike `agent.sh` which defensively guards all `errors.sh` calls. In practice this is safe because `AGENT_ERROR_CATEGORY` can only be non-empty if `classify_error` ran (which requires `errors.sh` sourced, and `suggest_recovery` is in the same file). Harmless inconsistency, but worth noting for future readers.
- [x] [2026-03-18 | "Correct the next 2 items in the DRIFT_LOG.md file."] `lib/agent_monitor.sh:237-244` — The ring-buffer dump block (inside the FIFO reader subshell) uses `local` declarations inside a `{...}` compound group that is NOT inside a function. In bash 4, `local` outside a function always fails with "local: can only be used in a function". With `set -e` active in the subshell, this causes the entire `{...}` block to abort at the first `local` line; the `|| true` at the end of the block (line 249) suppresses the error but the ring buffer is never actually written to `agent_last_output.txt`. The comment added this run correctly explains the rationale for avoiding a function call, but the `local` usage is the underlying issue. The fix is to remove the `local` declarations and use plain assignments (`_rb_total=...`), since the variables are already subshell-local by virtue of being in the `(...)` subshell. Pre-existing issue not introduced by this coder — flagging here since the new comment draws attention to this block.
- [x] [2026-03-18 | "Correct the next 2 items in the DRIFT_LOG.md file."] `lib/agent.sh` is 416 lines — exceeds the 300-line ceiling. Pre-existing from prior milestones; this coder's changes slightly reduced the line count.
- [x] [2026-03-18 | "Correct the first 3 items in the NON_BLOCKING_LOG.md file."] `lib/errors.sh` is missing `set -euo pipefail` at the top of the file. Pre-existing from Milestone 12.1 creation, not introduced by this run, but `reviewer.md` lists this as a blocker check. Sourced library files in this repo inherit the parent shell's options, so runtime impact is zero — but it's inconsistent with the convention in other `lib/*.sh` files.
- [x] [2026-03-18 | "Implement Milestone 12: Observability & Error Attribution Fix the currently failed test as well."] `stages/coder.sh:321` — `success "Coder agent finished."` still fires before the UPSTREAM error check at line 328 (carry-forward from cycle 1 non-blocking note). When the coder hits an API error the terminal shows `[✓] Coder agent finished.` immediately followed by `[✗] Coder agent hit an API error`. Consider guarding the success log or moving it after the UPSTREAM check.
- [x] [2026-03-18 | "Implement Milestone 12: Observability & Error Attribution Fix the currently failed test as well."] `lib/agent_monitor.sh` — File is 356 lines after dead code removal (down from 434). Still above the 300-line soft limit from reviewer.md. No rework needed given the directive on re-reviews, but worth splitting in a future cleanup sweep.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `lib/errors.sh:120–122` — The standalone `"status".*502` check is still dead code: the `50[023]` regex at line 116 already matches 500, 502, and 503. No runtime impact; safe to remove in a cleanup pass.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `lib/errors.sh:134` — `s` inside `grep -qiE` is a GNU extension, not POSIX ERE. Use `[[:space:]]` for portability: `connection.*timed?[[:space:]]*out`.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `lib/errors.sh:260` — `|` inside `grep -qiE` is non-standard (GNU BRE alternation; in ERE the bare `|` is the alternation operator). Change `'anthropic|claude|api.anthropic'` to `'anthropic|claude|api.anthropic'`.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `lib/errors.sh:277` — By convention, internal helper `_match_pattern` should be defined before `classify_error` which calls it. No runtime impact but makes the file harder to read top-to-bottom.
- [x] [2026-03-18 | "Implement Milestone 12.1: Error Taxonomy, Classification Engine & Redaction"] `lib/errors.sh:227,233` — The check at line 227 (turns ≤ 2, exit ≠ 0, files = 0) and the fixed check at line 233 (turns = 0, files = 0) have some overlap. A future cleanup could consolidate them: `if [[ "$turns" -le 2 ]] && [[ "$file_changes" -eq 0 ]] && ([[ "$exit_code" -ne 0 ]] || [[ "$turns" -eq 0 ]]); then`.
## Resolved
- [x] [2026-03-18] `split_milestone()` in `lib/milestone_split.sh` — changed `export` to plain shell variable assignment for `MILESTONE_DEFINITION`, `SCOUT_ESTIMATE`, `TURN_CAP`, `PRIOR_RUN_HISTORY`. `render_prompt()` reads these via `${!var_name}` indirect expansion in the same shell scope, so export was unnecessary environment bloat.
- [x] [2026-03-18] `lib/milestone_archival.sh` now explicitly listed in CODER_SUMMARY.md Files Modified section. Process tracking issue resolved.
- [x] [2026-03-18] Em-dash (U+2014) removed from all 5 regex character classes in `lib/milestone_archival.sh` (lines 35, 80, 101, 163, 225). Replaced `([:.\ -]|—)` with portable `[^[:alnum:]]` which matches any non-alphanumeric delimiter.
- [x] [2026-03-18] Recursion depth log line added to null-run auto-split path in `stages/coder.sh` at lines 348-350 and 470-473: `warn "Auto-split complete — re-running coder stage for milestone ... (depth N/M)..."`.
- [x] [2026-03-18] Comment added to `_switch_to_sub_milestone` in `stages/coder.sh` lines 21-22 explaining `get_milestone_title` fallback behavior when split agent uses a non-matching heading format.
- [x] [2026-03-18] `MILESTONE_SPLIT_MAX_TURNS` hard cap of 50 documented in `templates/pipeline.conf.example` line 285: "Hard cap: 50 (values above 50 are clamped automatically)".
- [x] [2026-03-18] Integration tests added to `tests/test_milestone_split.sh` covering 3 coder stage wiring paths: pre-flight sizing gate (split + re-scout), null-run auto-split (handle_null_run_split + recursive coder), and turn-limit minimal-output auto-split. Tests stub `run_agent`, `split_milestone`, `init_milestone_state`, and verify call sequences.
- [x] [2026-03-18] `lib/milestone_split.sh:42` — Split `local threshold=$((...))` into separate declaration and assignment to avoid SC2155.
- [x] [2026-03-18] `lib/milestone_archival.sh:250` — Dead `local awk_rc=$?` code path already resolved in prior run: `_replace_milestone_block` now uses `|| { rm -f ...; return 1; }` pattern which catches awk failure before `set -e` fires.
- [x] [2026-03-18] `lib/milestone_archival.sh:252` — `$rep_file` cleanup already resolved in prior run: the `|| { }` block now cleans up both `$tmp_file` and `$rep_file` on awk failure.
- [x] [2026-03-18] `handle_null_run_split()` `git diff HEAD` comment already present in prior run at lines 233-237 explaining intentional use of `git diff HEAD` vs bare `git diff`.
- [x] [2026-03-17] Coder scope drift — audited all items instead of task-specified quantity. Resolved by adding scope-adherence directive to `prompts/coder.prompt.md` and softening the "address what you can" language in `stages/coder.sh` non-blocking injection to defer to task scope.
- [x] [2026-03-17] (consolidated) `milestones.sh` exceeded the 300-line guideline. Resolved by extracting acceptance checking, commit signatures, and auto-advance helpers into `lib/milestone_ops.sh`. `milestones.sh` is now ~312 lines, `milestone_ops.sh` ~260 lines.
- [x] [2026-03-17] Three duplicate "milestones.sh too long" entries consolidated into a single resolved entry above.
