# Non-Blocking Notes Log

Accumulated reviewer notes that were not blocking but should be addressed.
Items are auto-collected from `## Non-Blocking Notes` in REVIEWER_REPORT.md.
The coder is prompted to address these when the count exceeds the threshold.

## Open
- [ ] [2026-05-26 | "unknown"] `internal/finalize/orchestrator.go:124` — Doc comment still says "canonical 26-hook registration" after m25 added `_hook_clarify_finalize`; the test correctly asserts 27. Stale comment only — carried from cycle 1.
- [ ] [2026-05-26 | "unknown"] `lib/specialists_helpers.sh:52-61` — Inlined NON_BLOCKING_LOG preamble omits two descriptive lines present in `NonBlocking.EnsureFile()` (`Items are auto-collected from …` and `The coder is prompted to address…`). Functionally equivalent. Carried from cycle 1.
- [ ] [2026-05-26 | "unknown"] `internal/finalize/drift_artifacts.go:137-183` and `cmd/tekhton/drift.go:192-205` — Local helpers (`splitLines`, `joinLines`, `startsWith`, `trimAll`, `isNoneOnly`, `hasPrefix`) duplicate stdlib `strings` functions. The `string(s[i])` byte concatenation in `splitLines` is O(N²) vs stdlib O(N). Harmless for short markdown files but unnecessary duplication; replace with stdlib in a follow-up. Carried from cycle 1.
- [ ] [2026-05-25 | "unknown"] (carried from cycle 1) Goal 3 (native in-process `HUMAN_NOTES_BLOCK`) was not fully achieved. `stages/coder.sh` forks `tekhton note extract` as a Go subprocess and exports the result as an env variable before calling `render_prompt`. The mechanical AC passes but the "no subprocess per render" intent is deferred; future in-process orchestrate callers will need the env-variable workaround until the coder stage itself ports to Go.
- [ ] [2026-05-25 | "unknown"] (carried from cycle 1) `internal/finalize/express_persist.go:75` — `var _ = os.Stat` import sentinel to suppress an unused-import compile error. The `os` package is not used in this file; remove the import and the `var _` line.
- [ ] [2026-05-25 | "unknown"] (carried from cycle 1) `MANIFEST.cfg` row for m24 lists `depends_on=m23,m26`; the AC specifies `depends_on=m23`. The extra `m26` entry is harmless at runtime (m26 is done) but will fail a literal byte-match against the acceptance criteria.
- [ ] [2026-05-25 | "unknown"] `lib/finalize_dashboard_hooks.sh:166` still defines a `_hook_tui_complete()` bash function body (uses `_tui_call` correctly). It is dead code — the Go orchestrator maps `_hook_tui_complete` to `internal/finalize/tui_complete.go` directly and the bash shim case arm was removed — but the literal AC grep (`grep -rnE 'tui_(complete...)'`) returns this definition as a substring match. Should be deleted in the m26 cleanup pass when `finalize_dashboard_hooks.sh` as a whole ports to Go.
- [ ] [2026-05-25 | "unknown"] `stages/architect.sh` (402 lines) and `stages/review.sh` (385 lines) exceed the 300-line bash hard ceiling. Both were pre-existing violations touched only incidentally by m23 (two `_tui_call` substitutions each); they were not at the ceiling before m23 arrived. These should be split in a dedicated cleanup pass — not a m23 regression, but the ceiling applies to modified files.
- [ ] [2026-05-25 | "unknown"] `substage.go` has no dedicated `substage_test.go`; the happy-path and auto-close tests for `SubstageBegin`/`SubstageEnd`/`AutoCloseSubstageIfOpen` live in `ops_test.go`. Coverage is present and correct but diverges from the `<name>_test.go` co-location convention used by every other file in the package.
- [ ] [2026-05-25 | "unknown"] internal/tui/state.go:269 — `SortedStageOrder` sorts alphabetically via `strings.Compare` but its docstring claims "the bash side appended idempotently; we preserve that contract." The bash contract was insertion order, not alphabetical order. The function is exported but never called anywhere in the codebase; delete it or, if needed downstream, correct the docstring to say "alphabetical, not insertion-order."
- [ ] [2026-05-25 | "unknown"] lib/finalize_dashboard_hooks.sh:166 — `_hook_tui_complete` function body is now dead code: the Go orchestrator routes `_hook_tui_complete` to `internal/finalize/TUIComplete` and never dispatches through the shim for this hook. The function can be removed in a follow-up cleanup when `_shim_load_finalize_bodies` is next touched.
- [ ] [2026-05-25 | "unknown"] lib/tui.sh:31 sources `lib/output_format.sh` but no function from that file is called inside the shim. The unnecessary source is moot if the file is deleted (per the blocker above), but worth noting.
- [ ] [2026-05-25 | "unknown"] `TestClearState_NoopWhenFileMissing` (`clear_state_test.go:87`) does not write the `.commit_decision` sentinel, so `shouldRunOnCompletion` returns false before the hook reaches the file-removal path. The test passes, but it exercises the "no sentinel → skip" branch rather than the named "file missing → no-op" branch. The `os.IsNotExist` path in `ClearState.Run` has no direct test. Same structural issue in `TestMarkDone_NoopWhenManifestMissing` and `TestCleanupMilestone_NoopWhenManifestMissing` — both skip due to absent sentinel, not due to missing artifact.
- [ ] [2026-05-23 | "unknown"] `lib/milestone_window.sh` is 356 lines after this change, exceeding the 300-line hard ceiling (CLAUDE.md Rule 8). The file was ~293 lines before `set_focused_milestone_block` was added. Fix: extract `set_focused_milestone_block` (and possibly `_read_milestone_file` as a shared dep) into `lib/milestone_window_focused.sh`. The file header already lists "Provides: build_milestone_window" — the new public function should appear there too.
- [ ] [2026-05-23 | "unknown"] Inconsistent `declare -f` guard style at call sites: scout/review/tester all use `if declare -f set_focused_milestone_block &>/dev/null; then ... fi` before calling, but the coder main path (coder.sh:465) calls `if ! set_focused_milestone_block 2>/dev/null` directly without the guard. Functionally safe since `milestone_window.sh` is always sourced before any stage runs, but the inconsistency will confuse future readers.
- [ ] [2026-05-20 | "unknown"] Reviewer agent did not produce a report — extra tester scrutiny recommended.
- [ ] [2026-05-20 | "unknown"] Reviewer agent did not produce a report — extra tester scrutiny recommended.
- [ ] [2026-05-18 | "unknown"] `ui_audit.go:255` — `strings.Join(files, "") // satisfy import; sort below` is dead code with a misleading comment. The `strings` package is already used by `strings.ToLower`, `strings.ReplaceAll`, and `strings.Contains` elsewhere in the file, so no import-satisfaction trick is needed. The line computes and discards a string and should be removed.
- [ ] [2026-05-18 | "unknown"] `ui_audit.go:263-268` — `sortStrings` re-implements stdlib insertion sort. `sort.Strings` from the `sort` package would do the same with one import line. Minor; functionally correct.
- [ ] [2026-05-18 | "unknown"] **Stop condition diverges from architect spec but is correct.** The plan said to stop at "the first line sourcing a `stages/` script, i.e. line 964." The JR coder stopped at the `# Stage helpers and implementations` comment on line 961 instead. This is actually better: stopping at the comment avoids capturing `lib/intake_helpers.sh` (line 962) and `lib/intake_verdict_handlers.sh` (line 963), which would have made the extracted count 111 vs `DefaultLibHelpers` 109 and caused a spurious test failure. The JR coder correctly recognized the spec was off by one semantic unit and fixed it silently.
- [ ] [2026-05-18 | "unknown"] `EmitRunMemory.buildRecord` reads task text via `os.Getenv("TASK")` directly rather than from `in.Result` or an `Input` field. This is a faithful port of the bash behavior (which also read `$TASK`), but future hook ports that need task text should carry it in `Input` rather than relying on the ambient env. Drift observation for when the `Input` struct is extended.

## Resolved

### Test Audit Concerns (2026-05-25)
#### COVERAGE: Regex-bug acceptance test accommodates rather than exposes the defect
#### COVERAGE: FEAT placement check has no test for root-level new files
