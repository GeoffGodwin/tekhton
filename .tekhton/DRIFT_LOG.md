# Drift Log

## Metadata
- Last audit: 2026-05-18
- Runs since audit: 13

## Unresolved Observations
- [2026-05-18 | "unknown"] [internal/preflight/ui_audit.go:255] — Dead `strings.Join` call with incorrect "satisfy import" comment; should be deleted in the next cleanup pass.
- [2026-05-18 | "unknown"] [tests/] — V4 migration is accumulating skip-guarded bash tests faster than Go-native replacements are being written. Five more at m22 close; `test_plan_browser` still pending from m21. Drift observation to trigger a dedicated test-migration sweep before the accumulation becomes untrackable.
- [2026-05-18 | "unknown"] [tests/testdata/preflight_parity/green_path/expected/] — The `green_path` fixture has an empty `expected/` directory (the `no_report` mode asserts absence rather than a baseline file). This is correct behavior, but the pattern is inconsistent with `env_only_fail` and `ui_config_autopatch` which both have explicit baseline files. A README note in `green_path/` explaining why there is no baseline file would reduce future confusion.
- [2026-05-18 | "unknown"] The two DRIFT_LOG.md staleness fixes (ADL-36 sub-items A and B) are factually correct. `TestDefaultLibHelpersParityWithLegacy` is confirmed at `internal/stagerunner/parity_test.go:42`. `scriptFor` is confirmed absent from `internal/stagerunner/adapter.go` and `adapter_test.go` (grep returns no matches). The resolved entries in DRIFT_LOG.md accurately describe both dispositions.
- [2026-05-18 | "unknown"] Scope was cleanly bounded. Only `.tekhton/DRIFT_LOG.md` was modified; no code files were touched. No scope creep.
- [2026-05-18 | "unknown"] The senior coder's no-op pass is correct — the Simplification section of the architect plan is empty, and the deferred item (drift_cleanup.sh non-blocking router sentinel) is properly documented as m24 work.

## Resolved
- [RESOLVED 2026-05-18] **Non-blocking router misclassifies CI-failing tests.** During the m21 dogfooded run, `TestDefaultLibHelpersFilesExist` (a hard Go test failure) was placed into NON_BLOCKING_LOG with the reasoning "this is pre-existing relative to S-1 and not caused by these changes." A test that fails CI is by definition a blocker, regardless of who introduced it. The router (`lib/drift_artifacts.sh` or `lib/test_baseline.sh`) should not classify any item that maps to a failing test as non-blocking. Investigate the routing rule in m22+ and add a sentinel that promotes any `[fail|FAIL]` test reference inside a non-blocking entry to the blocker queue.
- [RESOLVED 2026-05-18] **`lib/milestone_archival.sh` + `_helpers.sh` retained as transition.** m21 plan called for deletion; closeout audit found two real bash callers (`lib/milestone_split.sh:138,226` calling `_extract_milestone_block`/`_replace_milestone_block`; `tekhton-legacy.sh:2208` calling `archive_all_completed_milestones` from the V2→V3 migration path). Files restored, deletion deferred. Retire when (a) `lib/milestone_split.sh` ports to Go [target m25-ish], (b) `tekhton-legacy.sh` V2→V3 migration path retires [target m28]. Until then, the Go `internal/finalize/archive_milestone.go` is the authoritative path for new runs; the bash files are dormant in pure-Go run paths and only execute on the two remaining bash callers.
- [RESOLVED 2026-05-18] **Architect didn't see existing `parity_test.go`.** When writing ARCHITECT_PLAN.md, the architect proposed `TestDefaultLibHelpersMatchesLegacySourceBlock` without seeing that `TestDefaultLibHelpersParityWithLegacy` already existed in `parity_test.go:42`. Recommend the architect agent grep for existing `parity_test.go` (or any `*_parity_test.go`) before proposing new parity tests for the same invariant.
- [RESOLVED 2026-05-18] **`test_plan_browser` HTML escaping regression.** The HTML-escape assertion in `tests/test_plan_browser.sh` reads `&lt;script&gt;` as `<lt;script>gt;` — the entity-escape pass appears broken either in production (`lib/plan_browser.sh`) or in the test's expectation parser. Pre-existing at m20 baseline; not m21-introduced. Investigate during m26 (init + plan port) when the plan-browser subsystem ports to Go.
- [RESOLVED 2026-05-18] **`test_self_host_dry_run_gate` gate semantics.** The test expects `TEKHTON_SELF_HOST_DRY_RUN=1` with missing prerequisites to skip with exit 0, but the current `scripts/self-host-check.sh` errors-out at the top-of-script Go-toolchain pre-check (added in m20 self-host parity work), before the dry-run gate gets a chance to evaluate. Either the pre-check should defer to the dry-run gate, or the test should accept exit 1 + Go-missing message. Pre-existing at m20 baseline; not m21-introduced. Revisit alongside m22 (preflight port) since preflight owns the equivalent pre-check chain.
- [RESOLVED 2026-05-18] **Observation 3 — `orchestrate_*.sh` files repeating `set -euo pipefail`** The previous architect audit (2026-05-09) already evaluated this and recorded: "If the intent is to carve out sourced files from Rule 2, that requires a human amendment to CLAUDE.md before any code changes. No action in this pass." That conclusion stands. CLAUDE.md Rule 2 states "Bash files: `set -euo pipefail`" without exception for sourced files. Removing the declaration from sourced lib files would be a CLAUDE.md violation under the current wording. The observation is self-described as non-blocking and hygiene-only. No code action is valid until a human amends CLAUDE.md to define the scope of Rule 2 for sourced files. **Action required from human**: If the rule should not apply to sourced lib files, amend CLAUDE.md Rule 2 to say so explicitly. Once amended, a dedicated non-blocking milestone can do the family-wide hygiene pass.
- [2026-05-18 | ADL-36 sub-item A — resolved] **`DefaultLibHelpers` parallel representation** — `TestDefaultLibHelpersParityWithLegacy` exists at `internal/stagerunner/parity_test.go:42`. The parity invariant between `DefaultLibHelpers` in `helpers.go` and `tekhton-legacy.sh`'s global source block is already enforced by the test suite. No further action required.
- [2026-05-18 | ADL-36 sub-item B — resolved] **`scriptFor` dead code** — `scriptFor` is not present in `internal/stagerunner/adapter.go` or `internal/stagerunner/adapter_test.go`. Already deleted prior to m22. No further action required.
