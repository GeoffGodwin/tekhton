# Drift Log

## Metadata
- Last audit: 2026-05-18
- Runs since audit: 16

## Unresolved Observations
- [2026-05-18 | "m21 closeout"] **Non-blocking router misclassifies CI-failing tests.** During the m21 dogfooded run, `TestDefaultLibHelpersFilesExist` (a hard Go test failure) was placed into NON_BLOCKING_LOG with the reasoning "this is pre-existing relative to S-1 and not caused by these changes." A test that fails CI is by definition a blocker, regardless of who introduced it. The router (`lib/drift_artifacts.sh` or `lib/test_baseline.sh`) should not classify any item that maps to a failing test as non-blocking. Investigate the routing rule in m22+ and add a sentinel that promotes any `[fail|FAIL]` test reference inside a non-blocking entry to the blocker queue.
- [2026-05-18 | "m21 closeout"] **`lib/milestone_archival.sh` + `_helpers.sh` retained as transition.** m21 plan called for deletion; closeout audit found two real bash callers (`lib/milestone_split.sh:138,226` calling `_extract_milestone_block`/`_replace_milestone_block`; `tekhton-legacy.sh:2208` calling `archive_all_completed_milestones` from the V2→V3 migration path). Files restored, deletion deferred. Retire when (a) `lib/milestone_split.sh` ports to Go [target m25-ish], (b) `tekhton-legacy.sh` V2→V3 migration path retires [target m28]. Until then, the Go `internal/finalize/archive_milestone.go` is the authoritative path for new runs; the bash files are dormant in pure-Go run paths and only execute on the two remaining bash callers.
- [2026-05-18 | "architect audit"] **Architect didn't see existing `parity_test.go`.** When writing ARCHITECT_PLAN.md, the architect proposed `TestDefaultLibHelpersMatchesLegacySourceBlock` without seeing that `TestDefaultLibHelpersParityWithLegacy` already existed in `parity_test.go:42`. Recommend the architect agent grep for existing `parity_test.go` (or any `*_parity_test.go`) before proposing new parity tests for the same invariant.
- [2026-05-18 | "architect audit"] **Observation 3 — `orchestrate_*.sh` files repeating `set -euo pipefail`** The previous architect audit (2026-05-09) already evaluated this and recorded: "If the intent is to carve out sourced files from Rule 2, that requires a human amendment to CLAUDE.md before any code changes. No action in this pass." That conclusion stands. CLAUDE.md Rule 2 states "Bash files: `set -euo pipefail`" without exception for sourced files. Removing the declaration from sourced lib files would be a CLAUDE.md violation under the current wording. The observation is self-described as non-blocking and hygiene-only. No code action is valid until a human amends CLAUDE.md to define the scope of Rule 2 for sourced files. **Action required from human**: If the rule should not apply to sourced lib files, amend CLAUDE.md Rule 2 to say so explicitly. Once amended, a dedicated non-blocking milestone can do the family-wide hygiene pass.
exit status 127"` with `"error_class": "intake"`; `.claude/logs/intake.log` contains 147 copies of `stages/intake.sh: line 73: _intake_get_milestone_content: command not found`. The legacy `tekhton-legacy.sh` works because it sources `lib/intake_helpers.sh` (line 962) and friends globally before sourcing the stage script. Architect needs to choose between two designs: **(a) source-all** — have the bash wrapper glob `lib/*.sh` (or a pinned subset matching what tekhton-legacy.sh sources) so stages run in the same env they always have; matches V3 parity but couples every stage to every helper and risks source-time side effects from libs like `quota.sh`, `tui_ops.sh`. **(b) per-stage allowlist** — extend `DefaultStageScripts` in `adapter.go:90` to a struct `{Script string; Helpers []string}` populated from each stage script's `Expects:` header; cleaner but requires upkeep when helpers move and a one-time audit of every `stages/*.sh`. Recommend (b) — the `Expects:` headers already exist and document the contract. Per-stage helper lists at minimum: intake → `intake_helpers.sh`, `intake_verdict_handlers.sh`; coder → `coder_buildfix.sh`, `coder_buildfix_helpers.sh`; tester → `tester_tdd.sh`, `tester_continuation.sh`, `tester_fix.sh`, `tester_validation.sh`, `tester_timing.sh`; review → (none beyond common); security → (none beyond common); cleanup → (none beyond common); docs → `docs_agent.sh`. The fix needs a parity test that runs each stage under the Go BashAdapter against a fixture and asserts the same envelope as a legacy invocation. Without that, the next missing-helper bug (likely coder, since it has the biggest helper surface) ships silently."] `helpers.go:22` — `DefaultLibHelpers` is a parallel representation of `tekhton-legacy.sh`'s global source block (lines 846-987). Two canonical lists for the same truth create a maintenance hazard; every new lib file added to the legacy block must also be mirrored here. The parity test the coder recommends in "Observed Issues" is the correct long-term fix.
exit status 127"` with `"error_class": "intake"`; `.claude/logs/intake.log` contains 147 copies of `stages/intake.sh: line 73: _intake_get_milestone_content: command not found`. The legacy `tekhton-legacy.sh` works because it sources `lib/intake_helpers.sh` (line 962) and friends globally before sourcing the stage script. Architect needs to choose between two designs: **(a) source-all** — have the bash wrapper glob `lib/*.sh` (or a pinned subset matching what tekhton-legacy.sh sources) so stages run in the same env they always have; matches V3 parity but couples every stage to every helper and risks source-time side effects from libs like `quota.sh`, `tui_ops.sh`. **(b) per-stage allowlist** — extend `DefaultStageScripts` in `adapter.go:90` to a struct `{Script string; Helpers []string}` populated from each stage script's `Expects:` header; cleaner but requires upkeep when helpers move and a one-time audit of every `stages/*.sh`. Recommend (b) — the `Expects:` headers already exist and document the contract. Per-stage helper lists at minimum: intake → `intake_helpers.sh`, `intake_verdict_handlers.sh`; coder → `coder_buildfix.sh`, `coder_buildfix_helpers.sh`; tester → `tester_tdd.sh`, `tester_continuation.sh`, `tester_fix.sh`, `tester_validation.sh`, `tester_timing.sh`; review → (none beyond common); security → (none beyond common); cleanup → (none beyond common); docs → `docs_agent.sh`. The fix needs a parity test that runs each stage under the Go BashAdapter against a fixture and asserts the same envelope as a legacy invocation. Without that, the next missing-helper bug (likely coder, since it has the biggest helper surface) ships silently."] `adapter_test.go:119-125`, `adapter.go:119-125` — `scriptFor` is documented as "retained for tests that exercise the resolution path; new code should prefer `stageDefFor`." Its only call site outside the package is `TestBashAdapterUnknownStage` and `TestScriptForFallback`. Once those tests are migrated to call `stageDefFor` directly, `scriptFor` can be deleted as dead code. Non-blocking until that cleanup pass.

## Resolved
- [RESOLVED 2026-05-18] `internal/finalize/` has no `hooks/` subdirectory. The milestone spec (`m21-finalize-orchestrator-port.md:222`) planned files at `internal/finalize/hooks/<name>.go`, but the implementation placed all hook files directly in `internal/finalize/`. The flat layout is fine at 6 files; if this directory grows past ~15 files when m22–m25 land, a `hooks/` sub-package will become worth extracting. No action needed now.
- [RESOLVED 2026-05-18] `finalize_shim.sh` groups `_hook_baseline_cleanup`, `_hook_express_persist`, `_hook_note_acceptance`, and `_hook_failure_context_reset` into one `case` arm that loads express, notes, failure_context, AND all finalize bodies. These four hooks have different actual dependencies; the grouping over-sources for most of them. Acceptable for m21 but will want untangling in m24 (notes/drift port) to avoid sourcing failure_context.sh when all you need is express.sh.
- [RESOLVED 2026-05-18] **Observation 3 — `orchestrate_*.sh` files with `set -euo pipefail`.** The observation claims CLAUDE.md says sourced lib files should not repeat the `set -euo pipefail` declaration. CLAUDE.md Rule 2 reads: "Bash files: `set -euo pipefail`" — no exception for sourced files. Removing the declaration from sourced lib files would create a CLAUDE.md violation, not fix one. The observation is also self-described as "non-blocking" and "worth a family-wide hygiene pass in a dedicated non-blocking milestone." Taking that action here would exceed this audit's scope. If the intent is to carve out sourced files from Rule 2, that requires a human amendment to CLAUDE.md before any code changes. No action in this pass.
