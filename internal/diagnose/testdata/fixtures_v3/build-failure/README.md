# build-failure

Build gate detected non-empty BUILD_ERRORS.md after coder stage.

Expected bash verdict:
  classification = BUILD_FAILURE
  confidence     = high
  rule           = _rule_build_failure
  stage          = coder
